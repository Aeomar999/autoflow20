# AutoFlow — Progress

**Snapshot date:** 2026-09-04
**Current milestone:** M7 Templates, dashboard, quotas — **complete**: AF-M7-01 (gallery), AF-M7-02 (20 authored templates), AF-M7-03 (monitoring dashboard), AF-M7-04 (quota run gate), AF-M7-05 (onboarding), AF-M7-07 (command palette), AF-M7-08 (notifications). AF-M7-06 was pulled forward to M0. Deferred sub-items carried into M8: Polar meter + AI-spend limits (AF-M7-04), approval/system notification producers (AF-M8-13). Completed: M5 Multi-model AI + cost (AF-M5-01–09; the last three landed 2026-08-31: response cache, cost views, tutorial-node retirement); M6 Tenancy, RBAC, audit, SSO (AF-M6-01–10: Organization, Member, Workspace, Invitation, AuditLog, ApprovalRequest models, `orgProcedure` RBAC middleware, cross-tenant isolation, audit logging, approvals dashboard, profile settings); M-KB Knowledge base (AF-KB-01–05, 2026-08-30); M4 complete; M3 complete. **AF-M7-pre-1 (2026-08-31):** the workflow/execution/credential read/write path is org-scoped — `workflow.organizationId` is populated from the resolved org on create, workflows/executions/credentials read and write through `ctx.org.id` (not `userId`), the run path passes `organizationId` through `sendWorkflowExecution`, and the runner resolves credentials by `{ id, organizationId: workflow.organizationId }`. Integration tests prove org B cannot see or run org A workflows or executions. **AF-M7-01 (2026-08-31):** the `Template` gallery ships — additive tenant-agnostic model (migration `20260831000000_template_model`), `templates.list`/`getOne` org-viewer reads, and `templates.instantiate` (org-editor) that rewrites node ids to fresh cuids, strips `credentialIdRef` bindings, validates the copy through the engine `validate()`, creates a tenant-scoped draft workflow, and returns `pendingCredentials`. Gallery UI at `/templates` + `/templates/[slug]` with install. **Deviations recorded:** Fork omitted, post-install credential dialog replaced by a pre-install "Before you install" checklist, installs land as an unsaved draft. **AF-M7-04 (2026-08-31):** the execution-count quota gate is enforced in the runner — a new `QUOTA_EXCEEDED` `ExecutionStatus` + additive enum migration `20260831120000_add_quota_exceeded_status`, plan→limit resolution in `src/lib/quotas.ts` (org-level `Organization.plan` per ADR-0010, unknown/null collapses to FREE), `COUNTABLE_EXECUTION_STATUSES`/`isMeteredRun`/`quotaBreachMessage`, and a `quota-gate` + `fail-quota-exceeded` Inngest pair at the top of `executeWorkflow` (before `create-execution`) that hard-fails over-limit runs with `QUOTA_EXCEEDED` instead of silently dropping, for every trigger path (manual/webhook/cron/API). Test runs (`mode: TEST`) and `E2E_SERVER === "1"` bypass; metering is current-month Postgres `Execution` rows scoped by `workflow.organizationId`. UI renders the denial as a red `BanIcon` with the breach message. **Deferred sub-items (documented, not in this PR):** Polar customer meter for billing visibility and AI-spend limits once AF-M5-02 capture is authoritative.
**Overall vs. PRD Phase 1:** ~65% implemented
**Overall vs. full PRD (Phases 1–3):** ~25%

> **2026-08-22 RECONCILIATION.** The previous version of this document described a
> repository state that did not match the code: it claimed an execution engine did
> not exist, that the canvas could not save, and that M0/M1 tasks were shipped with
> PR numbers colliding with the tutorial history (`code-with-antonio` PRs #17–#30).
> A full audit on 2026-08-22 found the opposite in several places: a working
> Inngest execution engine with 10 registered node executors, functional save,
> executions + credentials UIs, and three triggers. Every claim below is verified
> against file paths. The changelog entries dated before 2026-08-22 describe
> aspirational work and are struck from the record; see §8.

This document records **what is actually built**, verified by reading the code. It is not a plan (`docs/planning/implementation_plan.md`) and not a backlog (`docs/planning/tasks.md`). Update it in the same PR as any change that alters the state below.

---

## 1. Executive status

AutoFlow is a multi-tenant workflow-automation platform: React Flow canvas **that saves**, an **Inngest execution engine** running workflows through a topological sort with per-node executors and realtime status channels, human-in-the-loop **Approval Gates** with Inngest step resumption, multi-tenant RBAC (`OWNER`, `ADMIN`, `EDITOR`, `VIEWER`) and immutable **Audit Logging**, an envelope-encrypted credential vault with a type registry, a **Knowledge Base (pgvector)** supporting multi-format document ingestion with semantic vector similarity search via `AI_RETRIEVE`, workflow versioning with rollback, and webhook + cron triggers.

---

## 2. Completion matrix vs. PRD §5

| PRD section | Requirement area | Status | % | Notes |
|---|---|---|---|---|
| 5.1 | Zero-overhead production platform | 🟠 | ~30% | Sentry wired; execution history + error capture exist; WorkflowVersion snapshots + rollback; per-node traces. |
| 5.2 | Visual builder & nodes | 🟠 | ~80% | Canvas saves via `workflows.saveGraph` with optimistic concurrency; 23 node types registered in `src/nodes/` registry (incl. `core.approval`, `ai.llm`, `ai.extract`); autosave, save states, schema-driven config panel, canvas linting and validation. |
| 5.3 | Multi-model AI & cost | 🟢 | ~90% | OpenAI + Anthropic + Gemini + OpenAI-compatible executor nodes + AI SDK v5 (the three per-provider nodes are deprecated as of AF-M5-09); embedding generator via OpenAI `text-embedding-3-small`. AF-M5-01 (2026-08-30): provider registry with capabilities + per-1M pricing as data (`src/lib/ai/registry.ts`). AF-M5-02 (2026-08-30): `ai.llm` node with model select, prompts with expressions, params, and JSON mode. AF-M5-03 (2026-08-30): `ai.extract` node for structured data extraction to schema-builder or raw JSON schemas. AF-M5-04 (2026-08-30): fallback chains (`src/lib/ai/fallback.ts`) with automated error recovery and served model trace tracking across AI nodes. AF-M5-05 (2026-08-30): token and cost capture on `NodeExecution` rolled up to `Execution` (both success and failure paths) using micro-dollar pricing. AF-M5-06 (2026-08-30): pre-run cost estimate in the editor — per-node and workflow-level token/cost estimates on the live canvas (`src/features/editor/lib/cost-estimate.ts`, `<CostEstimateBadge>` in the editor panel). AF-M5-07 (2026-08-31): workspace-scoped response cache with TTL, three-valued `NodeExecution.cacheHit`, a daily sweep, and `ai.cacheStats` hit-rate reporting. AF-M5-08 (2026-08-31): cost views at `/costs` — spend over time, per workflow, per model, and the most expensive runs (`src/features/costs/`). AF-M5-09 (2026-08-31): the three tutorial per-provider AI nodes deprecated out of the palette, with a validated config migration onto `ai.llm` (ADR-0011). **M5 complete.** |
| 5.4 | Governance, security, compliance | 🟢 | ~90% | Better Auth + Polar billing gate + multi-tenant RBAC (`OWNER`, `ADMIN`, `EDITOR`, `VIEWER`), immutable audit logging (`AuditLog`), approval request workflows (`ApprovalRequest`), envelope-encrypted credential vault (AES-256-GCM, per-record DEK, typed registry, single decrypt site). | *AF-M7-pre-1 (2026-08-31): the workflow/execution/credential read/write paths are now org-scoped (`orgProcedure`, queries filtered by `ctx.org.id` — no fetch-then-filter) and `Workflow.organizationId` is populated + backfilled. `Organization.plan` is the plan source of truth (ADR-0010). AF-M7-04 (2026-08-31) wired the quota gate into `executeWorkflow`: over-limit production runs hard-fail with `QUOTA_EXCEEDED` (metered by current-month `Execution` rows per org); TEST runs and `E2E_SERVER === "1"` bypass. Deferred: Polar customer meter + AI-spend limits.* |
| 5.5 | Knowledge base & connectors | 🟠 | ~50% | Knowledge Base with PostgreSQL pgvector (HNSW cosine index), recursive text chunking, PDF/DOCX/HTML text extraction, Inngest durable ingestion, SSRF-safe URL scraper, `AI_RETRIEVE` node, and `/knowledge` UI. Eight connectors (Postgres, Airtable, HubSpot, Google Sheets, SMTP, Slack, Discord, Webhook-out). |
| 5.6 | Multi-agent orchestration | ⚫ | 0% | Nothing. |
| 5.7 | Analytics, ROI, marketplace | ⚫ | 0% | `recharts` installed; no dashboards or marketplace (templates shipped 2026-08-31 as AF-M7-01). |
| 5.8 | Developer platform | ⚫ | 0% | tRPC internal-only; no public API/SDK/CLI/Git sync. |
| 6 | Non-functional | 🟢 | ~90% | 696 tests passing across 75 files, 0 skipped (the integration suites run against the local `TEST_DATABASE_URL` created by `npm run test:db:up`, and skip visibly when absent); Biome lint clean (0 errors); Next.js 15 production build clean. |

🟢 done · 🟠 partial · 🔴 minimal · ⚫ absent

---

## 3. What works today

### Infrastructure — verified versions from `package.json`
| Capability | Detail |
|---|---|
| Framework | Next.js **15.5.4** (App Router, Turbopack), React **19.1.0**, TypeScript 5 |
| Database | Prisma **6.16.3** + PostgreSQL; **12 migrations** applied (2025-10-04 → 2025-11-01) |
| API | tRPC **11.6** + TanStack Query v5, superjson, SSR prefetch + hydration |
| Auth | Better Auth **1.3.26**, Prisma adapter, email/password + GitHub/Google OAuth; password reset + email verification via Resend (AF-M8-04, ADR-0014) |
| Billing | Polar (`@polar-sh/better-auth` 1.1.9) — checkout + portal + `premiumProcedure` gate |
| Jobs | Inngest **3.44.1** + `@inngest/realtime` 0.4.4; `/api/inngest` route; dev CLI via mprocs |
| Errors | Sentry **10.18** (client/server/edge configs) |
| Lint/format | **Biome 2.2.0** (not ESLint) — `npm run lint` = `biome check` |
| Canvas | `@xyflow/react` 12.8, jotai editor state |
| Expressions | Handlebars 4.7 compiled at runtime inside node executors (see S3) |

### Features — verified working
| # | Feature | Evidence |
|---|---|---|
| 1 | Sign up / sign in (email+password, GitHub/Google OAuth) | `src/features/auth/**`, `src/lib/auth.ts` |
| 2 | Route protection + redirect | `src/lib/auth-utils.ts` |
| 3 | Polar checkout, billing portal, subscription state | `src/features/subscriptions/**`, `src/lib/polar.ts` |
| 4 | Premium gate on workflow creation | `src/trpc/init.ts:41` (`premiumProcedure`) |
| 5 | Workflow CRUD: create (slug + INITIAL node), list w/ pagination+search (count shares `where` — correct), rename inline, delete | `src/features/workflows/server/routers.ts:161,10,78` |
| 6 | **Canvas save + autosave** — nodes+edges persisted transactionally with optimistic concurrency; dirty tracking, debounced autosave, save states, CONFLICT auto-reload, `beforeunload` guard | `editor-header.tsx`, `store/atoms.ts`, `routers.ts:57` |
| 7 | Execution engine: topo sort (cycle rejection), per-node `step.run`, retries 3 in prod / 0 otherwise, `onFailure` marks run FAILED | `src/inngest/functions.ts:17`, `src/inngest/utils.ts:6` |
| 8 | 9 node types registered via Node SDK registry | `src/nodes/manifest.ts` (definitions), `src/nodes/registry.ts` (server registry with validation) |
| 9 | Realtime per-node status streaming to canvas | `src/inngest/channels/*.ts` (9 channels), `publish()` in every executor |
| 10 | Credentials CRUD + encrypted values | `src/features/credentials/**` — envelope vault (`server/vault.ts` + `server/routers.ts`), registry (`credential-types.ts` + `credential-registry.ts`) |
| 11 | Triggers: manual run button, Google Form webhook, Stripe webhook (unauthenticated — see S1) | `src/features/triggers/**`, `src/app/api/webhooks/{google-form,stripe}/route.ts` |
| 12 | Execution history: list + detail with per-node-type output components, status polling | `src/features/executions/components/**`, routers `executions.getMany/getOne` |
| 13 | Dashboard shell: sidebar nav, header, upgrade modal | `src/components/app-sidebar.tsx` |
| 14 | Graph data model | `prisma/schema.prisma` (`Workflow`, `Node`, `Connection`, `Execution`) |

---

## 4. What is stubbed or missing

### Missing routes
*(none — `/` landing page shipped 2026-08-26, AF-M7-06; post-checkout success page at `/workflows/billing/success` shipped 2026-08-26, AF-M0-10)*

*(The previous revision listed `/executions` and `/credentials` pages as `<p>` stubs — false since the lesson-27+ code; they are fully built.)*

### Blocking gaps (re-scoped)
| # | Gap | Consequence |
|---|---|---|
| B1 | ~~Canvas persistence~~ **RESOLVED** — save works | — |
| B2 | ~~Node catalogue is a hardcoded Prisma enum + scattered executor files~~ **RESOLVED 2026-08-26** (AF-M1-01/02): new `src/nodes/` registry with folder convention; enum dropped; Node.type is String; old executor layer deleted. | — |
| B3 | Engine runs **every node in topo order regardless of edges taken** — no branching/skip semantics, no per-node trace records | Condition-style workflows impossible; debugging limited to final outputs |
| B4 | No tests, no CI | Nothing verifies refactors; engine correctness is anecdotal |
| B5 | Triggers have no authentication (S1) and no scheduling/cron | Workflows start manually or via open webhooks |
| B6 | Tenancy/RBAC absent | Single-owner product |
| B7 | Expressions are Handlebars strings compiled per run | Template-injection surface (S3); no expression language per spec |

### Not started at all
Agents · RAG · marketplace · analytics · public API · SDKs · CLI · SSO · self-hosting · model routing/fallback.

*(Items removed since the 2026-08-22 reconciliation snapshot because they have since shipped: templates (AF-M7-01), audit logs (AF-M6), quotas (AF-M7-04), response caching (AF-M5-07), schedule triggers (AF-M4-04), workflow versioning/publish (AF-M4-01/02).)*

---

## 5. Known defects & security findings

| ID | Severity | Finding | Location |
|---|---|---|---|
| S1 | **Critical** | ~~Stripe webhook accepts unsigned POSTs with arbitrary `workflowId`~~ **RESOLVED 2026-08-24** — per-workflow `webhookSecret` + Stripe signature verification (`AF-A-01`); migration `20260822030000` | `src/app/api/webhooks/{stripe,google-form}/route.ts`, `src/lib/secure-compare.ts` |
| S2 | High | Credentials encrypted with Cryptr (AES-CBC); **partially addressed 2026-08-24** (boot-time env validation + lazy key via `src/lib/env.ts`), **envelope module 2026-08-27 (AF-M3-01)**, **storage swap done 2026-08-27 (AF-M3-02)**, **credential injection done 2026-08-27 (AF-M3-04)**, and **UI upgrade done 2026-08-27 (AF-M3-03)**: `Credential` now stores the sealed AEAD envelope; engine resolves credentials via `resolveNodeCredentials` (single decrypt site) and never merges them into traces; UI handles partial updates, masked inputs, and in-use warnings; test connection audit record implemented. | `src/lib/encryption.ts` (legacy, converter only), `src/lib/crypto.ts`, `src/features/credentials/server/vault.ts`, `src/features/executions/server/credential-resolver.ts`, `src/features/credentials/components/credential.tsx` |
| S3 | High | User-supplied endpoint/body strings passed through `Handlebars.compile` at runtime — template-injection surface; also no SSRF guard or timeout on outbound HTTP | `http-request/executor.ts:67–81` |
| S4 | Medium | Save input carries `z.record(z.string(), z.unknown())` — node config is not strictly typed at the zod boundary, **but is validated against each node's schema before any write**: `saveGraph` runs `validate()` → `checkConfigs` (`safeParse` against the manifest `configSchema`) and rejects invalid workflows with `BAD_REQUEST` (`routers.ts:206`); the same schema now also drives live canvas linting (AF-M1-07). Residual: boundary stays loose by design; strong validation happens inside `saveGraph`, not at the parse. | `workflows/server/routers.ts:97` |
| S5 | Medium | Fake tRPC context `{ userId: 'user_123' }` still exported (inert but a latent authz trap) | `src/trpc/init.ts:11` |
| D7 | ~~Medium~~ **RESOLVED 2026-08-26** | ~~`NodeType` Postgres enum — new node type requires a migration~~ Dropped in AF-M1-02; `Node.type` is `String`. | — |
| D9 | Low | Sentry example routes still present | `src/app/sentry-example-page/`, `src/app/api/sentry-example-api/` |
| D11 | Low | README is create-next-app boilerplate; root `/` 404s | `README.md` |
| D12 | Low | ~~303 Biome errors / 71 warnings~~ **RESOLVED 2026-08-24** — `npm run lint` exits clean; CI gate added (`AF-A-06`) | repo-wide |
| D13 | Low | Raw `console.error` in webhooks — **logger part RESOLVED 2026-08-24** (`src/lib/logger.ts` w/ redaction + Sentry `beforeSend`); `.env.example` created; still no local `.env` in this checkout (app cannot boot without provisioning) | `webhooks/*/route.ts`, `.env.example` |
| D14 | Low | Dev retries=0 vs prod retries=3 hides failure paths locally; error stack stored raw on `Execution.error` — **RESOLVED 2026-08-24** (`ENGINE_RETRIES` constant + 8 KB stack truncation, `AF-A-07`) | `src/inngest/config.ts` |
| D15 | Medium | **Found & fixed 2026-08-24:** `toposort@2.0.2` throws "Cyclic dependency" on self-edges; the old `topologicalSort` added a self-edge per disconnected node, so any workflow with ≥1 connection AND ≥1 isolated node failed to execute with a false cycle error. Fixed by appending unconnected nodes after the sorted set + mapping toposort errors to the domain message; covered by unit tests | `src/inngest/utils.ts` |

Resolved by audit (were previously mis-tracked): D1 save no-op (**never existed** — save works), D4 swallowed prefetch errors (**not present** — grep clean).

---

## 6. Milestone tracker

| # | Milestone | Status | Notes |
|---|---|---|---|
| M-A | Audit hardening (new) | ✅ Done — 2026-08-24 | Waves 1+2: S1 closed, SSRF guard, template-injection decision (ADR-0007), save-boundary Zod validation, per-node traces (AF-A-01..07). |
| M0 | Stabilize the base | ✅ Done | M0-00/01/05/06/07/08/02/03/04/09 ✅ (M0-06 Testing Library done 2026-08-24; DB-backed integration suites now run green against a local `TEST_DATABASE_URL` — 5 files, 38 tests, `npx vitest run --project integration`; suites are DB-gated and skip visibly otherwise). M0 complete. |
| M1 | Graph persistence + Node SDK | ✅ Done | AF-M1-01 ✅, AF-M1-02 ✅, AF-M1-03 ✅, AF-M1-04 ✅ (2026-08-26): saveGraph with concurrency; autosave + dirty tracking + CONFLICT reload. AF-M1-05 ✅ (2026-08-29): manifest-driven node palette with fuzzy/keyword search, drag-and-drop onto canvas, click-to-append `+` handle, single-trigger constraint, and universal generic node fallback. AF-M1-06 ✅ (2026-08-29): schema-driven config panel. AF-M1-07 ✅ (2026-08-29): canvas validation and linting. AF-M1-08 ✅ (2026-08-29): first 7 node definitions with Zod schemas, ports, icons, descriptions, and 36 unit tests. M1 complete. |
| M2 | Execution engine + traces | ✅ Done | AF-M2-01 .. AF-M2-09 ✅ (AF-M2-01..08 2026-08-26/27, AF-M2-09 2026-09-02): node executors, executions API, executions UI, in-editor test runs, branch-taken skip semantics, expression helpers, shared validator, execution data model, executor-boundary node output cap. M2 complete. |
| M3 | Credential vault + connectors | ✅ Done | AF-M3-01 ✅ (2026-08-27): envelope crypto module. AF-M3-02 ✅ (2026-08-27): vault model + typed registry (8 kinds). AF-M3-04 ✅ (2026-08-27): credential injection into execution engine. AF-M3-03 ✅ (2026-08-27): UI upgrade with masked inputs, partial edits, test connection, and in-use warnings. AF-M3-05 ✅ (2026-08-27): OAuth2 flow + auto-refresh. AF-M3-06 ✅ (2026-08-28): five new credential types + testers (stage 0) then all **eight connector nodes** committed — definition + execute + unit tests + palette metadata + `docs/nodes/*.md`; Postgres uses parameterized queries only. M3 complete. |
| M4 | Triggers, publish, versioning | ✅ Done | AF-M4-01 ✅ + AF-M4-02 ✅ (2026-08-28): WorkflowVersion model, publish/activate/deactivate router, version history UI with rollback (`56cbb86`). AF-M4-03 ✅ (2026-08-29): Webhook receiver route with secret auth, raw capture, sync response option, and rate limiting. AF-M4-04 ✅ (2026-08-29): Schedule trigger via Inngest cron with timezone parsing. AF-M4-05 ✅ (2026-08-29): Manual trigger payload editor and context injection. AF-M4-06 ✅ (2026-08-29): Concurrency limits on workflows, tenant/user fairness keys, and cron jobs. M4 complete. |
| M5 | Multi-model AI + cost | ✅ Done | AF-M5-01 ✅ (2026-08-30): provider/capability/pricing data registry + model resolution + cost estimator (`src/lib/ai/registry.ts`, ADR-0009). AF-M5-02 ✅ (2026-08-30): `ai.llm` node with model select, expression-compiled prompts, params, JSON mode (`src/nodes/ai/llm/`, `ai.llm` in registry/manifest) — 18 execute tests. AF-M5-03 ✅ (2026-08-30): `ai.extract` node with structured extraction, field-list schema builder, and config panel integration (`src/nodes/ai/extract/`, `ai.extract` in registry/manifest) — 32 execute tests. AF-M5-04 ✅ (2026-08-30): fallback chains with automated error recovery and served-model tracing (`src/lib/ai/fallback.ts`) — 13 unit tests. AF-M5-05 ✅ (2026-08-30): token and cost capture across node executions with Inngest rollups to `Execution` on completion and failure (`src/inngest/trace.ts`, `src/inngest/functions.ts`, `src/lib/ai/fallback.ts`). AF-M5-06 ✅ (2026-08-30): pre-run cost estimate in the editor — `estimateTokens`/`estimateRunCostUsd`-derived per-node and aggregate estimates over the live canvas via `workflowCostEstimateAtom`, `<CostEstimateBadge>` in the editor bottom-center panel (+ hidden when no AI nodes), per-node estimate line in the config panel (`src/features/editor/lib/cost-estimate.ts`, `cost-estimate-badge.tsx`) — 15 unit + DOM tests. AF-M5-07 ✅ (2026-08-31): workspace-scoped `AiResponseCache` with TTL + hit-rate reporting — sha256 request fingerprint, the tenant in the lookup rather than only in the hash, cached runs reporting zero spend, three-valued `NodeExecution.cacheHit`, a daily sweep cron, and `ai.cacheStats` / `ai.clearCache` — 39 unit + 10 integration tests. AF-M5-08 ✅ (2026-08-31): cost views — `NodeExecution.model` for per-model attribution, `costs.summary` (totals, zero-filled UTC daily series, per workflow / per model / top runs) and the `/costs` dashboard — 18 unit + 3 DOM + 5 integration tests. AF-M5-09 ✅ (2026-08-31): `OPENAI`/`ANTHROPIC`/`GEMINI` deprecated out of the palette but kept registered and executable, with a validated config mapper onto `AI_LLM` and a dry-run migration script; folder deletion deferred to AF-M8-12 (ADR-0011). |
| M6 | Tenancy, RBAC, audit, SSO | ✅ Done | AF-M6-01–10 ✅ (2026-08-26/27/31): `Organization`, `Member`, `Workspace`, `Invitation`, `AuditLog`, `ApprovalRequest` models; `orgProcedure` RBAC middleware (`OWNER`/`ADMIN`/`EDITOR`/`VIEWER`); cross-tenant isolation on workflow/execution/credential reads (no fetch-then-filter); immutable audit logging; approvals dashboard; profile settings. AF-M7-pre-1 (2026-08-31) closed the last tenancy gap: `workflow.organizationId` populated on create + backfilled, and the workflow/execution/credential read/write and run paths go through `ctx.org.id`. |
| M-KB | Knowledge base | ✅ Done | AF-KB-01–05 ✅ (2026-08-30): pgvector HNSW index, `AI_RETRIEVE` top-k retrieval node, recursive chunker, multi-format text extraction (PDF/DOCX/HTML/TXT/MD), SSRF-safe crawler, embeddings, durable Inngest ingestion, `/knowledge` dashboard — 448 unit tests. |
| M7 | Templates, dashboard, quotas | 🟢 Complete | AF-M7-01 ✅ (2026-08-31): template gallery + one-click instantiate with credential placeholders (see changelog). AF-M7-03 ✅ (2026-08-31): monitoring dashboard with per-org executions-over-time, p50/p95 duration, error breakdown, cost trend, and quota usage (see changelog); a follow-up UX pass the same day moved the page onto `EntityContainer`, replaced the hand-rolled SVG charts with the recharts primitives `/costs` already uses, and corrected labels that implied run and node-step durations were one distribution. AF-M7-04 ✅ (2026-08-31): per-plan execution-count quota enforced in the runner with hard-fail `QUOTA_EXCEEDED` status (see changelog; Polar meter + AI-spend deferred). AF-M7-02 ✅ (2026-08-31): 20 authored gallery templates plus the authoring harness that gates them and the `seed:templates` projection (see changelog). AF-M7-05 ✅ (2026-08-31): first-run checklist with server-derived step completion, one-click sample workflow through the instantiate path, and rewritten empty states (see changelog). AF-M7-07 ✅ (2026-08-31): global Cmd+K command palette over a tenant-scoped search router with client-side fuzzy ranking (see changelog). AF-M7-08 ✅ (2026-08-31): in-app notification centre — tenant-scoped model with a replay-safe single write path, live execution and credential-expiry producers, bell with unread badge, per-workflow toggles (see changelog; approval/system producers filed as AF-M8-13). |
| M10 | Automation library: the 35 reference automations | 🟠 In progress | **Phase A COMPLETE — 15 of 15 (2026-09-03).** AF-M10-01 ✅ credentials on `HTTP_REQUEST` (ADR-0022) — the keystone: 31 of the 35 automations are an authenticated REST call, so this converts most of Phase B from integration work into configuration. AF-M10-02 ✅ 12 service credential types + generated write schema. AF-M10-03 ✅ five scoped Google credential types (ADR-0023), `google.oauth2` deprecated not removed, and the org-scoping fix that made OAuth credentials resolvable at all. AF-M10-04 ✅ seven OAuth providers (Intuit, GitHub, Atlassian, Notion, Shopify, LinkedIn, X) with provider-extra capture, PKCE, and Intuit's rotating refresh. AF-M10-35 ✅ `NodeDefinition.logo` + 18 new brand marks, CI-checked. AF-M10-05 ✅ the polling-trigger framework + `TriggerState` (ADR-0024) — 18 automations' opening move, with no history replay and at-least-once dedupe. AF-M10-06 ✅ `FileRef` + blob store (ADR-0025) — binaries by reference, org-scoped, lifetime tied to execution retention. AF-M10-08/10/11 ✅ the graph primitives — `WAIT` (durable, chunked, cancellable, `WAITING` in the trace), `FILTER` and `DEDUPE` (placement-sensitive, typed comparison, `TriggerState`-backed window), `EXTRACT_DOCUMENT_TEXT`. AF-M10-07 ✅ multimodal `AI_LLM`/`AI_EXTRACT` — attachments, save-time vision check across the whole fallback chain, per-provider PDF handling recorded in the trace, content-hashed cache key. AF-M10-12 ✅ `HTML_TO_PDF` — pure-JS renderer (~27 MB, no browser binary), no network and no script execution by construction. AF-M10-13 ✅ `VectorStore` interface + Pinecone — org-scoped namespaces, dimension check before the first write. AF-M10-14 ✅ `FORM_TRIGGER` — hosted public form, one 404 for every hidden state, allowlisted uploads to `FileRef`s. AF-M10-09 ✅ `APPROVAL` — send-and-wait on the existing model, POST-confirmed single-use links, every refusal audited. **Phase B — 3 of 9 (2026-09-04).** AF-M10-17 ✅ the Slack Web API family, superseding the incoming-webhook node (ADR-0011): five nodes over a client that treats Slack's `ok: false`-inside-HTTP-200 envelope as the failure it is, and per-node scope declarations so a missing grant reads as a sentence rather than the token `missing_scope`. Exposed and fixed two silent-failure classes in shipped work — templates authoring config keys no schema reads, and a root validator with no model of `CODE`'s return spread. AF-M10-16 ✅ QuickBooks Online: ten nodes, sandbox/production and the company id sealed in the credential so a graph cannot point itself at the wrong books, a pinned minor version, and Intuit's app-wide webhook endpoint verified over the raw body before any routing happens. Exposed and closed a config-panel gap: `z.array(z.enum(...))` is now a `multiEnum` checkbox group. AF-M10-15 ✅ the Google Workspace family: eleven nodes over one shared client with directional error classification (quota backs off, permission does not) and bounded pagination; Drive's parent-swap "move" and Workspace-doc export handled where they belong; Gmail header injection, RFC 2047 and line-length handled in the message builder. Forced two catalogue corrections: `credentialCount` counts distinct credential *types*, not bindings, and `TemplateSpec.tier` separates the one-credential onboarding promise (`starter`) from the multi-service reference automations (`library`, capped at four). **Phase B remaining (~15.5d):** GitHub/Jira/Notion, Apify/Apollo/Google Search+Maps, Airtable/Shopify/MailerLite/Stripe, Telegram/WAHA, X/LinkedIn/YouTube/Upload-Post, media generation. **Phases C–D not started:** the 35 templates (~11d), fixture suites and the all-35-green CI gate (~4.5d). |

---

## 7. Metrics baseline

| Metric | Value | Source |
|---|---|---|
| Source files (`src/**` ts/tsx) | 198 | file listing |
| Prisma models | 12 (`User`, `Session`, `Account`, `Verification`, `Credential`, `Workflow`, `Node`, `Connection`, `Execution`, `NodeExecution`, `WorkflowVersion`, `Template`) | `schema.prisma` |
| tRPC routers | 4 (`workflows`, `executions`, `credentials`, `templates`) | `src/trpc/routers/_app.ts` |
| Executable node types | 23 | `src/nodes/manifest.ts` |
| Inngest functions | 1 (`execute-workflow`) + 9 realtime channels | `src/inngest/functions.ts` |
| Tests | **696 passed, 0 skipped across 75 files** (integration suites in `tests/integration/` run against the local `TEST_DATABASE_URL` — `npm run test:db:up`; they skip visibly when the DB is absent) | `npm test` / `npm run test:integration` |
| CI pipelines | 1 (`.github/workflows/ci.yml`: lint + tsc + test + build on postgres:16) **+ Vercel production build green** — `postinstall: prisma generate` emits the gitignored client before `next build` (`04b9a35`, 2026-08-28) | repo root |
| Type check | ✅ clean after `npx prisma generate` | `tsc --noEmit` exit 0 |
| Lint | ✅ clean (`biome check` exits 0; vendored-UI overrides documented) | `biome check` |
| Migrations | 32 (latest `20260831180000_notification_model` — additive `NotificationType` enum, the tenant-scoped `Notification` table with a unique `dedupeKey` replay guard, and `Workflow.notifyOnFailure`/`notifyOnSuccess` for AF-M7-08; prior `20260831170000_add_execution_idempotency_key` — AF-M8-01: nullable `Execution.idempotencyKey` + `@@unique([workflowId, idempotencyKey])` so `POST /api/v1/workflows/:id/run` dedupes retries; prior `20260831160000_add_api_keys` — AF-M8-01: the `ApiKey` model storing only a SHA-256 hash + 8-char support prefix (ADR-0012); prior `20260831140000_cost_usd_float_to_decimal` — AF-M8-11: converts `Execution`/`NodeExecution`/`AiResponseCache`.`costUsd` from `DOUBLE PRECISION` to `Decimal(12,6)`; prior `20260831120000_add_quota_exceeded_status` — additive `ExecutionStatus` enum value `QUOTA_EXCEEDED` for the AF-M7-04 quota hard-fail status; prior `20260831000000_template_model` — the tenant-agnostic `Template` gallery model for AF-M7-01; prior `20260830150000_node_execution_model` — `NodeExecution.model` for per-model cost attribution; prior `20260830140000_ai_response_cache`, `20260830120000_org_subsystem_init` + `20260830130000_org_scope_backfill` (org tenancy + idempotent `organizationId` backfill), `20260828042727_add_workflow_version`; `20260827170000_credential_vault_af_m3_02` switches `Credential` from `value` to envelope columns + registry-id `type`; `20260824100000` drops the dead tutorial `Post` table — all unapplied locally, CI applies via `migrate deploy`, integration suites via `migrate deploy` in global setup) | `prisma/migrations` |
| Connectors | 8 nodes (Slack · SMTP · Sheets · Postgres · Airtable · HubSpot · OpenAI-compatible · Webhook-out) | `src/nodes/manifest.ts` |
| Templates | 20 authored (`src/features/templates/catalog/` — marketing 6, support 5, ops 5, data 4; every palette node type demonstrated; ≤1 credential each, 9 need none) · system shipped (`Template` model + `templates.list`/`getOne`/`instantiate` + `/templates` gallery + `/templates/[slug]` detail UI). Seeded into `Template` rows by `npm run seed:templates`, which re-runs the authoring harness and refuses to write on any issue | AF-M7-01 · AF-M7-02 |

---

## 8. Changelog

Newest first.

| Date | Change | Milestone |
|---|---|---|
| 2026-09-04 | **AF-M10-17 — the Slack Web API family, and two silent-failure classes it exposed.** Five nodes (`SLACK_POST`, `LIST_CHANNELS`, `CREATE_CHANNEL`, `INVITE`, `DM_BY_EMAIL`) replacing the incoming-webhook node, which is deprecated per ADR-0011 — kept registered and executable, dropped from the palette. **Slack does not use HTTP status codes to report failure:** nearly every error is HTTP 200 with `{"ok": false, "error": "channel_not_found"}`, so a client checking `response.ok` — the obvious thing to write — records a failed post as a success and lets the workflow continue as if the message had gone out. A missing `ok` is failure too; absent is not true. **Scopes are now declared per node** (`CredentialRequirement.scopes`), because one connection serves operations at very different privilege levels and an admin may grant `chat:write` while declining `users:read.email`; the panel shows the requirement before the run and the executor turns Slack's bare `missing_scope` into a sentence naming the scope. `name_taken` returns the existing channel so create-if-absent needs no branch and a retry is idempotent; `already_in_channel` is success, because it is the goal state and Slack sends it even when only some of the named users were already members. **Two defects in already-shipped work surfaced.** Two M10 templates authored a `CONDITION` with `leftValue`/`rightValue` against a schema whose fields are `left`/`right` — Zod is not `.strict()` here, so the unknown keys parsed and the comparison ran on two undefineds, taking the same branch every time. The harness now rejects any authored key the node's schema does not read, which found eight more dead keys across the catalogue. And the root validator had no model of a `CODE` node's return spread, so an ordinary graph — CODE returning `{ valid, errors }`, a CONDITION on `{{valid}}` — was reported as referencing an unknown root; it now reads a literal return object and, when it cannot, declares the root set unknowable and stops checking rather than warning about roots it cannot enumerate. **Catalogue consequences**: 16 template nodes migrated, four templates reclassified `library` as genuinely two-service, and the onboarding sample lost its Slack tail — a first run that dies on its last node because Slack is not connected is a worse introduction than one that simply produces its output. Nine templates went from zero credentials to one, but were never zero-*setup*: the webhook node's secret was a URL in plain node config, so `credentialCount === 0` was measuring "declares no credential binding" rather than "needs no setup". Three new credential-free templates (a validated JSON endpoint, a payload reshaper, an idempotent webhook receiver) restore the one-third floor, kept rather than relaxed. 47 catalogue entries; 1569 unit/dom tests, `next build` green at 98 pages, tsc and lint clean. **Integration suite not run — the dev machine's disk is at 100% and the suite cannot complete.** | M10 |
| 2026-09-04 | **AF-M10-16 — the QuickBooks Online family, and the config panel learned multi-select.** Ten nodes (find/create customer, invoice, estimate, sales receipt, expense, get, invoice PDF, attach, webhook trigger) on one client — QBO is the largest single-service dependency in M10 at 10 of the 35. **Sandbox vs production is a property of the credential, and a node cannot override it.** The source templates make the company id node config, which is how they ship with a sandbox company baked into a workflow someone then runs against real books — or point at production while still carrying sandbox item ids. Here `realmId` and the environment are captured at connect time and sealed in the credential; moving a workflow between companies means picking a different credential, and nothing in the graph can disagree with it. The minor version is pinned at 75 because Intuit ships breaking response changes *inside* v3, so an unpinned field can vanish on a Tuesday with no deploy on our side. **Intuit's webhook model is one endpoint per app, not per workflow**, so the route works backwards from every other trigger: no per-workflow secret exists in the URL, the signature is the only proof, and routing happens after verification by matching the payload's realm to a stored credential and then to the published workflows whose trigger binds it — which is why that trigger takes a credential at all, and why a second connected company's invoices do not start the first company's workflow. The signature is HMAC-SHA256 over the **raw** body: reading it with `request.json()` and signing `JSON.stringify(parsed)` is the obvious implementation and produces a byte-for-byte different document, so every check would fail in a way that looks like a wrong verifier token — there is a test for that exact mistake. A deployment with no verifier token rejects everything, because an endpoint that cannot verify must never fail open. **Deviation from the task text, recorded:** rejections are logged, not written to `AuditLog` — that table is org-scoped, and an unverified payload has no proven realm, so every rejection would be filed against a guessed tenant in the one table whose value depends on its rows being true. `STARTPOSITION` is 1-indexed and QBO treats 0 as 1, so a loop starting at 0 re-reads the first page's tail forever — an infinite loop that looks like a working one; a short page is the only end-of-results signal QBO gives. Error 6240 ("Duplicate Name Exists") gets its own message naming the find-then-create pattern, because Intuit reports it as "Business Validation Error". Amounts are coerced from `"$1,299.00"` and **refused when blank** rather than posted as zero — `Number("")` is `0`, and a zero-amount line on a real invoice is never what was meant. **One platform gap closed on the way:** the schema-driven config panel had no multi-select, so `z.array(z.enum([...]))` threw `UnsupportedConfigFieldError` and the trigger's entity/operation filters were unauthorable — a `multiEnum` kind now renders a checkbox group, which every webhook connector that narrows events will need. Six templates cover all ten node types; the find-or-create one wires its two branches to their own terminal nodes rather than a `MERGE`, because only one branch runs and merge waits for an input that never arrives. 53 QBO tests; 42 catalogue entries; 1537 unit/dom and 243 integration tests green. | M10 |
| 2026-09-04 | **AF-M10-15 — the Google Workspace family. Phase B opens on the widest dependency in the milestone: 21 of the 35 automations need Google beyond one sheet append.** Eleven nodes across four services (`SHEETS_READ`/`UPDATE`/`UPSERT`/`TRIGGER`, `GMAIL_SEND`/`GMAIL_TRIGGER`, `DRIVE_TRIGGER`/`DOWNLOAD`/`UPLOAD`/`MOVE`, `CALENDAR_TRIGGER`), on four service modules over one shared client. **The shared client is where the leverage is**, and its error classification has a direction that matters: a 429, or a 403 whose *reason* is a quota, is transient and backs off; a 401, or a 403 whose reason is permissions, is permanent — retrying it three times spends the user's remaining quota to receive the same refusal and buries "the token lacks the Drive scope" under retry noise. Google overloads 403 across both, so the reason is read, not just the status. Pagination is bounded at 20 pages with truncation reported; `while (nextPageToken)` on a folder of 200,000 files is one node making an unbounded sequence of requests. **Three things the source automations forced.** *Drive has no "move"* — it is a parent swap, and the old parent must be named explicitly or the file sits in both folders, so the watched folder still contains it and the next poll reprocesses the same contract; `moveDriveFile` reads the file first (which also makes a retried step idempotent) and removes **every** current parent, not just the first. *A Google Doc has no bytes* — `alt=media` errors for Docs/Sheets/Slides, so they are exported to their Office equivalent and the caller is told which happened, because a filename and MIME type that disagree with the payload break the next node instead of this one; a Form or a Site has neither bytes nor an export and is named as such rather than handed over as an empty file. *`DRIVE_TRIGGER` advances to the newest `modifiedTime` it saw*, not to `now` — the local clock would skip a file written between the request and the response. **Gmail is where the security work is:** header values are CR/LF-stripped, because a newline in a templated subject is header injection (`"Hi\nBcc: everyone@…"` adds a recipient) and these values come from run data; headers are RFC 2047-encoded so an em dash or an accented name is not mojibake; attachment base64 is wrapped at 76 characters because unwrapped lines breach RFC 5322's 998-character limit and some relays mangle them. Drive query values are escaped — a folder name with an apostrophe would close the quoted string and have its remainder parsed as query syntax, which is SQL injection in a query language people forget is one. `GMAIL_TRIGGER` skips fetching bodies on its first poll (the framework dispatches nothing on a first sight, so those message-gets would be spent for results that are discarded), and `CALENDAR_TRIGGER` keys items as `${event.id}@${event.start}` so a rescheduled meeting briefs again, dropping `resource: true` attendees because Calendar counts a meeting room as an attendee. **Two catalogue corrections this task forced.** `countRequiredCredentials` counted credential *bindings*, so a Sheets template that reads a row and writes it back was scored as two connectors when the user connects Google once and picks it twice; it now counts distinct **types**. And the one-credential rule was written when every catalogue entry was an onboarding template — the M10 library ports automations that are multi-service in the source (#1 is Sheets + Gmail, #27 is Calendar + Gmail), so `TemplateSpec` gained a `tier`: `"starter"` keeps the onboarding promise unchanged, `"library"` is capped at four **and must need more than one**, so the label cannot be used to dodge the stricter rule. The credential-free floor is now measured over starter entries, which stops it getting easier to clear as M10 adds credential-bound templates. Four templates ship with it, covering all seven new node types. 45 tests in `src/features/google`; 36 catalogue entries; 1481 unit/dom and 243 integration tests green. | M10 |
| 2026-09-03 | **AF-M10-09 — `APPROVAL` node, send and wait. PHASE A COMPLETE (15 of 15).** #30 emails an approver and blocks on the answer. `src/features/approvals` had the model and the dashboard; this binds them to the graph — **one model, one list**. A node with its own table would have given operators two places to look and two definitions of "answered". The wait is `step.waitForEvent`, not a poll, matched on the **approval id** rather than the execution: a workflow can hold two approvals, and matching on the run would let the first answer resume both. Two outputs through the AF-M9-09 `resolveOutputs` contract; a timeout routes to `rejected` with the reason recorded, because a timeout is a decision and dying would leave whatever the approval gated in an unknown state. The trace row shows `WAITING` (AF-M10-08's status), so a run parked for a day does not read as hung. **Links are POST-confirmed, not GET-acting.** Mail scanners, link previewers and corporate security gateways all fetch links in email — an endpoint that resolved on GET would be approved by a scanner before the human read the message. The link opens a confirmation page; a button posts the decision. Tokens are HMAC-signed, **org-scoped** (a token names its tenant and the route checks it against the row's), **expiring** (the expiry is inside the signature, so it cannot be extended by editing the URL), and **single-use — enforced by the request's own status transition rather than a separate flag**, so answering makes every token for that request inert and the two cannot disagree. The final write is a conditional `updateMany` on `status: PENDING`, which is what makes two simultaneous clicks resolve once instead of resuming the run twice. Every refusal is audited, including the ones that look like noise: a replayed token and a cross-tenant token are what an attack looks like from the inside, and a security event nobody recorded is one nobody can investigate. The decision, identity and timestamp land in `AuditLog`. 16 tests (7 unit, 9 integration), plus a real gallery template (spend request → threshold → manager approval). | M10 |
| 2026-09-03 | **AF-M10-14 — `FORM_TRIGGER`, a first-party hosted intake form.** Four automations start from "user submits a form". `GOOGLE_FORM_TRIGGER` required owning a Google Form and wiring Apps Script; there was no first-party option. A published workflow now serves a real page at `/forms/<workflowId>` (plus an optional path segment) from fields authored in the node. **One 404 for every reason.** No such workflow, not published, no form trigger, a disabled one (AF-M9-17), or the wrong path segment all return the same answer from the same lookup, shared by the page and the submit route — so a prober cannot enumerate workflows, and the renderer cannot show a form the route would reject. The path segment is documented as obscurity, not authentication (it is in browser history and server logs), and compared as such rather than in constant time, which would imply a secrecy the design does not have. **Every limit is load-bearing because the endpoint is unauthenticated by design.** Plan-aware rate limit consumed only after the form is known to exist, so an unknown id cannot spend a real org's allowance; `Content-Length` rejected before the body is read; 10 MB per file (well under the platform's 100 MB, because the question here is what a stranger may push into our storage); 25 MB per submission; and a MIME **allowlist**, not a blocklist — accepting anything else means hosting arbitrary content for strangers under our domain. A test asserts `text/html` is refused and nothing is stored. Files become `FileRef`s through the AF-M10-06 store with no `executionId` — the run does not exist yet — so they carry an explicit TTL and the orphan sweep collects them if the run never happens. Validation reports every problem at once rather than one at a time, and values reach the run context with real types (`number` as a number, an unchecked box as `false`), so `{{#if form.fields.subscribe}}` means what it looks like. The payload is `form.{nodeId,title,submittedAt,fields,files}`, nested under one root for the same reason `webhook` is — a field named `title` must not shadow the form's own metadata — and `FORM_TRIGGER` was added to the AF-M9-07 trigger-root map so those expressions validate. 22 tests (10 unit, 12 integration). | M10 |
| 2026-09-03 | **AF-M10-13 — external vector store for `AI_RETRIEVE` (Pinecone).** `AI_RETRIEVE` was bound to the internal `KnowledgeChunk` table; automation #23 needs a caller-supplied Pinecone index at 768 dimensions, because the user already has the vectors and does not want to re-ingest them. One `VectorStore` interface, two implementations. **The internal store stays the default** — an absent `store` field means pgvector, so no saved node changes behaviour. It is read-only through the interface: writes go through the ingestion pipeline, which owns chunking, revisions and source status, and a half-written source is worse than an unsupported operation. **Namespaces are org-scoped and a node cannot name one raw.** A namespace is the only isolation Pinecone offers inside an index, and an index is a billed resource tenants share; the org prefix is applied by the store and the caller's suffix is stripped of anything but `[A-Za-z0-9_-]`, so `../org_b__secrets` cannot escape it. Tests assert two orgs get different namespaces for the same requested name. **The dimension is checked before the first write and the error names both numbers** — #23's prerequisite is a 768-dimension index, and "vector dimension 1536 does not match" without saying which is which is what costs people an afternoon. A test asserts no upsert is sent when the check fails. Two smaller decisions worth their comments: `minSimilarity` is applied client-side because Pinecone has no similarity floor, so the same setting means the same thing in both stores; and a provider error is raised rather than returning `[]`, because an empty result looks exactly like "no relevant context" and the model then answers confidently from nothing. REST rather than the SDK — three POSTs, and the client's own retry semantics would sit awkwardly beside AF-M9-06's. 16 tests. | M10 |
| 2026-09-03 | **AF-M10-12 — `HTML_TO_PDF`, rendered without a browser.** #28 needs an attorney-ready report. The obvious implementation is headless Chrome and it was rejected for two reasons. **Deployment:** Chromium is ~300 MB of binary that has to exist in the runtime image, which on Vercel it does not — a second deployment target for one node is a platform decision, not something a node should make. **Safety:** with a browser, "no network access, no JS execution" is request interception plus a disabled JS context, i.e. configuration, and a configuration mistake fails open with the document fetching whatever it likes from inside our network. The stack is `jsdom` → `html-to-pdfmake` → `pdfmake` (**~27 MB, pure JavaScript, no binaries**), where both properties are structural: jsdom is constructed without `runScripts` and without `resources`, and pdfmake's `setUrlAccessPolicy` denies every URL while `setLocalAccessPolicy` allows only the fourteen PDF standard font names. Two layers, deliberately. The DOM is sanitized first — scripts, iframes, `link`, `style`, `on*` attributes and any `<img>` that is not already a `data:` URI — so a stray remote image degrades to a missing image rather than aborting the render; the deny policies stay underneath so a miss in sanitization still cannot fetch. Tests assert an embedded `<script>` never sets a global and that `fetch` is never called for a remote image, stylesheet or font. **What it costs, stated in the node's own description:** CSS support is `html-to-pdfmake`'s — headings, paragraphs, lists, tables, inline styles. Floats, flexbox, grid and page-break control are not honoured. Adequate for a generated report; not adequate for rendering an arbitrary web page. Ships with a real gallery template (contract review → PDF) that chains `FILE_DOWNLOAD` → `EXTRACT_DOCUMENT_TEXT` → `AI_LLM` → `HTML_TO_PDF`. 9 tests. | M10 |
| 2026-09-03 | **AF-M10-07 — multimodal input for `AI_LLM` and `AI_EXTRACT`.** The registry has declared a `vision` capability on five models since M5 and nothing could reach it: no node let a user attach an image or a PDF to a prompt. Both AI nodes now take an `attachments` template resolving to one or more `FileRef`s, shared through `src/nodes/shared/ai-attachments.ts` because the hard parts are identical. **A model that cannot see is refused at SAVE time**, as an error, and every model in the chain is checked — not just the primary. A text-only fallback would silently drop the image and answer anyway on the day the primary is down, and that failure has no symptom until someone checks the numbers. It is re-checked at run time too, because a chain can be edited after the attachment was configured. **PDF handling is explicit and recorded.** Anthropic and Google take `application/pdf` natively and do their own page rendering, which preserves the layout that matters when reading an invoice; providers without document input get extracted text instead. Resolution happens *per candidate*, so a fallback is never handed a payload it cannot use, and the node's output carries a `handling` list saying which path each attachment took — a PDF that read badly has a visible cause rather than a guess. **The AF-M5-07 cache key now includes attachment content hashes, not file ids**: two copies of one invoice should share an entry and two different invoices must not, and ids differ on every upload while bytes do not. The AF-M5 canvas estimate charges for attachment bytes rather than estimating zero — telling a user that adding a 4 MB scan to every invoice costs nothing is the opposite of true. `AI_EXTRACT`'s `content` became optional when an attachment is present: for #21 (faxed PDF) and #8 (invoice image) the document *is* the input, and requiring text as well would mean inventing it. `splitModelId` and a new `findModelForCandidate` moved into the client-safe registry, because the save-time validator runs on the client and `fallback.ts` is server-only. 13 new tests. | M10 |
| 2026-09-03 | **AF-M10-08 / AF-M10-10 / AF-M10-11 — the graph primitives: `WAIT`, `FILTER`, `DEDUPE`, `EXTRACT_DOCUMENT_TEXT`.** **`FILTER` and `DEDUPE` are placement-sensitive**: inside a `SPLIT_OUT`/`AGGREGATE` segment they act on the current item, outside one they filter an array. Dropping an item needed an engine change — a new `_dropItem` marker the segment loop reads, so a dropped item is neither collected nor recorded as `failed`, and `AGGREGATE` reports it in a separate `dropped` list. Filtering everything out ends the run `SUCCESS`; it is an answer, not a failure. Three params were added to the node SDK to make this possible without guesswork: `workflowId`, `executionId`, and `item` (the fan-out value), each passed from both engine loops. **Comparison is typed** (`src/nodes/shared/compare.ts`), because `CONDITION`'s string compare makes "keep the ones where ok is true" keep everything — a boolean `false` renders as `"false"`, which is a non-empty string. The parse is strict, never coercing (AF-M9-08's rule), with two deliberate exceptions: ordering against an absent value is `false` rather than an error (a blank cell must not kill the run), and `contains` stays a string question whatever the type. **`DEDUPE` reuses `TriggerState`** rather than adding a table — same question, different point in the graph — keyed `(workflowId, nodeId)` so two dedupe nodes in one workflow stay independent. "Forever" has a stated 10,000-key ceiling because `lastSeenIds` is rewritten on every run. An empty key fails the node rather than collapsing the batch, and a changed key expression clears the window and reports `windowReset`. **`WAIT`** uses Inngest's durable sleep, split into hourly chunks with a cancellation check between them — one six-day `sleepUntil` is durable but opaque, so "cancel" on a parked run would mean "cancel in six days". The 30-day maximum is enforced at **save** time for `duration` mode (a workflow that fails three days into a six-day wait has already burned three days); `until` carries the same ceiling at run time. A time already past resolves immediately and says so. New `NodeExecutionStatus.WAITING` (migration `20260903160000_node_execution_waiting`) so a run parked for six days does not read as hung. A test caught `resolvedImmediately` being computed after the sleep, which made it unconditionally true. **`EXTRACT_DOCUMENT_TEXT`** wraps the knowledge base's extractor — one implementation, not a copy — and adds what a graph needs: `FileRef` resolution, org-scoped reads, and a refusal for types the extractor would silently decode as UTF-8 (a PNG would have "succeeded" with a page of binary garbage). Truncation is reported, never silently applied. Fixed a silent failure in the shared extractor while wiring it: a PDF that failed to parse fell back to `buffer.toString("utf-8")`, turning a corrupt or password-protected file into mojibake that then got chunked, embedded and answered from — it now throws a typed `DocumentExtractionError`, and `pageCount` is surfaced. Two real gallery templates demonstrate them (once-only sync; summarise-and-hold-until-morning), because the catalogue harness requires every palette node to be exercised by a template. 1336+ unit/dom tests and 222 integration tests pass. | M10 |
| 2026-09-03 | **AF-M10-05 — the polling-trigger framework and `TriggerState` (ADR-0024). The milestone's largest single unlock.** 18 of the 35 automations begin with "when a new X appears", and nothing in the platform could express it: `evaluate-schedules` dispatched whole workflows on a cron and kept no state, so "new since last run" had nowhere to live. `TriggerState` is one row per `(workflowId, nodeId)` — cursor, a 500-id dedupe window, last-polled, failure count with backoff, and a key fingerprint — and **a poller never writes it**. A polling trigger declares `polling.poll({ config, credentials, cursor, isFirstPoll, limit })` on its registration and the framework owns dispatch, dedupe, cursor persistence and backoff, so an off-by-one in "already handled" is one bug rather than eighteen. Three failure modes drove the design and each is a test: **no history replay** (activating a workflow against a 500-row sheet dispatches zero runs, not 500), **at-least-once with dedupe** (providers answer "changed since T" inclusively, so two overlapping polls yield exactly one run per item), and **backoff** (a failing poll changes nothing but `nextPollAt`, so an outage costs a delay and never a gap). State is persisted *before* runs are dispatched: a crash between the two costs one cycle, the other order replays every run. Budgets: 50 items per poll, 200 pollers per sweep, oldest-first. The sweep extends `evaluate-schedules` rather than adding a second cron — same workflow read, same tick — as its own `step.run` so a provider outage cannot make schedule evaluation look failed. Pollers are discovered through the registry, so a connector is a node folder and nothing else. Migration `20260903140000_add_trigger_state`; 32 tests. | M10 |
| 2026-09-03 | **AF-M10-06 — binary payloads by reference (`FileRef` + blob store, ADR-0025).** 12 automations move a PDF, image or video between nodes. They cannot travel in `WorkflowContext`: it is JSON-serialized into `NodeExecution.output` under the ADR-0018 cap, so a 3 MB invoice fails the run at the output boundary — and base64 makes it worse, inflating 33% and re-copying into every downstream node's context. What travels is `{ $file: { id, filename, mimeType, size, sha256 } }` — a few hundred bytes, asserted by a test that stores 5 MB and checks the serialized node output stays under 1 KB. Bytes go to a `BlobStore` with two implementations (local filesystem, any S3-compatible store) and **no SDK type above the interface**, which is what makes the local store a real implementation the tests exercise rather than a mock. Backend selection is **by configuration present, not by `NODE_ENV`**: a production deploy that forgot the bucket would otherwise write to a container filesystem that vanishes on the next deploy. A bucket without credentials refuses to boot rather than falling back. Tenancy is enforced on read, not assumed: `readFile` takes an org and refuses a mismatch, because a `FileRef` is a plain object a `CODE` node could mint — and "not found" and "someone else's" return the *same* message so the endpoint is not an existence oracle. Keys carry the org prefix structurally and are rejected if they could escape the store root. Quota is summed from rows, checked before the write; an empty file is refused outright (a zero-byte result is a failed fetch reported as success). **Lifetime:** `StoredFile.executionId` is `ON DELETE SET NULL`, not `CASCADE` — deliberately, and it is the subtle part. `CASCADE` looks tidier and silently leaks storage forever, because the row is the only record of the object's key. The AF-M8-06 retention sweep now deletes a run's blobs *before* the run, and a second bounded stage collects files that never had a run (a form upload has nothing to inherit a lifetime from). Ships `FILE_DOWNLOAD` (URL to `FileRef`, through the full egress guard, size-capped while reading) and a real gallery template that exercises it. Migration `20260903150000_add_stored_file`; 16 unit + 16 integration tests against Postgres and a real local store. | M10 |
| 2026-09-03 | **AF-M10-01 — credentials on `HTTP_REQUEST` (ADR-0022). The M10 keystone.** 31 of the automation library's 35 workflows are ultimately an authenticated REST call; before this the only way to authenticate the generic node was to type the secret into its `headers` map, which lands verbatim in `NodeExecution.input` and breaks the AF-M3-04 rule that plaintext never reaches a trace. The node now declares `credentials: [{ key: "credentialId", type: "*", required: false }]` — a wildcard matched by the new `acceptedCredentialTypes` grammar (`"*"` = any registered type, `"a|b"` = either) — plus an `authMode` of `none | bearer | header | basic | queryParam | oauth2`. `src/nodes/shared/http-auth.ts` translates a decrypted secret into request material, reading **only** from `NodeRunParams.credentials`, never from `data`. Two hardening steps came with it: (a) `createSafeFetch` now also drops *this request's own* auth header on a cross-origin redirect — `Authorization` was already covered by ADR-0015, but the node can authenticate with an arbitrary header name and an `X-API-Key` following a `302` to an attacker's host is the same leak; (b) `src/nodes/shared/redact.ts` scrubs every secret the request sent from the value the executor returns, because an endpoint that echoes the request back (debug services, API gateways, this milestone's own fixture servers) puts the credential in the response body, which the engine then persists. The generic config panel also grew a **working credential picker** (`credential-field.tsx`): it previously rendered a disabled input telling the user to pick a credential somewhere that did not exist, which every node added in M10 would have inherited. 24 tests; `docs/nodes/http-request.md` written. | M10 |
| 2026-09-03 | **AF-M10-02 — 12 credential types for the library's services, and the write schema stopped being hand-copied.** New: `apify.apiKey`, `apollo.apiKey`, `mailerlite.apiKey`, `pinecone.apiKey` (+`environment`, `indexHost`), `openrouter.apiKey`, `creatomate.apiKey`, `telegram.botToken`, `waha.apiKey` (+`baseUrl`), `uploadPost.apiKey`, `googleCustomSearch.apiKey` (+`cx`), `shopify.accessToken` (+`shopDomain`), `jira.apiToken` (+`email`, `siteUrl`). Eleven carry connection testers; `googleCustomSearch.apiKey` is explicitly `NOT_TESTABLE` because every Custom Search call is metered against a 100-query free daily quota. That distinction is now enforced: `CredentialTypeDef` gained `notTestableReason`, and a test fails any type that has neither a tester nor a stated reason — "nobody wired one" and "this provider has no cheap authenticated GET" are indistinguishable in a registry otherwise. The three testers that hit a **user-supplied** host (WAHA, Shopify, Jira) go through the egress guard rather than raw `fetch`; a "Base URL" field reaching a server-side fetch is an SSRF probe with a button on it. `server/write-schema.ts` now **generates** its write and update variants from the registry definitions instead of listing ~50 hand-written blocks — M10 adds 24 types across four tasks and the parity test existed precisely because the two could drift. `.strict()` is preserved per variant. Tests now derive their sample payloads from the defs, so a new type cannot be silently skipped; every type round-trips seal → open and is asserted never to leak a raw secret field into its preview. | M10 |
| 2026-09-03 | **AF-M10-03 — one Google credential per service (ADR-0023), and OAuth credentials became usable at all.** `google.oauth2` had `defaultScopes: userinfo.email` — a single connection standing for every Google API a workflow might call. Widening it was the obvious fix and the wrong one: every workflow in the org resolves the same row, so adding `gmail.readonly` for automation #26 would let a spreadsheet-appending workflow read the connecting user's mail. Five scoped types now exist (`google.sheets`, `google.gmail`, `google.drive`, `google.calendar`, `google.docs`), each with its own scope set, and `oauth.test.ts` asserts per type that no scoped credential drags in another service's scopes. `google.oauth2` is **deprecated, not removed** — `CredentialTypeDef` gained a `deprecated` marker mirroring `NodeDefinition`'s, the picker filters on it, and `GOOGLE_SHEETS_APPEND` declares `"google.sheets|google.oauth2"` so nodes saved before the split keep resolving (ADR-0011's retirement rule applied to credentials). **The blocking defect found while doing it:** the OAuth callback created credentials with a `userId` and *no* `organizationId`. Every read path is org-scoped — `credentials.list` filters on it and the engine resolves by `{ id, organizationId }` — so connecting Google appeared to succeed and then nothing could see or use the credential. The active org is now captured at connect time and carried in the signed state; the state is also HMAC-compared in constant time and expires after 15 minutes. Deviation from the task text, recorded: `google.drive` takes the full `drive` scope, not `drive.file drive.readonly`. `drive.file` only ever sees files this app created, which makes "watch the folder contracts land in" and "move a processed file to /approved" (#28/#29/#30) impossible — the narrower scope would be non-functional, not safer. | M10 |
| 2026-09-03 | **AF-M10-04 — seven OAuth providers: Intuit, GitHub, Atlassian, Notion, Shopify, LinkedIn, X.** `OAuthProvider` became a data shape rather than a set of `if (provider === ...)` branches in the routes: authorize/token URLs, env-backed client getters, scopes, authorize params, PKCE, token-endpoint auth style and body format, connect-time parameters, and two capture hooks. **Provider extras**, the piece the old callback had nowhere to put: Intuit's `realmId` (the company id, which arrives on the redirect and which every QBO path needs), Shopify's shop domain, Notion's workspace ids, X's granted scopes — all now land in the credential secret. Atlassian additionally needs its `cloudId`, which is in no response at all, so a `postExchange` hook fetches it during connect and **fails the connection** rather than storing a credential every node would later reject. Per-provider quirks handled where they belong: Intuit rotates its refresh token on every exchange and the old one dies immediately, so the refresh job writes the new one in the same statement; X requires PKCE and Basic token auth; Notion's token endpoint is JSON-only; Shopify's endpoints live on the merchant's own host, so the shop is a connect-time parameter validated against `*.myshopify.com` — an unvalidated shop parameter would point the authorize step, and the client id, at any origin. Shopify/GitHub/Notion tokens are marked non-expiring so the refresh cron does not read a null expiry as "already stale". `exchangeToken`/`buildOAuthSecret` are now shared by the callback and the refresh job, which previously hand-rolled the same POST with different bugs. 14 env vars documented in `.env.example` and `src/lib/env.ts`. 20 tests. | M10 |
| 2026-09-03 | **AF-M10-35 — nodes can show a brand logo.** `NodeDefinition` gained `logo?: string`, mirroring `CredentialTypeDef.logo`; the palette, canvas node and config panel render it with the lucide `icon` as fallback, so a node with no mark is unchanged. The `BRAND_LOGOS` map that matched type ids inside the UI layer is gone — a node now declares its own artwork next to the rest of its metadata. **18 missing marks added** as SVGs (Telegram, LinkedIn, X, Pinecone, Apify, Apollo, MailerLite, Creatomate, Pollinations, YouTube, Instagram, OpenRouter, WAHA, Google Drive/Calendar/Maps, Veo, Upload-Post), each drawn to read on both light and dark canvas; existing PNG marks were copied to URL-safe kebab-case names because a path with a space works only because the browser percent-encodes it. A test walks both registries and fails CI on any `logo` that does not resolve to a file under `public/` — a broken mark renders an empty box nobody notices in review and everybody notices in a demo. | M10 |
| 2026-09-03 | **Built the five missing M6 UI surfaces the audit found.** All operator-facing screens whose server layer shipped in M6 but whose client was never written now exist. New `src/features/organizations/components/`: `members-list` (role badges, role-change menu, remove-with-confirm; own-row and owner rows locked so a workspace can't be accidentally locked out), `invite-member-dialog` (role picker with inline descriptions; success state shows a copyable accept link and whether email was sent), `invitations-list` (pending invites with cancel), `audit-log-list` (paginated, filterable by action/resource, rows expand to a `before`/`after` JSON diff), `workspace-switcher` (dropdown of memberships, active one checked, switches by writing the `autoflow_active_org` cookie the server already read + `router.refresh()`), `create-workspace-dialog` (derives a unique slug, switches in on create). Plus `src/features/auth/components/profile-settings` (edit name via `authClient.updateUser`; **active-session management** — list, per-device label, per-session revoke, "sign out other devices" — the security-relevant "sign out a lost laptop" surface that was missing). Routes: `/settings/{profile,members,audit-logs}` under a shared tab-nav layout with a `/settings` → profile redirect, and top-level `/accept-invite`. The stale static `WorkspaceCard` (its own comment read "There is no switcher yet") was replaced by the real switcher and a Settings nav entry added. **Server work the UI required:** a `getActive` procedure (resolved org + caller role, for switcher highlighting and admin-only tab gating); a **best-effort** `sendInvitationEmail` (an invitation must not fail when Resend is unconfigured, unlike the auth emails — the dialog shows a copyable link either way); `inviteMember` now returns `{ acceptUrl, emailed }`; and the login form honours a same-origin-validated `?redirect=` so the accept-invite round trip resumes after sign-in. **The browser verification earned its keep:** it caught a real infinite-render loop in the accept-invite effect (no-token branch set state before the `attempted` ref guard while the effect depended on the unstable mutation object) that tsc, lint, and build all passed — fixed and re-verified loop-free on a fresh mount, with the login redirect round trip confirmed (`/accept-invite?token=x` → `/login?redirect=%2Faccept-invite%3Ftoken%3Dx`). Authed pages were verified by build + types + shared patterns rather than a live login (entering a password to authenticate is barred). **Coverage:** `tests/integration/organizations-members.integration.test.ts` (5: invite→accept→member→promote→remove, `getActive`, copyable-link return, audit entry) + 3 `email.test.ts` cases for the best-effort sender. **Gates:** unit **1421/1421** (125 files), integration **198/198** (22 files), tsc clean, lint clean (zero warnings), build green (all 5 routes). Only AF-M6-10's approval *node* remains open in M6, and that is `AF-M10-09` scope. | M6 |
| 2026-09-03 | **M0–M9 audit: five M6 tasks were marked done for UI that does not exist, and the project's top-priority defect class had no lint enforcement.** Every task from M0 to M9 was re-checked against the filesystem rather than the record. **M0–M5, M-KB, M7, M8 and M9 hold up** — spot-checked claims (M7's onboarding/command-palette/notifications/templates/analytics components, M8-01's honest "Management UI deferred", M6's models/RBAC/isolation-tests/SSO) are all real on disk. **M6 does not.** Its server half is genuine and load-bearing, but `src/app` contains **no** `settings`, `accept-invite`, `approvals`, `api-keys`, `audit` or `profile` route; `src/features/organizations/` and `src/features/api-keys/` have **no `components/` directory at all**; and `src/features/approvals/` is a hook plus a router, where the hook is imported by nothing. Five entries were corrected: **AF-M6-04** (router real, no member-management UI — nobody can invite, promote or remove anyone), **AF-M6-05** (`logAuditEvent` real, no viewer — the audit trail is written and unreadable without database access), **AF-M6-07** (no `OrganizationSwitcher`; `app-sidebar.tsx`'s **own comment already said "There is no switcher yet"**, contradicting the task entry in writing), **AF-M6-08** (no `/settings/profile`, so session revocation has no surface), **AF-M6-09** (no `/accept-invite` — combined with M6-04, the invitation flow is unreachable end to end). **AF-M6-10** was corrected separately: it claimed a `core.approval` node with `approved`/`rejected` ports and a dashboard; none of the three exists, and **AF-M8-13 had already documented this** ("no approval node ships, so the AF-M6 approvals dashboard reads a table with no writer"), so the two entries had contradicted each other since 2026-08-31. The remaining scope is already planned as `AF-M10-09`. M6's section header is now 🟡. This is the second occurrence of the provenance failure the 2026-08-22 reconciliation was written about; the rule it set — check only after verifying on disk — was not applied to M6. **Separately, a real enforcement gap:** `engineering_rules.md` declares **[HARD]** that "an empty catch block fails `npm run lint` (`no-empty` with `allowEmptyCatch: false`)" — an **ESLint** rule name that does not exist in Biome and was never configured after the migration, so the self-declared "top-priority defect class" had no enforcement for the life of the repo. Enabling Biome's `suspicious/noEmptyBlockStatements` found three empty blocks in production code: a `catch {}` in `upload-source-dialog.tsx` that **silently swallowed knowledge-base upload failures** (the mutation hooks toast their own errors, but file-read and base64-encode failures inside the same `try` had no reporter — the dialog sat open with no explanation), and two in `postgres/query/execute.ts`, where fixing them also surfaced that `client.end()` ran in both a rethrowing `catch` and the `finally`, closing the client twice on every error path. The rule is now enabled (off for test files, where `() => {}` is an idiomatic no-op mock), the doc names the real rule, and the last standing lint warning (`sandbox.ts` optional chain) is closed — **`npm run lint` is now clean at zero errors and zero warnings for the first time.** **Gates:** `npm test` 1413/1413 across 124 files, `npm run test:integration` 193/193 across 21 files, `tsc` clean, lint clean, build green. | M0–M9 |
| 2026-09-03 | **AF-M9-16 — M9 COMPLETE. The three reference workflows execute green in CI, and the acceptance suite immediately found four real defects.** `tests/integration/reference-workflows.integration.test.ts` (6 tests) runs W1/W2/W3 **from the seeded `Template` rows** — projected through `toSeedRow`, the same function `seed:templates` uses, upserted, then read back out of Postgres — against a loopback fixture server (`tests/integration/fixtures/http-fixture-server.ts`, `node:http` only) started by the integration `globalSetup`. The only edit made to a seeded graph is rewriting the two public hostnames onto the fixture, and the suite asserts that rewrite applied and that no external host survives it, so a template gaining a third external host fails loudly instead of quietly reintroducing a network dependency. Plus `tests/integration/template-round-trip.integration.test.ts` (4 tests) closing AF-M9-15's last box as a test rather than a browser check: `prepareTemplateGraph` → `saveWorkflowInputSchema` → `workflows.saveGraph` → the `Node`/`Connection` rows, asserting every persisted edge names a port its source node actually declares (the G1 proof) and that the SWITCH keeps two *distinct* ports. **Four defects found, none of which any existing gate caught.** (1) **The engine could not rejoin after a branch.** `_outputPort` is a control signal consumed at the node that produced it, but `buildNodeInput` copied it into the next node's input; since every executor returns `{ ...input, … }`, that node re-emitted the branch's port id as its own, `markTakenEdges` matched none of its outgoing edges, and **everything below the join was marked "not reachable via taken branches" while the run still reported SUCCESS**. Route → per-branch work → rejoin, the most common branching shape there is, silently dropped its tail. `trace.test.ts` could not have caught it — those unit tests call `markTakenEdges` with hand-built port ids, so the poisoned value never appears; the same blind spot the milestone recorded for G1. Fixed at the root (strip the key when merging upstream outputs) plus a `declaredOutputPort` guard: a node may only route to a port it declares, and anything else is treated as non-branching rather than as a silent kill switch. Three regression tests in `run-graph.test.ts`. (2) **`_run` was stripped by the save boundary** — see the AF-M9-06 correction; the per-node run policy never persisted, so setting retries in the UI did nothing and all three templates lost theirs on install. (3) **`{{{json x}}}` emitted invalid JSON for a missing path**: `JSON.stringify(undefined)` returns the JS value `undefined`, which Handlebars renders as the bare word `undefined`, so any template building a JSON payload from an optional path produced a malformed document. Now emits `null`. An AF-M9-07 helper defect affecting every template. (4) **The integration `globalSetup` had never run** — it reads `TEST_DATABASE_URL`, but `.env` was only loaded per worker, so the variable was `undefined`, the guard returned early, and **migrations were silently never applied on a local run** despite the file's comment saying they were; CI was unaffected (job-level env var plus its own `migrate deploy` step), which is exactly why it went unnoticed. Also fixed: W1's `payload` mapping crashed on any request without a payload (every `ping`), and `WEBHOOK_TRIGGER` still described itself as *"(Stub — full implementation in M4.)"* in the palette and node reference long after M4 shipped the receiver. CI wires `ALLOW_LOOPBACK_EGRESS=1` at the job level with the reasoning that it cannot widen anything (loopback only, and `allowLoopbackEgress()` throws at boot under `NODE_ENV=production`). **Gates:** `npm test` **1413/1413** across 124 files, `npm run test:integration` **193/193** across 21 files, `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green, `seed:templates` dry run clean at 24 templates. | M9 |
| 2026-09-03 | **M10 planned — the 35-automation library.** `docs/automations/autoflow-automations.md` (Intuz's 35 published n8n templates) audited against the code and written up as milestone **M10** in `tasks.md`: 35 tasks (`AF-M10-01`…`AF-M10-35`), ~62 engineer-days, with a per-automation coverage matrix, a 15-row gap register verified against files on disk, a descope order, and an explicit "what one day actually buys" section. **Headline finding:** the 35 automations need **34 distinct third-party services**, and `HTTP_REQUEST` has **no `credentials` array** — no `Authorization` handling in its executor, no credential requirement in its definition — so 31 of the 35 are currently unreachable except by pasting a secret into a plaintext `headers` map, which would land in `NodeExecution.input` against the AF-M3-04 rule. Putting credentials on the generic HTTP node (`AF-M10-01`) is therefore the keystone: it converts most of the connector work from "write a node family" into "write a template". Other blocking gaps: no polling triggers and no cursor state model (18 of 35), one Google credential scoped to `userinfo.email` only (21 of 35), no binary payloads (12 of 35), no QuickBooks (10 of 35), and `NodeDefinition` has no `logo` field — so `public/logos/` cannot reach the canvas at all, and 18 of the marks the library needs are missing. **No code shipped for M10**; this row records the plan only. | M10 |
| 2026-09-03 | **AF-M9-10 — `RESPOND_TO_WEBHOOK` + real synchronous webhook responses (gap G3), and AF-M9-15's three templates.** Before this, `?sync=true` returned a fixed `{success, executionId, error}` envelope and the workflow's own output never reached the caller — no status, content-type or header control, POST-only. **New node** `src/nodes/core/respond-to-webhook/` (`ACTION`, status 200–599, `contentType`, templated `body`, templated `headers`), registered in `registry.ts` + `manifest.ts`. **New column** `Execution.response Json?` (additive migration `20260903120000_add_execution_response`); NULL keeps meaning "never responded", which is the signal the route uses to preserve the legacy envelope. **The response is written eagerly, at the node, not at settle** — `onFailure` is a separate Inngest context with no access to the handler closure, so a settle-time write would lose the response whenever a node *downstream* of the respond node failed, which is exactly the "respond early, then do slow work" shape the node enables; eager writing also survives AF-M8-27 cancellation. Harvested per node via one `persistWebhookResponse` helper called from both the main loop and the fan-out segment loop, because AF-M9-12 gives each node its own input and a respond node on a non-final branch would otherwise vanish. **Header policy** (`src/lib/webhook-response.ts`) is an **allowlist** — known-safe response headers or `x-` prefixed — refusing `Set-Cookie` (session fixation), hop-by-hop headers, `Content-Length` and `Content-Type`; applied in the node **after template resolution** (a template resolving to `Set-Cookie`, or smuggling CRLF through a *value*, is the case a literal-config check misses) and **again in the route** on the value read back from the DB. Body capped at 1 MB and **rejected, never truncated**. **Route** now exports `POST`/`GET`/`PUT`/`PATCH`/`DELETE` over one handler, with `webhook.method` reaching the graph and `webhook.body` null (not `""`) for bodyless methods. The 500 ms poll is **kept deliberately** — an Inngest realtime subscription or Postgres `LISTEN` binds a long-lived connection to a serverless handler Vercel may freeze between event-loop turns, trading correctness for latency this endpoint does not need; recorded at the call site and in the task. **`validate()`** gained `checkRespondNodes`: warns when a respond node has no enabled webhook trigger, and when one respond node is an *ancestor* of another (ancestry, not a count — sibling branches are W1's whole shape and must not be flagged). **AF-M9-15's three templates** authored and gated: `api-router-sync-response` + `multi-channel-broadcast-merge` (ops) and `api-etl-batch-deliver` (data), all at **0 credentials**, W3 in the full `SPLIT_OUT`/`AGGREGATE` form rather than §1's batched fallback. **Three pre-existing defects found and fixed while making the gates honest:** (1) `CODE`, `SPLIT_OUT` and `AGGREGATE` were **missing from `updateNodeSchemas`**, so none could be saved from the editor — the exact drift that file's own comment documents, recurring after AF-M9-13/14; (2) `CODE.defaults = { code: "" }` and `SPLIT_OUT.defaults = { path: "" }` both failed their own schemas, leaving `registry.test.ts` red on the branch — fixed with real starter values (`SPLIT_OUT` now defaults to `path: "items"`, the key `CODE` stores an array under, so the documented pairing works on drop); (3) two `tsc` errors in untouched test files. **Verification:** 15 header-policy + 10 executor + 9 validator unit tests, 11 integration tests (`tests/integration/respond-to-webhook.integration.test.ts`); `npm test` **1394/1394** across 122 files, `npm run test:integration` **180/180** across 19 files, `npx tsc --noEmit` **clean** (both prior baselines fixed), `npm run lint` clean, `npm run build` green, `seed:templates` dry run clean at 24 templates. | M9 |
| 2026-09-03 | **AF-M9-17 — A disabled trigger no longer dispatches a run (found during AF-M9-04).** AF-M9-04 made the engine honour `Node.disabled`, but by then the `Execution` row already existed and the run had been billed against the org's quota — the engine is the wrong place to stop a trigger, so a disabled webhook trigger still accepted the POST, still created an execution, and still counted against the plan limit. **This task moves the disabled check upstream to all three dispatch paths, so a disabled trigger is never dispatched at all.** **Webhook route** (`src/app/api/webhooks/[workflowId]/[path]/route.ts`): new `hasDisabledWebhookTrigger(graphSnapshot)` helper — the route now reads `activeVersion.graphSnapshot` (added to the select) and returns the same generic `404` it returns for an unknown workflow, **before** the rate-limiter consume and **before** secret verification, so a prober cannot distinguish a disabled workflow from a nonexistent one and no quota is consumed. **Cron evaluator** (`src/inngest/cron.ts`): extracted exported `collectScheduledDispatches(activeWorkflows, now): ScheduleDispatch[]` — it skips a workflow whose `SCHEDULE_TRIGGER` node is disabled silently (no per-tick error) and `evaluateSchedules` dispatches from that list. **`workflows.run`** (`src/features/workflows/server/routers.ts`): refuses with `BAD_REQUEST` and a message naming the disabled trigger when the draft's only trigger is disabled — checked before the `Execution` row is created, so the Run button cannot start a run that does nothing. **Canvas lint** (`src/engine/validate.ts` `checkTriggers`): new **warning** — `Workflow's only trigger "${name}" is disabled...` with the node id — fired when the workflow's single trigger is disabled and no other trigger exists, so the cannot-fire state is visible at save time, not discovered by silence (2 new unit tests: warns when the only trigger is disabled; no warning when only a non-trigger node is disabled). **Integration tests** (`tests/integration/disabled-trigger.integration.test.ts`, real Postgres, real tRPC caller + mocked `sendWorkflowExecution` + real webhook POST via `await import`): 6 tests — 2 per dispatch path, each disabled case asserting **no `Execution` row** + no enqueue, with an enabled-trigger control per path proving the disabled check cannot silently swallow legitimate runs. **Verification:** all 6 integration tests green, 55 validator unit tests green, `tsc` clean apart from the 2 pre-existing baselines, biome clean. | M9 |
| 2026-09-03 | **AF-M9-14 — Bounded item fan-out: `SPLIT_OUT` → segment iteration → `AGGREGATE` (gap G6, ADR-0021).** The bounded expiry of Decision D's loop deferral: a single, non-nested iteration between an explicit start and an explicit end. **Two new node types.** `SPLIT_OUT` (`TRANSFORM`, `src/nodes/core/split-out/`) reads an array at a configured `path` and opens an iteration segment; `AGGREGATE` (`TRANSFORM`, `src/nodes/core/aggregate/`) closes it and returns `{ items, count, failed }` (per-item `items`, `count`, and `failed` = list of item indices that failed). Both registered in `src/nodes/registry.ts` + `manifest.ts` with bare `NodeDefinition` objects (concrete config lives only in each `execute.ts`, per convention). **Save-time shape enforcement.** `validate()` gained `checkSegments` (`src/engine/validate.ts`), all `error` severity: pairing is 1:1 via mutual reachability (`split→agg` forward-reach × `agg→split` inversion), no `SPLIT_OUT`/`AGGREGATE` may nest inside another pair, and no edge may cross the segment envelope `{interior ∪ {S,A}}` — 5 new unit tests (valid segment, unpaired SPLIT_OUT, SPLIT_OUT reaching 2 AGGREGATEs, nested, crossing). New `src/engine/segments.ts` `planSegments(nodes, edges)` derives `{ segments, consumedNodeIds, segmentStartIds }` from the saved `connections`. **Per-item execution.** A new `runSegment` path in `executeWorkflowHandler` (`src/inngest/functions.ts`) drives: `SPLIT_OUT` once (itemIndex null), each interior node once per item with `itemScope { $item, $itemIndex }` (added to `buildTemplateContext`/`makeResolver` in `src/features/executions/template.ts`, plus `ALWAYS_PRESENT_ROOTS` in the validator so a valid in-segment expression validates), then `AGGREGATE` once with `overrideOutput: aggregateResult` (itemIndex null) — all sequential, honouring per-node retry/timeout and `continueOnFail` inside the segment. A failing interior item under `continueOnFail` writes a FAILED row for that `{nodeId, itemIndex}` and lands its index in `aggregateResult.failed`; all segment participants are recorded in `handledBySegment` so the main loop never re-runs them. **Hard cap (ADR-0021).** Default 100 items, engine ceiling 1000; exceeding the cap throws a `NonRetriableError` naming the cap — the run fails cleanly, never a truncated half-fan reported as success. **Trace shape.** `NodeExecution` gains a nullable `itemIndex` (`Int?` + `@@index([executionId, nodeId, itemIndex])`, migration `20260903000000_node_execution_item_index`; `prisma generate` run), so one row exists per node per item and a 10-item × 2-node segment is 20 keyed rows, not 20 unrelated ones (UI grouping is the AF-M9-16 surface; the data model + per-item tracing under ADR-0021's `{executionId, nodeId, itemIndex}` key is what ships here). **Retention/quota decision recorded in ADR-0021** (100-item segment writes 100× the node rows; counted as one execution for quota with the row-multiplication foot-gun stated). Unsupported (recorded in ADR/validated): nesting, parallel items, `splitInBatches` resumption, `Wait` inside a segment. **Verification: 3 engine integration tests** in `tests/integration/engine/run-graph.test.ts` (10 items → 10 interior rows with itemIndex 0..9 + AGGREGATE `{count:10, failed:[]}`; a failing item under `continueOnFail` → FAILED row + `{count:3, failed:[1], items.length:2}`; 101 items vs cap 100 → `runGraph` rejects with a `NonRetriableError` naming the cap) **+ 5 validator unit tests**; full integration suite 163 green, 53 unit green, `npx tsc --noEmit` clean apart from the 2 pre-existing baselines, `npm run build` + lint (biome) clean. | M9 |
| 2026-09-03 | **AF-M9-13 follow-up — `CODE` sandbox escape closed (cross-realm containment).** Review of the committed CODE node found a **critical vm escape**: `sandbox.ts` injected a host-realm `input` object (deserialized from `workerData`) into the vm context, and the canonical `node:vm` attack — `input.constructor.constructor("return process")()` — resolves through the host object's `constructor` chain back to the worker thread's `Function`, returning the **worker's real `process`**. Verified end-to-end before fixing: `process.env` returned `OPENROUTER_API_KEY`; `process.getBuiltinModule("fs").readFileSync(...)` read `package.json`; `process.getBuiltinModule("child_process").execSync("whoami")` ran a command. (`process.mainModule` is `undefined` on this eval-worker path, so `getBuiltinModule`, Node ≥ 18.3, is the reachable primitive; `process`/`require` are not sandbox globals — which is why the pre-existing "no host bindings" test passed vacuously. Defect found in review, not shipped-to-user.) **Fix.** `sandbox.ts` now passes the input as a JSON string (`workerData.inputJson`) and materializes it **inside the vm realm** with `vm.runInContext("(" + inputJson + ")", context)` over an empty `vm.createContext({})` — so user code only ever reaches vm-realm objects whose intrinsics cannot name the host. `runUserCode(code, input, caps)` now `JSON.stringify`s the input first and fails **loudly** (`CodeExecutionError`) if it cannot serialize — no silent drop of `undefined`/functions/BigInt/cyclic. Line-number rebasing unchanged (wrapper still occupies line 1, body starts line 2). **Tests.** Strengthened `execute.test.ts`: a new adversarial regression case pumps `constructor.constructor("return process")()` through the input, nested input, object/array literals, and `JSON.parse` and asserts **every vector is contained** (the suite previously held only the vacuous global-typeof check). **Verification:** CODE suite 9/9 green, `npm run build` green, `biome check .` clean (only the 1 pre-existing non-blocking warning), `tsc --noEmit` only the 2 pre-existing baselines (neither in code). ADR-0020 §2a and `docs/architecture/security.md` §6 updated: the "no host bindings" claim is now true-and-documented, with the cross-realm mechanism, the escape, and the fix recorded rather than implied. | M9 |
| 2026-09-03 | **AF-M9-11 — `MERGE` v2: real multi-input ports (gap G4), the first consumer of ADR-0019's keyed-port context.** `MERGE` declared one `main` input port and rebuilt its result from the flat rolling bag, so `Combine`/`[Merge]` could never express what picked the merge key per branch. **Fix 1 — config-derived input ports.** `NodeDefinition` gains `resolveInputs(config): PortDef[]` (sibling of AF-M9-09's `resolveOutputs`; `ports.ts` is still the one shared resolution path — `inputPorts`/`defaultInputId`/`resolveEdgePorts` now take a config-aware `NodeDescriptor`, and `resolveInputs` is consulted whenever present). `MERGE` bumps to `version: 2`, declares `inputCount` 2–5 and emits `input-0…input-{n-1}`, rendering each as a distinct canvas handle. **Fix 2 — carry the keyed context into the node.** `buildNodeInput` (AF-M9-12) now emits **every** declared port for a `resolveInputs` node, `{ [port.id]: merged-or-null }`, where a port with no contributing edges, or whose upsteam never produced output, is `null` — not a missing key, so a *skipped* branch is distinguishable from an *empty* result. That keyed map is what the executor receives as `context`. **Fix 3 — the v2 executor.** Modes: `byInput` → `{ input0, input1, … }` (dash dropped — the W2 shape), each branch under its port key, `null` for a branch that never ran; `append` concatenates each branch's `items` list into one `items` (also flattening legacy flat-array context); `mergeByKey` shallow-deep-merges the branches. **Legacy keep-alive.** v1 configs (`{ mode: "combine" }` / `{ mode: "append" }`, no `inputCount`) remain valid in the v2 schema (the hidden `combine` value is preserved for save) and execute — `combine` still wraps each branch under `combineKey` — so a pre-v2 saved `MERGE` runs unchanged on the new engine. `definition.migrate` (1→2) backfills `mode: "append"` (the v1 default) and `inputCount: 2`. **Tests**: 14 unit (`definition.test.ts`) + 14 unit (`execute.test.ts`) + 3 engine integration (port order independent of node/topo order; a skipped CONDITION branch yields `{ input0:{…}, input1:null }`; a legacy `combine` config executes to its old wrapped output). Full unit suite 1100 green, engine integration 25 green, `tsc`/`biome`/`build` clean. *Recorded, not hidden: `def.migrate` still has no call site — version-threading `TraceNode.typeVersion` through `buildExecutionPlan` – `buildNodeInput` is out of scope for this task, so a genuinely `version:1` row is handled at the execute layer (the legacy configs above) rather than by an explicit migrate pass.* | M9 |
| 2026-09-03 | **AF-M9-12 — Branch isolation: each node's input is resolved from its incoming edges (gap G5, ADR-0019).** The runner kept one `context` bag and did `context = result` after every node, so in `A → (B, C) → D`, C ran with B's output (B ran just before C in the sequential loop) and D ran with only C's — fan-out branches shared one mutable bag. **Fix.** `context` is no longer the source of truth for a node's input. New `buildNodeInput(node, incoming, nodeOutputs, idToName, fallbackContext)` in `src/inngest/functions.ts` resolves each node's input from its **incoming edges**, with `nodeOutputs` keyed by node name (edges identify endpoints by `fromNodeId`, hence the `idToName` map): no incoming edges (a trigger) → the run's initial `context` (`event.data.initialData`); one edge → that upstream's output; several edges into one `main` port → merged left-to-right in deterministic edge order; several edges across several ports → keyed by `toInput` port id (implemented but unreachable today, since every registered node declares a single `main` port — it is the contract multi-port nodes will consume). `nodeInputValue` is used for **both** `makeResolver` (so `$json`/`$node`/expressions resolve against per-node input) and the executor's `context`. The flat rolling `context` is **retained as a read-only backcompat view** — still accumulated and still the fallback for no-incoming-edge nodes, but no longer on the critical path; for linear chains edge-derived input is identical to the old bag, so every existing and seeded catalogue template keeps resolving. **Pass-through preserved.** The disabled-node path (AF-M9-04) now records its resolved input as its `nodeOutputs` entry before skipping, so a successor's edge resolution sees the pass-through value rather than an empty object and the branch is not severed — the pre-existing disabled pass-through engine test passes unchanged. A `continueOnFail` failure stays absent from `nodeOutputs` (a failed node has no output), so downstream merges to `{}` rather than inheriting a stale bag — a documented isolation-over-inheritance deviation (ADR-0019), no existing test regressed. **Concurrency is explicitly out of scope** — isolation, not parallelism; execution stays sequential in topological order. **Tests.** Three engine integration tests in the new `branch isolation (AF-M9-12)` suite: (1) C's recorded `input` carries A's output (`fromA`) and is free of B's field, proving C is not B's successor; (2) D's input carries both B's and C's fields; (3) two incoming edges into one port merge to the same input across repeated runs (a pure function of the persisted graph). Full suite **1306 passing** (was 1303); `npm run build` + biome clean. ADR-0019 records the decision and the `execution_engine.md` §3.2/§4/§5 detail it supersedes. | M9 |
| 2026-09-03 | **AF-M9-18 — Per-node input/output persisted on `NodeExecution` (found during AF-M9-05).** `NodeExecution.input`/`output` were declared in the schema, selected by `executions.getOne`, documented in `execution_engine.md` §8 and promised by correctness property P4 — and the runner never wrote either, so every per-node IO panel rendered `null`. **Fix.** The `trace-end` step now writes `input` (the node's resolved input) and `output` (its return) in the **same `updateMany`** as status/timing/cost, so the Inngest retry loop re-runs the whole step and overwrites rather than double-appends. `input` is the flat `context` the node received, captured at loop entry before execution (AF-M9-05 kept it clean of `$`-scaffolding); `output` is the node's return. Because the engine sets `context = result`, first-node `input` and `output` coincide — so the round-trip engine test uses a `SET` with a real mapping to assert they **genuinely differ** for the same node. **Truncation with a marker.** New `boundTraceValue` (`config.ts`): at/under `MAX_NODE_OUTPUT_BYTES` the value passes through unchanged; over the cap it becomes `{ [TRUNCATION_MARKER]: true, bytes, storedBytes, excerpt }` with a bounded excerpt — the marker key is property-name-carrying, so a truncated trace and an empty trace are never confused, and a customer payload is never re-emitted whole. Because AF-M2-09's hard reject already fails a run whose **output** exceeds 1 MiB at the executor boundary, the truncate path is what a stored over-cap **input** (e.g. an unbounded external-trigger `initialData`) hits. **Credentials.** Both fields derive from `context`/`result`, which AF-M3-04 already guarantees never carry the resolved-credential map, so the no-secret property transfers without a second parallel test of the same invariant. **Verified unchanged** (not re-implemented): AF-M8-06 `redactIo` already nulls `input`/`output` driven by `ioRetentionDays`; `getOne` returns them; the execution panel renders `trace.input`/`trace.output` via JsonViewer. **Tests.** 4 unit (`boundTraceValue` inc. under-cap identity, over-cap marker, independent excerpt size, stored-row-fits-cap) + 2 engine (SET round-trip; `SKIPPED` stores neither) + the Suite-6 context-hygiene test was **flipped**: it previously *pinned* `input`/`output` to `null` "until the day these get written" — that day is now, so it pins the opposite (every stored row non-null and under the ADR-0018 cap). `prisma.schema` unchanged — the columns pre-existed. Gates: build + lint pass; full vitest 1303 green. | M9 |
| 2026-09-03 | **AF-M9-09 — `SWITCH` node with config-driven outputs (gap G2).** The node catalogue had no n-way router and `NodeDefinition.outputs` was a **static** array, so a node whose output count depends on its config could not be declared. **Fix 1 — config-derived ports.** `NodeDefinition` gains an optional `resolveOutputs(config): PortDef[]`; when absent the static `outputs` array is used. A **single** shared helper `outputPorts(type, config)` in `src/nodes/ports.ts` is now the only resolution path used by the editor (handle rendering via `NodePortHandles`/`base-execution-node`, `defaultOutputId` for append), `validate()` (`checkOrphanedEdges`) and the engine (edge matching / disabled pass-through) — no third code path, so a port legal on the canvas is legal at run time. **Fix 2 — the node.** `SWITCH` (`LOGIC`) declares ordered rules `[{ outputKey, left, operator, right }]` (max 10, first match wins, operators shared with CONDITION), plus `fallback: "none" | "extra"`; `resolveOutputs` maps each rule to its `outputKey` port and appends an `extra`/"Else" port only when fallback is `"extra"`. **Fix 3 — no-match semantics.** A no-match with `fallback: "none"` emits the reserved `UNMATCHED_OUTPUT_PORT` sentinel (`trace.ts`); `markTakenEdges` early-returns on it (marks nothing) so `computeSkippableNodes` marks the entire downstream `SKIPPED` with a readable `skipReason` and the run still ends `SUCCESS` — matching n8n's `execute once`-style gate. **Fix 4 — rename safety.** `checkOrphanedEdges` reports an error naming the orphaned edge and the stale port when a `fromOutput` no longer exists on the node's resolved ports, so renaming an `outputKey` can no longer silently detach that branch. **No custom config component.** SWITCH's `rules` (string columns + enum operator) renders via the auto-`fieldList` branch of `resolveConfigFields`, so the panel cost is zero. `/docs/nodes/SWITCH` ships through the existing `generateStaticParams` from the registry, and `node-reference.ts` now resolves documented ports through the same `outputPorts` helper so the docs cannot describe a divergent port set. **Tests:** SWITCH `execute.test.ts` (8), `definition.test.ts` (10), a `ports.test.ts` invariant, `trace.test.ts` sentinel + SWITCH-reachability (each rule to its own branch; fallback-none no-match SKIPs everything downstream), and `validate.test.ts` orphaned-edge; a `deployment-env-router` gallery template now demonstrates SWITCH so the catalogue "exercises every node type the palette offers" invariant holds (the template harness' `checkGraphShape` was updated to resolve a source node's ports through `outputPorts` rather than the static `outputs` array — the same fix as the validator — and `EXPECTED_TEMPLATE_COUNT` moved 20→21); `npm run build`, `npm run lint`, and the full suite pass. | M9 |
| 2026-09-03 | **AF-M9-08 — Typed `SET` assignments, and a nested-write aliasing fix (gap G8).** SET wrote the compiled string, so a boolean assigned to a mapping persisted as `"true"` and an object as `"[object Object]"`. **Fix 1 — per-mapping type.** The SET `configSchema` now declares `mappings[].type` as a `z.enum(["string","number","boolean","object","array"]).default("string")` (`src/nodes/core/set/definition.ts`), so **pre-typing configs keep working unchanged** (the default fills in on `safeParse`). Because `type` is a third column, SET's `mappings` shifts from the 2-key `keyValueList` to the `fieldList` branch of `resolveConfigFields` — but the config panel was already generic (AF-M1-06): `resolveListColumns` derives a `<select>` for the enum column from the schema, so the panel renders a **Key / Value / Type** row editor with zero hand-written UI, fulfilling "expose a type selector per mapping" purely through schema introspection. The parsed-time read uses raw `data`, so the executor defaults via `mapping.type ?? "string"`. **Fix 2 — strict typed parsing.** `execute.ts` gains `parseTypedValue`: `string` passes through; `number` requires a non-empty finite literal (`Number.isFinite`); `boolean` must be exactly `"true"`/`"false"` (case-insensitive); `object`/`array` `JSON.parse` with shape enforcement (object rejects arrays; array rejects objects). **Every failure throws `new NonRetriableError('Set node: "<field>" …')` naming the field — never a silent coercion**, matching the acceptance criterion and the `http/request`/`webhook/out` message style. `object`/`array` resolve as JSON text (compiled via the `json` helper from AF-M9-07) before parse. **Fix 3 — the aliasing defect.** The runner's `context = result` plus `{ ...context }` shallow-copy meant `setNestedValue` stepped into the *shared* upstream nested object, so writing `user.name` mutated the previous node's recorded output (`nodeOutputs[prev]`/`context`). `setNestedValue` now **clones-on-descend** — before stepping into an existing plain-object branch it writes a shallow copy back and descends into that, so a nested write never touches the upstream object (arrays are still not descended into). **Tests:** 16 in SET (definition `type`-default + 5-type acceptance + unknown-type rejection; `execute.test.ts` covers each type round-trip, default-to-string, the non-aliasing nested write, and every failure path with its field-named `NonRetriableError`); config-schema catalogue, registry guards, template (63) and validate (41) all stay green. `npm run build` and `npm run lint` pass. | M9 |
| 2026-09-03 | **AF-M9-07 — Expression helpers, raw output, and the `webhook.*` map (gaps G8, G14).** Handlebars had none of the portability helpers real n8n templates rely on — no `?.`, no `\|\|`, no arithmetic — and the only expression a ported `$json.body.x` could produce resolved silently to `""` (G14: the webhook receiver nests under `webhook.{body,headers,query,method}`, every template writes `$json.body`). **Fix 1 — the helper set.** A canonical `EXPRESSION_HELPERS` list in `src/features/executions/template.ts` (the single source of truth shared by runtime registration and validation): `default`, `get` (dotted path incl. `"a.b.0.c"`), `json` (kept), `eq/ne/gt/gte/lt/lte`, `and/or/not`, `add/sub/mul/div` (div-by-zero throws), `len`, `upper/lower`, and `formatDate` (date-fns; ISO string / `Date` / epoch ms/s; unparseable throws) — each unit-tested including its failure path (63 tests in `template.test.ts`). **Fix 2 — unknown-top-level-root warning.** The shared graph validator (`src/engine/validate.ts` → `checkTemplateRoots`, user-decided **Option B: full root inference**) now warns at save time when a config template references a root the workflow cannot produce. Valid roots = the always-present meta keys (`$json`/`$node`/`$execution`/`$workflow`/`$now`) + `EXPRESSION_HELPERS` + every node's `variableName` + every SET mapping first segment + the trigger's seeded keys (webhook → `webhook`; schedule → `schedule`; google-form → `googleForm`; stripe → `stripe`; manual → `trigger`) + the manual trigger's flat-spread payload keys (enumerated from the node's own `payload` JSON — the same string the executor parses, so any key a graph references from a manually-supplied payload validates). **Root inference is what makes the authoring harness gate green:** the reference workflows reference `{{schedule.timestamp}}`, `{{googleForm.*}}`, `{{stripe.*}}` and manual-payload keys like `{{title}}`, all of which resolved as unknown until the inference covered them (the harness's `expect(...).toEqual([])` on save warnings was the acceptance check, and it had failed before this). Root extraction is AST-based via the helper-aware `getTemplateRoots` (`Handlebars.parse` — helper callees and `@data` refs are not data roots), so a template is never compiled or executed during validation. **Webhook roots deliberately scoped to `["webhook"]` only** — flat `body` is excluded so the idiomatic ported `{{body.x}}`/`{{$json.body.x}}` is caught (the G14 acceptance case). **Warning severity, non-blocking**: the shared validator's `errors[]` carries `severity`, the save router throws only on `"error"`, and the canvas lint already surfaces warnings — no save-response change, matching how disconnected-node warnings already flow. Disabled nodes skipped (AF-M9-04 parity). +11 tests (`validate.test.ts`, now 41; covers meta/variableName-roots ok, helper-callee-not-a-root, webhook.* ok, stale `{{body.userId}}` warns with message suggesting `webhook.body`, SET-key roots, literal-only/no-root, disabled-node skip, unparseable skip). **Docs:** new `docs/nodes/expressions.md` (the full map + triple-stache/`json` raw-JSON rule, mirroring the n8n→AutoFlow section added to the in-app `/docs/expressions` page); `docs/nodes/webhook-out.md` example corrected from the stale `{{data.*}}` to the valid `{{webhook.body.*}}`; ADR-0007 **amended** (not superseded) with the helper table + escaping rule + unknown-root-validation note. `npm run build` and `npm run lint` pass. | M9 |
| 2026-09-03 | **AF-M9-06 — Per-node run policy is declared, validated and editable (gap G11).** The runner read `data._timeoutMs` and `data._continueOnFail` — two keys in no `configSchema`, no UI and no documentation. Nothing could set them, nothing validated them, and all three reference workflows AF-M9 targets set `retryOnFail: true, maxRetries: 2` per node, which was simply not expressible. **Fix.** One reserved key `_run`, one `runPolicySchema` (`src/nodes/shared/run-policy.ts`, client-safe): `maxAttempts` 1–5, `backoffMs`, `timeoutMs` 250 ms–5 min, `continueOnFail`. A pure `resolveRunPolicy` implements the precedence — node `_run` > legacy keys > `definition.defaultRetry`/`timeoutMs` > engine defaults — with an out-of-bounds *inherited* timeout **clamped rather than rejected**, so tightening a limit cannot turn saved workflows into failures. The config panel gained a collapsed "Run settings" section whose placeholders show the **inherited** value ("Inherits 3") instead of pre-filling it, so an explicit 3 stays distinguishable from the default 3; clearing the last field removes `_run` entirely rather than leaving `{}`. **Three acceptance criteria did not survive contact and are recorded as substitutions, not silently reinterpreted.** (1) `_run` is **not** merged into each node's `configSchema`: that schema also drives the config form (`resolveConfigFields` reads its `.shape` and throws on unsupported field kinds), and three definitions are `z.object({}).optional()` so `.extend()` is not even uniformly available — `validate()` parses `_run` separately and reports through the same error channel (`path: "_run.maxAttempts"`), which is the user-visible outcome the criterion wanted. (2) **No data migration ships.** The grep is over the code rather than a database, which is the stronger check: neither legacy key has ever had a writer — not the editor, not a template, not the public API, only engine test fixtures — and "grep the database" is precisely what AF-M8-12 got burned by, since it covers only the database you point at. The resolver reads both keys as a documented fallback instead: four lines that cannot be wrong, against a migration over rows that provably do not exist. (3) **"Records `attempt` correctly" needed a code fix.** `NodeExecution.attempt` was seeded from the *Inngest function* attempt, which is `1` for every node on a normal run, and neither the success nor the failure path updated it — so a node that failed twice and succeeded on the third try recorded `attempt: 1` and the retry was invisible in the trace. `execution_engine.md` §6 claimed "each attempt is a `NodeExecution` row with an incrementing `attempt`"; there has only ever been one row per node. The row now records the attempt it finished on, and the doc is corrected. **Tests: 15 unit (precedence ladder) + 7 validator + 7 dom + 5 engine + 2 reserved-key guards.** The engine harness gained transient-failure injection at the step boundary — where the retry loop actually catches — plus a `stepLog`, so a test asserts attempts 1/2/3 happened and 4 did not. 31 existing fixtures migrated to `_run`, with one deliberately left on the legacy keys to prove the no-migration decision holds. Gates: `tsc` clean, `biome` clean (629 files), 1215/1215 tests, `npm run build` green. | M9 |
| 2026-09-03 | **AF-M9-05 — Template scaffolding no longer leaks into stored execution records (gap G9).** The runner handed executors the **enriched** context (`$json`, `$node`, `$execution`, `$workflow`, `$now`) as `context`, and every executor returns `{ ...context, … }` — so the scaffolding was returned with the real data, and `$json` self-references the accumulated context, meaning each hop embedded the previous hop's entire payload. `Execution.output` grew superlinearly with node count, and since AF-M2-09/ADR-0018 measures the per-node byte cap on exactly that returned object, the cap was being spent on scaffolding the user never asked for. **Fix.** `NodeRunParams` splits into `context` (the plain accumulated upstream output) and `resolve(template)` (a closure over the enriched view, built by the new shared `makeResolver` in `template.ts`). The enriched object now exists only inside that closure, so it is structurally impossible to return — not a rule anyone has to remember. All **14 executors** migrated from `compileTemplate(x)(context)` to `resolve(x)`; the runner and the node unit tests build their resolver through the same `makeResolver`, so a unit test cannot drift from what the engine does. **The static check is not the one the task specified, and that is recorded rather than glossed:** "fails if an executor returns a key starting with `$`" is a runtime property and cannot be checked statically. The stronger static property is that no `execute.ts` may import `compileTemplate`, `buildTemplateContext` or `makeResolver` — with `context` clean, those imports are the only route back to the scaffolding. Two guards in `registry.test.ts`, both walking the tree rather than using a hand-list so neither can fail open. Likewise the "within 2× the largest single node output" bound was unmeasurable (see below), and was restated as growth-bounded-by-data: a 6-node graph's output stays under 4× a 2-node graph's, plus an absolute ceiling so both cannot bloat together. **Tests: 3 engine integration + 2 static + 12 fixture migrations.** The hygiene test walks every key at every depth; a companion test proves `$execution.id` still *resolves*, since simply deleting the scaffolding would otherwise satisfy it. A test-only `withResolve` helper (`src/nodes/shared/test-params.ts`) fills `resolve` from each fixture's own context — `resolve` is deliberately **required**, not optional, because an executor able to fall back to `context` would reintroduce the leak. **Found while doing this: `NodeExecution.input`/`output` are never written.** The columns exist, `executions.getOne` ships them to the client, `execution_engine.md` §8 documents them and correctness property P4 promises them — and the runner persists only `Execution.output`. Per-node IO is permanently `null` in the UI, so AF-A-05's "per-node execution traces" is complete only for status, timing and cost. Filed as **AF-M9-18**; the hygiene assertions over those two fields hold vacuously today and a test pins that, so they already guard the columns the day they start being written. Also corrected the now-stale AF-M9-01 harness comment claiming the JSON deep-clone was load-bearing against circular structures — it no longer is, and the reason to keep it (step-serialization fidelity) is different. Gates: `tsc` clean, `biome` clean (627 files), 1179/1179 tests, `npm run build` green. | M9 |
| 2026-09-03 | **AF-M9-04 — `Node.disabled` is honoured by the engine (gap G10).** The editor has shipped an "Enabled" checkbox since M1 (`node-config-panel.tsx`), `saveGraph` has persisted `Node.disabled` all along, and `cost-estimate.ts` already excluded disabled nodes from the estimate — so switching a node off looked like it worked and even moved the cost preview, while **the engine ran the node anyway**. `disabled` was read nowhere in `functions.ts` or `validate.ts`. **Fix.** The column is now plumbed through every path that reaches the runner — the Prisma read, the draft `graphSnapshot` (so in-editor test runs skip the same nodes), `buildTestGraph`, the client lint adapter `toGraph`, and `buildExecutionPlan` — and the node loop skips a disabled node with a `SKIPPED` trace and `skipReason: "Skipped: node is disabled"`, visible per correctness property P1 rather than dropped. **Semantics are n8n's pass-through**, with three sharp edges recorded in `execution_engine.md` §3.3: (1) the disabled check runs **after** the branch-taken check, so a disabled node on an untaken branch is skipped as unreachable and its edges are *not* marked taken — marking them would resurrect the tail of a branch the run never entered; (2) pass-through takes the node's **first declared output** (via AF-M9-03's `defaultOutputId`), because a disabled branching node has no condition left to evaluate and taking every output would execute a graph the author never drew; (3) a disabled node is **exempt from config and required-input validation** — turning a node off is how people park work in progress, and a half-finished config on one must not block saving the workflow — while structural checks (cycles, unknown types) still apply, because the engine resolves every registration to build the plan. **Tests: 3 engine integration + 6 validator unit.** The pass-through test asserts the downstream received the *upstream* value through a marker the disabled node would have overwritten; a second disables a node whose config would throw if executed, proving the executor is never invoked; a third proves reachability still wins. Two validator tests assert the **enabled** case still fails, so the exemption is the flag and not the fixture. Two pre-existing `toEqual` shape assertions were updated for the new field. **Deliberately out of scope:** disabling a *trigger* still dispatches a run — the webhook route, cron evaluator, and Run button decide that upstream of the engine and none consult `disabled`, so the row is created and the quota consumed before the engine ever sees it. Filed as **AF-M9-17**. Gates: `tsc` clean, `biome` clean (626 files), 1174/1174 tests, `npm run build` green. | M9 |
| 2026-09-03 | **AF-M9-03 — Declared ports are the handle identity (gap G1).** `docs/architecture/node_sdk.md` has always said `PortDef.id` is "the React Flow handle id and Connection.fromOutput/toInput". The code did not obey it: `BaseExecutionNode`/`BaseTriggerNode` rendered ONE hardcoded `source-1`/`target-1` pair per node, `node-selector.tsx` hardcoded the same literals when appending, and `saveGraph` persisted them verbatim as `fromOutput`. `markTakenEdges` then compared `edge.fromOutput` against a CONDITION's `_outputPort` of `"true"`/`"false"`, never matched, and `computeSkippableNodes` marked the node's **entire downstream `SKIPPED`** — so every branching workflow built in the editor has been silently doing nothing after the condition, ending `SUCCESS` while skipping the work. `src/inngest/trace.test.ts` passed throughout because it hand-builds edges with correct port ids, which the product never did. **Fix.** New client-safe `src/nodes/ports.ts` is the single resolution path (`inputPorts`, `outputPorts`, `defaultOutputId`/`defaultInputId`, `normalizePortId`, `resolveEdgePorts`, `handleOffset`), and new `NodePortHandles` renders one handle per declared port, id = `PortDef.id`, evenly distributed, labelled only where a side has ≥2 ports so single-port nodes stay pixel-identical. **Four surfaces now share `resolveEdgePorts`** — `saveGraph`, `buildTestGraph`, the client lint adapter `toGraph`, and the `runGraph` harness — which turns "the client lint MUST mirror the server" from a comment asking future authors to remember into something the code enforces. It translates a stale browser tab's legacy handles rather than rejecting them, and passes an unrecognised id through untouched so `validate()` reports it instead of quietly rewiring a user's edge to a branch they never chose. **Two judgement calls, both recorded in the task.** The default port is the *first declared* one, not `"main"` — a CONDITION has no `main`, so appending lands on `true`; and the `+` append affordance is hidden on multi-output nodes, where "next" has no unambiguous meaning and the port labels occupy that spot. **Migration** `20260902120000_declared_port_ids` repairs live rows. CONDITION is the only registered type whose first output is not `main`, so the SQL is small; a CONDITION with **two** legacy edges is deliberately left alone and reported by `RAISE NOTICE`, because both were drawn from the same rendered handle and nothing recorded which was meant to be the false branch — rewriting both to `true` would invent a graph the author never drew. **Tests: 21 unit + 7 dom + 12 integration.** The AF-M9-01 G1 characterization test flipped from asserting the bug to asserting the fix, and gained the two cases it could not distinguish (a fix that ran *both* branches would have satisfied the old assertion identically): true-path and false-path each run exactly one side. Six integration tests execute the **shipped migration file** rather than a copy of its logic, covering idempotency, the ambiguous CONDITION, an unmanifested type (the AF-M8-12 retirees), and that an authored `"false"` is never touched. The three-output dom case mocks a synthetic manifest entry, since no registered type declares three outputs until `SWITCH` (AF-M9-09). **Not verified:** the rendered canvas — no screenshot; reaching the editor needs a signed-in session and the alternatives were worse than the gap. Residual risk is cosmetic label placement only. Gates: `tsc` clean, `biome check` clean at 626 files, 1023/1023 unit+dom, 141/141 integration, `npm run build` green. | M9 |
| 2026-09-03 | **AF-M9-13 — `CODE` node: sandboxed, no network, hard caps (ADR-0020).** The `CODE` type (`TRANSFORM`, single `main` input/output) runs arbitrary tenant JS as the body of an arrow function `(input) => { … }`, inside a **dedicated `worker_threads` Worker** with a fresh `node:vm` context — never on the main/Inngest thread, because `vm` alone stops neither `while(true)` nor an OOM. The `configSchema` (`src/nodes/core/code/definition.ts`) accepts `code` (1..100k chars) plus per-node caps clamped to engine ceilings: `limitTimeoutMs` default 5000 / max 30000, `limitHeapMb` default 64 (ceiling 256), `limitOutputBytes` default 1 MB (ceiling == 1 MB). The worker glues the sandbox with no host bindings — `process`, `require`, `Buffer`, `fetch`, `setTimeout`, `module` are all `undefined`; network is structurally impossible. Every cap breach throws a `NonRetriableError` **naming the limit** — never a silent truncation (infinite loop killed at 250 ms, allocation bomb killed at 32 MB heap in tests). Object returns pass through verbatim; an array is stored under `items` so `SPLIT_OUT` consumes it. User errors surface the **user's line number**: the wrapper's syntax-error stack shows `code.js:2` and runtime shows `code.js:3:12`, so `sandbox.ts` matches `/code\.js:(\d+)/` and rebases (`rawLine - 1`, body starts on line 2). One deliberate deviation recorded in the task: per-node CPU time is **not** recorded on the `NodeExecution` (out of scope) — `worker.resourceLimits` gives no accurate CPU readout, so wall-clock `durationMs` already in the trace is the proxy; documented as an AF-M7-04 follow-up. 8 unit tests cover object/array/primitive-return, line-number error, infinite loop, allocation bomb, no host bindings, prototype-pollution containment. `npm run build` + `npm run lint` (biome) clean; `tsc --noEmit` clean apart from the 2 pre-existing baselines. ADR-0020 records the sandbox choice and what it does **not** defend against; `docs/architecture/security.md` §6 gained the Code-node carve-out. | M9 |
| 2026-09-02 | **AF-M9-02 — Test-only loopback egress allowance (G13).** `ALLOW_LOOPBACK_EGRESS=1`, parsed in `src/lib/env.ts` and off by default, lets the workflow HTTP nodes reach `127.0.0.1`/`::1` (and `localhost`) so the engine's acceptance suite can run a deterministic, offline fixture server. It widens **exactly loopback and nothing else**: `isBlockedIp`/`isBlockedIpv4` in `egress-guard.ts` gained an `allowLoopback` option that unwinds only the `127/8` and `::1` branches — metadata (`169.254/16` incl. `169.254.169.254`), `10/8`, `172.16/12`, `192.168/16`, CGNAT (`100.64/10`), and unique-local IPv6 all stay blocked under the flag. `resolveSafeEndpoint` reads `allowLoopbackEgress()` once per call and threads it into `isBlockedIp`, so both `assertSafeEndpoint` call sites and `safeFetch`'s per-redirect-hop re-vetting inherit it, and the flag flips without a restart. **Production refusal is enforced twice**: `allowLoopbackEgress()` throws a clear "test-only flag … forbidden in production" error, and `ensureEnv()` calls it at boot so a misconfigured deploy refuses to start before any request runs. Documented as test-only in `.env.example` and recorded in `docs/architecture/security.md` §5 with the reason it cannot widen. 18 new unit tests (egress-guard: off→blocked, on→loopback+`localhost` allowed, metadata/private/CGNAT still blocked, IPv4-mapped loopback, end-to-end wiring; env: production refusal + `=== "1"` exactness) — green alongside the existing 59 egress-guard tests. | M9 |
| 2026-09-02 | **AF-M8-28 — Auth emails redesigned into a shared, metric-based template.** `verificationEmail` and `passwordResetEmail` (sent via Resend from `src/lib/email.ts`) now share a single `wrapEmail()` shell: table layout with inline-only CSS (clients strip `<style>`), `#fafafa` page background, white card, zinc footer, and a single locked accent. The header carries the AutoFlow wordmark as an **inline flat SVG** (three-slab mark, `#FF7A00`/`#FF9736`/`#FFBC7D`, 96x39) — the `#autoflow-darkmode-logo.svg` gradient/blob blend was rejected because complex gradients mangle in email clients. CTA button uses `#c2410c` (orange-700) with white text for ~4.5:1 AA rather than pure `#FF7A00` (fails AA). Copy follows the applied design rules: system-ui/Geek font stack (no web-font loading), **no em-dashes anywhere**, no filler verbs, warm non-slop tone. Both emails keep their plain-text fallback link and anti-phishing safety lines; password reset keeps "The link expires after one hour." `EmailBody` was un-tangled: dead `fallbackHint` removed and `ctaUrl` promoted into the type, killing the `EmailBody & { ctaUrl: string }` intersection. 5 email tests unchanged and green; `tsc --noEmit` and `biome check` clean. | M8 | `ALLOW_LOOPBACK_EGRESS=1`, parsed in `src/lib/env.ts` and off by default, lets the workflow HTTP nodes reach `127.0.0.1`/`::1` (and `localhost`) so the engine's acceptance suite can run a deterministic, offline fixture server. It widens **exactly loopback and nothing else**: `isBlockedIp`/`isBlockedIpv4` in `egress-guard.ts` gained an `allowLoopback` option that unwinds only the `127/8` and `::1` branches — metadata (`169.254/16` incl. `169.254.169.254`), `10/8`, `172.16/12`, `192.168/16`, CGNAT (`100.64/10`), and unique-local IPv6 all stay blocked under the flag. `resolveSafeEndpoint` reads `allowLoopbackEgress()` once per call and threads it into `isBlockedIp`, so both `assertSafeEndpoint` call sites and `safeFetch`'s per-redirect-hop re-vetting inherit it, and the flag flips without a restart. **Production refusal is enforced twice**: `allowLoopbackEgress()` throws a clear "test-only flag … forbidden in production" error, and `ensureEnv()` calls it at boot so a misconfigured deploy refuses to start before any request runs. Documented as test-only in `.env.example` and recorded in `docs/architecture/security.md` §5 with the reason it cannot widen. 18 new unit tests (egress-guard: off→blocked, on→loopback+`localhost` allowed, metadata/private/CGNAT still blocked, IPv4-mapped loopback, end-to-end wiring; env: production refusal + `=== "1"` exactness) — green alongside the existing 59 egress-guard tests. | M9 |
| 2026-09-02 | **AF-M9-01 — Engine execution harness: run a whole graph in a test.** There was no way to assert a graph *executes* — only that it plans. `tests/integration/engine/run-graph.ts` exports `runGraph`, which takes a `TemplateGraph`, seeds a fixture org + workflow + execution via Prisma, converts the planning-era `TemplateGraph.edges` connection shape into the executor's `{ fromNodeId, toNodeId, fromOutput, toInput }` form, drives `executeWorkflowHandler` directly, and returns the terminal `Execution` plus its ordered `NodeExecution` rows — asserted on **real DB rows**, not mocks. **No `@inngest/test` dependency**: the task's stated first approach was replaced with a **memoless fake `step`** whose `run` executes each callback inline (no replay, no memoisation) — the pattern the AF-M8-27 cancellation suite already proved against real Postgres. **Two defects surfaced by making the suites honest.** (1) The `$json` context is self-referential (`buildTemplateContext` sets `$json: accumulatedContext`), so the fake `step.run` deep-clones via `JSON.parse(JSON.stringify(value))` to mirror Inngest step-serialization, or `serializedBytes` throws `Converting circular structure to JSON`. (2) The quota suite initially seeded 100 executions into an org the **test** owned while `runGraph` seeded a fresh one, so the gate saw `current=0` and let the run through — the suite passed for the wrong reason; `runGraph` now accepts `orgId`/`userId`/`workflowId` overrides, the quota suite passes its own org, and it ungates the global `E2E_SERVER=1` bypass (restored in `finally`) so the gate evaluates. **Four suites, all green:** linear 3-node → `SUCCESS`; failing SET without `continueOnFail` → downstream `SKIPPED`; quota-exceeded → terminal `QUOTA_EXCEEDED`, zero node rows, no retry; and the **G1 regression** (a CONDITION whose edges carry the editor's real `source-1` handle ids leaves downstream `SKIPPED` when it should be `SUCCESS` — the assertion documents the bug and must flip in AF-M9-03), proving `markTakenEdges` compares `edge.fromOutput` against CONDITION's `_outputPort` of `"true"`/`"false"` and never matches the canvas's `"source-1"`. Suites truncate fixture tables in `beforeEach`, run serial inside the existing `integration` project (`maxWorkers: 1`). Gates clean: `run-graph.test.ts` 4/4, `npx biome check .`, `npx tsc --noEmit`, `npm run build`. `docs/engineering/testing_strategy.md` gained §3.5 "Engine — execution tests". | M9 |
| 2026-09-02 | **AF-M2-09 — Node output bounded at the executor boundary (ADR-0018).** The AF-M2-00 spike proved by measurement that Inngest state limits cap a run's accumulated output; this is that limit put into the execution path instead of letting an oversized payload die as an opaque Inngest state error attributed to no node a few steps after the node that produced it. `executeWorkflow` (`src/inngest/functions.ts`) checks output size exactly once, immediately after the per-node retry loop and before the value is captured for `$node`/`context` — the single point every executor's return passes through, so one check covers every node type with no per-node edits. Over-limit fails with a `NonRetriableError` naming the node and the actual size; it **never truncates**, because a workflow that silently drops half an API response produces results that look right and are wrong (AF-M0-05). Threshold is one exported constant (`MAX_NODE_OUTPUT_BYTES` in `src/inngest/config.ts`, next to `MAX_STACK_LENGTH`) so the measurement can move it in one place. Verified by a 9-unit suite: `serializedBytes` UTF-8 semantics plus the boundary predicate on both sides and exactly-on the threshold. *(This changelog row was written after the fact, with the AF-M8-27 docs pass — the code shipped in `c29c530` and the entry was in `tasks.md`, but the progress row was missed.)* | M2 |
| 2026-09-02 | **AF-M8-27 — Cancelling an execution now stops it (cooperative, inside the node loop).** Closing the gap found during AF-M2-00: `executeWorkflow` never read `CANCELLED`, so a cancelled run kept executing every remaining node — third-party calls, AI spend, quota, and `NodeExecution` rows all continued against an execution row that said it stopped. The `Execution` row's status is now checked at the top of each node iteration via a `cancel-check:<nodeId>` Inngest step; on `CANCELLED` the remaining nodes are pushed into `skippedNodes` (they get SKIPPED traces instead of running), a `cancellationDetected` flag is set, and the loop breaks. The finalize step and `notify-execution-succeeded` are gated on that flag, so the terminal `CANCELLED` status/`completedAt` are never overwritten back to SUCCESS and no success notification fires. Handler extracted to a hoisted `executeWorkflowHandler` annotated `Context.Any & { publish: ... }` with a typed cast at the `createFunction` boundary (`Handler.Any` cannot be destructured). Cooperative as scoped: bounds the damage to the single node executing at cancel time. 2 new integration tests (pre-cancelled run → all nodes SKIPPED, result CANCELLED; in-flight run → run stops between nodes and final status stays CANCELLED rather than being rewritten to SUCCESS). Full suite **108 files / 1095 tests**, tsc + biome + `npm run build` clean. | M8 |
| 2026-09-02 | **AF-M8-25 — Docker Desktop replaced with Podman; integration suite runs green against a host-network test DB.** Docker Desktop is unusable on this box (its Windows engine won't start, and the WSL VM could not pull images), and `docker run -p 5433:5432` failed with `netavark (exit code 1): nftables error` — the WSL VM's kernel has no loadable `nf_tables` module, so Podman's netavark cannot publish ports. **Podman replaced Docker Desktop**: `podman machine start` exposes the `npipe:////./pipe/docker_engine` pipe; the CLI is pointed at Podman via the `podman-machine-default` context (see persistence note below). Recipe that works: containers run `--network host` with the port set at the app layer (`PGPORT=5433`), reachable from Windows at `127.0.0.1` through `%USERPROFILE%\.wslconfig` `[experimental] hostAddressLoopback=true`; `test:db:down` needs no change. **Script aligned 2026-09-02:** `test:db:up` still published `-p 5433:5432` (same netavark failure on repeat runs), so it now uses `--network host -e PGPORT=5433`, and the full `test:db:up` → `test:integration` → `test:db:down` cycle was re-verified green (13 files / 114 tests, 0 failing). Verified `npm run test:integration` → **13 files / 114 tests, 0 failing**. Documented where the workaround can be found: `docs/operations/environment_setup.md` (§5.1 callout + §10 troubleshooting row), `docs/operations/local_setup_guide.md` (§5 docker-CLI routing note + the "Running the integration suite (test database)" recipe + §8 troubleshooting rows + §9 checklist row), `docs/engineering/testing_strategy.md` §6.2. **Persistence resolved 2026-09-02:** the CLI's active context was switched to Podman's `podman-machine-default` (`docker context use podman-machine-default`; persisted in `~/.docker/config.json`), so fresh terminals are routed without any env var; revert with `docker context use desktop-linux`. | M8 |
| 2026-09-01 | **AF-M8-24 — Finished the AF-M8-12 fallout, and found a live save-path bug on the way.** Deleting the retired AI node folders left the branch red: **`tsc` and `biome` both failing, 9 tests down across 10 files** — CI could not have caught a real regression in that state. **The serious find was not test fallout.** `saveWorkflowInputSchema`, the `.input()` of the `saveGraph` mutation, hand-mirrors the registry, and the two had drifted in *both* directions: `ANTHROPIC`/`GEMINI`/`OPENAI` still listed — resolving deleted types, throwing `UnknownNodeTypeError` at import and taking the module down, which is what broke the two integration suites — and **`AI_LLM` and `AI_EXTRACT` missing entirely.** That one predates AF-M8-12 and is user-facing: both shipped in M5, both sit undeprecated in the palette, both are what the retired trio was migrated *onto*, and **a canvas containing either could not be saved**, because input validation rejected it before the handler ran. The list stays hand-written (`makeNodeSchema` needs a literal type per entry or `z.discriminatedUnion` loses its discriminant), so drift is now caught by a test asserting the union and the registry agree both ways and naming the offending types. **Four unit guards asserted that a deprecated type EXISTS before checking anything about it**, so a completed retirement failed the suite — the tests punished the cleanup they existed to make safe. Each is restated as the rule it actually guards (ADR-0011: deprecated stays registered and executable; never offered in the palette; never authored by a template — and now never *unregistered* either), so each holds vacuously today and re-arms itself at the next deprecation. `credential-injection` resolved three node types by name and therefore only ever guarded three nodes; it now walks every credential requirement in the catalogue. The `server-only` guard was a hand-listed set of folders that named three deleted directories and **failed open** — add a node, forget to list it, and the guard silently skipped it, the one case it exists to catch — so it now walks `src/nodes`. Also fixed a **pre-existing** rate-limit flake unrelated to AF-M8-12: the FREE bucket holds 60 tokens and refills at 1/s, and the test awaited 61 database-backed requests in turn, so under full-suite load it refilled faster than it drained. It passed alone and failed in the full run — the worst way for a test to fail. Fired as a concurrent burst instead, which is deterministic and a truer test of a token bucket. Fourth fix this session for an assertion coupled to something incidental. Result: **1012/1012 across 102 files**, and all four CI gates green — `tsc`, `biome`, `npm audit --audit-level=high`, and `npm test`. | M8 |
| 2026-09-01 | **AF-M8-10 — Beta launch checklist, support process, and drafted legal documents; one blocker found.** **The finding that dominates the task: `Organization.plan` is written exactly once — `"FREE"`, at organisation creation — and never again.** `organization.update` touches only `name`/`slug`; there is no Polar webhook route, no webhook handler on the auth plugin, and no `POLAR_WEBHOOK_SECRET`. Quota (AF-M7-04), rate-limit buckets (AF-M8-02), and retention windows (AF-M8-06) all read that column, so **a customer can complete checkout, be charged, and remain on FREE limits indefinitely.** Filed as **AF-M8-23** and named a go/no-go blocker — charging for a plan that is never applied is not a risk to carry into beta. **Checklist** (`docs/operations/beta_launch_checklist.md`) covers billing, security, operations, legal, and support, with every row marked by reading the code rather than trusting an earlier checklist, and ends in an explicit four-item go/no-go: payment not changing the plan, nothing paging anyone (AF-M8-21), restore never rehearsed, policies not lawyer-reviewed. Everything else is stated as carryable with its risk written down. **Support** (`docs/operations/support.md`): one inbox and an explicit "there is no on-call, do not imply coverage"; triage that routes multi-workspace symptoms to the runbooks before answering tickets; the five things a "my workflow is broken" report almost always is — an expression resolving empty (silent, because a missing path renders empty and the run continues), `{{ }}` escaping breaking a JSON body, a quota refusal, a revoked credential, a real bug; the standing rule that **support cannot read a customer's credential**, so never promise to check one and treat a request to as suspicious; and a billing section telling staff to treat "I paid but I'm still limited" as **true and expected** until AF-M8-23 ships. **Legal**: `/terms`, `/privacy`, `/dpa` drafted against what the software actually does. The privacy retention table renders from `PLAN_RETENTION`, so the policy cannot promise a window the pruner does not honour; the subprocessor schedule is a typed list in `src/config/legal.ts`, verified against the code and shared by the Privacy Policy and the DPA so the two cannot drift. **They are engineer drafts, not legal advice** — they exist so a lawyer reviews facts instead of a blank page, and that review is a launch blocker. **The engineering contribution is the guard:** the pages refuse to render their body until the legal entity is configured, showing what is missing instead. A privacy policy displaying `[COMPANY_LEGAL_NAME]` reads as a real policy to a user and as negligence to a regulator, and "we will remember before launch" is not a control; partial configuration, including a missing effective date, counts as unconfigured. Tests: **11 unit + 10 DOM** covering both directions of the guard. Verified in the browser: all three routes 200 and all three withholding their body on this unconfigured deployment. No migration. | M8 |
| 2026-09-01 | **AF-M8-13 — `SYSTEM` notifications have a producer; `APPROVAL_REQUESTED` is still blocked, and that is the honest half.** ✅ **`SYSTEM` shipped** as an operator script, `npm run notify:system` — maintenance windows, incident updates, deprecation notices, the announcements no automated signal produces. **Surface chosen deliberately:** a script, not an in-app procedure. There is no platform-admin role (`Role` is org-scoped), so an operator procedure would have meant inventing a cross-tenant privilege and exposing it on the public app — far larger and riskier than this task, for something done a handful of times a year. A script runs with database credentials the operator already holds, adds no authenticated surface, and matches the existing `scripts/` pattern including dry-run-by-default. The logic sits in `src/features/notifications/server/system-notifier.ts` rather than the script, so it is testable against a real database and a future in-app surface has something to call. **Correction to the task's own premise:** AF-M7-08 did not ship a builder for `SYSTEM` — it had an icon, a colour, and a label and nothing more; `buildSystemNotification` and `systemDedupeKey` are new. **The finding worth keeping:** `Notification.dedupeKey` is globally `@unique`, not unique per organization, so the first broadcast wrote to exactly one workspace and was silently skipped for all the others — caught by an integration test asserting two orgs both receive it. Every existing producer survives that constraint only because its key embeds a globally-unique row id (`execution:<cuid>`, `credential:<cuid>`, `approval:<cuid>`) belonging to exactly one org, so global and per-org uniqueness happened to coincide. A system announcement is the first notification type with **no owning row**, so the org id had to move into the key: `system:<announcementId>:<organizationId>`. **No migration** — the fix is in the key, not the constraint. The announcement id is operator-supplied and deliberately not derived from the copy, so fixing a typo and re-sending reaches whoever missed the first version rather than silently doing nothing. ⬜ **`APPROVAL_REQUESTED` remains without a producer, re-verified:** the approvals router exposes `list` and `respond` only, both operating on rows that must already exist, and every `approvalRequest.create` in the repo is generated Prisma code. The blocker is the approval node itself (**AF-P2-E**, Phase 2) — writing the producer now would be a call site with nothing to call it. Tests: **11 unit + 8 integration** (all-workspace delivery, no originating ids, idempotent re-run, resume after a partial broadcast, targeting isolation, new id treated as new, href, empty-org no-op); CLI exercised end to end. | M8 |
| 2026-09-01 | **AF-M8-09 — Node reference + expression docs, rendered from the registry.** A public docs site inside the existing Next app: `/docs`, `/docs/nodes` (catalogue by category), `/docs/nodes/[type]` (one page per registered type, `generateStaticParams` off the registry), `/docs/expressions`. **No new dependency** — a Docusaurus/Nextra install would have meant a second toolchain and a second design system in a repo whose rule is to prefer the existing stack; these are Next routes using the app's own tokens, public and outside the `(dashboard)` group because the reference is what someone reads *before* signing up. **The node reference is derived from `nodeManifest` at render time rather than written or generated.** Both alternatives drift — a hand-written page is stale the first time a config field changes, a generated file is stale until someone re-runs the generator — and the `reference/nodes/<node>.md` plan in `docs/README.md` was the first kind, now marked superseded. Config fields come from `resolveConfigFields`, the **same** deriver that builds the editor's config panel, so a documented field is by construction the field the editor renders and not a second description of it (ADR-0001). Deprecated types are documented on purpose: a saved workflow can still contain one and still run it (ADR-0011), so someone reading a trace has to be able to look it up — each carries its notice and a link to its replacement. A schema the deriver cannot handle degrades to "not documented automatically" rather than rendering an empty table, which would read as "takes no configuration"; a test asserts no node needs that path today. **The expression page is grounded in the implementation, not in memory:** 7 tests were added first to pin the behaviour the page promises, and they caught the page being wrong — the `$node` form is dot-bracket **without quotes** (`{{$node.[HTTP Request].field}}`), and quoting it searches for a key literally containing the quotes. The page leads with the sharpest edge, escaping: `{{ }}` HTML-escapes, so a JSON body built that way breaks the moment a value contains a quote — `{{{ }}}` or the `json` helper instead. It also says plainly that a missing path renders empty and does not fail (so a typo is silent, and an empty field downstream should make you suspect the expression before the data), that interpolating an object gives `[object Object]`, and that the absence of `eval`/`new Function`/VM is the security control rather than a missing feature (ADR-0006). Verified by loading every route including a deprecated type (notice and replacement link render) and an unknown type (404). Tests: **12 unit + 7 template**; no migration. | M8 |
| 2026-09-01 | **AF-M8-07 — Health endpoint, public status page, runbooks, and error budgets.** **Built:** `GET /api/health` (machine-readable, for an uptime monitor or load balancer) and `/status` (public, server-rendered, no session, no tRPC, no client JS — deliberately outside the `(dashboard)` group, since the moment it is needed is the moment the authenticated shell may not render). Two probes, each picked to map to a runbook entry rather than to whatever happens to be pingable: `database`, and `runner` — executions that started long ago and never reached a terminal status. **`runner` is the one that earns its place:** it detects the failure where every other surface looks healthy — the app serves, triggers fire, runs are accepted, and none of them finish. Aggregation lives in the isomorphic `src/lib/health.ts` so the sharp edges are testable without a database, and the two rules that are easy to get backwards are pinned by tests: a probe that throws is `down`, never "unknown" and never skipped (a health check that fails open reports green while the service burns), and an **empty check list is `down`** — no evidence of health is not evidence of health. `degraded` returns **HTTP 200** on purpose: a 503 would make a load balancer evict an instance that is still doing useful work, converting a partial outage into a total one, so the distinction is carried in the body for humans and alerting rather than in the code infrastructure reacts to. The endpoint is rate-limited via the AF-M8-02 shared store — each probe runs real queries, so an unauthenticated endpoint doing DB work is a free amplification primitive — and the body carries verdicts only, never an error string, which would name hosts, ports, and roles. **A defect caught by actually loading the page:** the first render after boot reported a false **"Major outage"** because a cold Prisma connection exceeded the 2s probe budget. The database probe now gets 5s; a status page that cries wolf on every deploy is worse than no status page, because people learn to ignore it. **Runbooks** (`docs/operations/runbooks.md`) for five failure modes this system can actually reach, none of which detects the others: F1 runs accepted but never completing, F2 database down or pool exhausted, F3 credentials failing to decrypt across the board (the tell is that non-credentialed workflows are unaffected), F4 AI provider outage vs runaway spend, F5 webhook flood from one tenant. Each is symptom → confirm, with the actual SQL → diagnose in order → mitigate, under one standing rule: capture the evidence before restarting, because in every one of these the state *is* the diagnosis. **SLOs and error budgets** (`docs/operations/slos.md`): four SLIs over a rolling 28 days — availability 99.5%, execution success 99.0%, latency 95% under 60s, trigger fidelity 99.9% — with the budget framed as a decision rule (ship, or fix reliability first) rather than a scoreboard, and a note that a team never spending its budget has set the target too low. S2 excludes `CANCELLED` and `QUOTA_EXCEEDED` deliberately: a user cancelling a run is not a failure and a quota refusal is the system working correctly, so neither should consume an engineering error budget. Eight alert definitions, each naming the runbook entry it maps to — an alert with no runbook teaches people to ignore alerts. **Recorded as NOT built rather than implied:** nothing pages anyone today, there is no external prober so S1 availability is unmeasured, no on-call rotation or escalation policy, no burn-rate tracking, S4 is uninstrumented, and — the most serious gap — restore has never been rehearsed, so there is no data-loss runbook at all. Filed as **AF-M8-21**. Tests: **13 unit + 6 integration**; no migration. | M8 |
| 2026-09-01 | **AF-M8-06 — Execution retention enforced; partitioning deferred on purpose.** `Execution` and `NodeExecution` are the highest-growth tables in the system and nothing pruned them, so `security.md` §9's "execution IO is customer data … retention is bounded" was untrue and `implementation_plan.md` R8 was unmitigated. **Two stages, not one** (ADR-0016): `input`/`output` are nulled at the plan's `ioRetentionDays`, and the `Execution` row is deleted at `deleteAfterDays` (`NodeExecution` cascades). Collapsing them into a single delete would have forced a choice between holding customer payloads as long as we want cost history, or throwing cost history away as fast as we want to drop payloads; two stages needs neither, because redaction keeps status, timings, tokens, `costUsd`, `model`, and error text — the monitoring and cost dashboards keep reporting truthfully over rows whose payloads are gone, and the privacy clock (7 days on FREE) is far shorter than the analytics one. **Windows live as per-plan data** in `src/lib/retention.ts`, the same shape as `PLAN_QUOTA_LIMITS`, with unknown/NULL collapsing to FREE (ADR-0010 — for retention, "more than FREE" means keeping data longer): FREE 7d/35d, STARTER 30d/90d, PRO 90d/365d, ENTERPRISE 365d/never. **The delete floor is the non-obvious constraint:** the runner meters the monthly execution quota by *counting* `Execution` rows in the current calendar month, so any delete window shorter than a month would remove rows still being counted and silently refund quota — paid capacity given away on a timer. `QUOTA_SAFE_DELETE_FLOOR_DAYS` is 35 and a unit test asserts every plan honours it, so lowering a window below the quota window breaks the build instead of leaking revenue. Enforcement is **bounded** — a page of ids per statement with a per-run ceiling, returning `truncated` and logging a warning rather than taking a long lock on the two largest tables — and **idempotent**: redaction filters on `Prisma.DbNull` (a SQL NULL, distinct from `JsonNull`), so an already-redacted row stops matching and a replayed Inngest step is a no-op. Rows still marked `RUNNING` past the window *are* pruned: no Inngest run lives 35 days, so such a row is a crashed run, and skipping it would leak precisely the runs that never finish. Nightly `sweep-execution-history` at 03:45, deliberately off the AI cache sweep's 03:15. **No archival tier** (a second copy of customer data to secure and delete-on-request, for a retrieval nobody has asked for — the public API already exports history while it is live) and **no partitioning yet**, deferred behind a written trigger: ~50M rows / ~50 GB, repeated `truncated` days, or autovacuum falling behind. Prisma has no partitioned-table support, the PK would become `(id, startedAt)` maintained by raw SQL that `migrate diff` fights, and it needs a partition-creation job whose failure mode is write errors on the busiest table — all for a size this table is nowhere near now that it is bounded. **No migration; policy only.** Tests: **27 unit + 9 integration** against real Postgres (redaction preserves every metric a dashboard reads, deletion cascades to `NodeExecution`, ENTERPRISE never deletes but still redacts, one org's plan never governs another org's rows, org-less legacy rows swept as FREE, second sweep is a no-op, truncate-and-resume, stuck-`RUNNING` pruning). Corrected `data_model.md`, which had pointed retention at `AF-M8-04` — the auth-email task. Consequence filed as **AF-M8-20**: the dashboards do not yet tell a user their history window is plan-dependent. | M8 |
| 2026-09-01 | **AF-M8-08 — Security review + dependency audit; both HIGH findings closed.** Reviewed `security.md` §13 against the code rather than against the spec — a box is ticked only where the control was actually read. **Dependencies: 39 findings (3 CRITICAL, 18 HIGH) → 6, all LOW.** `npm audit fix` closed both CRITICALs (`better-auth` basePath DoS; `handlebars` AST-confusion JS injection — which matters here because ADR-0007 compiles user-supplied templates at runtime) and 11 HIGHs with lockfile-only changes. Four transitive HIGHs whose only npm-suggested fix was a **downgrade** (`prisma` 7.9.1→6.12.0, `inngest-cli` 1.12.1→0.16.3) were closed with scoped `overrides` instead: `deepmerge-ts@^8.0.2`, `adm-zip@^0.6.0`, `postcss@^8.5.26`, `undici@^6.28.0` under `@ai-sdk/provider-utils` (5.x has no fixed release; 6.28.0 is one major rather than two). The `better-auth` fix forced 1.3.26→1.7.2, breaking `@polar-sh/better-auth@1.1.9`, which imports `createAuthEndpoint` from `better-auth/plugins` (moved to `better-auth/api`); pinning back to a still-patched 1.6.30 did not help because the export was already gone, so the Polar chain moved to `@polar-sh/better-auth@1.8.4` + `@polar-sh/sdk@0.47.1`. Residual 6 LOW are one `@ai-sdk/provider-utils` advisory and its dependents, closable only by a major SDK bump (**AF-M8-19**). **Code findings — two controls this document already required that no code implemented.** (1) **SSRF, the HIGH:** `assertSafeEndpoint` validated only the first URL, and all six call sites let `ky`/`fetch` follow redirects, so one `302 Location: http://169.254.169.254/…` from an attacker-controlled host reached cloud metadata. Fixed by `safeFetch` (AF-M8-16, ADR-0015) — walks the chain with `redirect: "manual"`, re-checks every hop, caps at 5, and strips credential headers across origins. Given to `ky` as its `fetch` so no call site's timeout/retry/error semantics changed. (2) **Sentry:** §9 makes `beforeSend` redaction **[HARD]**, but only the browser had it; the server config had none, `sendDefaultPii: true` (attaching `Authorization`/`Cookie`), and `vercelAIIntegration` recording customer prompts and completions; edge had nothing. Fixed by the shared `scrubSentryEvent` across all three runtimes. **Also found:** a real cascading-substitution bug in template instantiation (**AF-M8-15** — the actual cause of what AF-M8-14 had treated as a brittle assertion), and an integration suite that only *appeared* serial (**AF-M8-18** — the races AF-M8-02 recorded as a "pre-existing FK seeding issue"; `npm test` went from an intermittent 7–36 failures to 917/917 twice, and 295s → 157s). **Deliberately left open, not ticked:** DNS-rebinding TOCTOU (**AF-M8-17**), `npm audit` unenforced in CI, production session-cookie flags unverified (no deploy to inspect), backup/restore never rehearsed, no external pentest scheduled. Full suite **917/917 across 95 files**, tsc + biome clean. | M8 |
| 2026-09-01 | **AF-M8-04 — Password reset + email verification shipped with Resend.** Both Better Auth flows now work end-to-end with **Resend** as the email provider (new `resend` dependency; ADR `0014-resend-for-auth-email.md`). Delivery is one server-only module `src/lib/email.ts`: a `sendAuthEmail` helper builds the Resend SDK payload for two templates (verify + reset), never logs the API key / token / password (only recipient + link leave the server), and throws `ResendNotConfiguredError` when `RESEND_API_KEY` is unset so an auth action that NEEDS an email fails loudly rather than silently. `src/lib/auth.ts` wires `emailAndPassword.sendResetPassword` + the `emailVerification` plugin (`sendOnSignUp: true`, `autoSignInAfterVerification: true`; `requireEmailVerification` intentionally unset so existing unverified users are not locked out); Better Auth token storage reuses the existing `Verification` table — **no migration**. Screens (all inside the `(auth)` group): `/forgot-password` (neutral "check your inbox" — never leaks whether an account exists), `/reset-password?token=…`, `/verify-email?token=…`, plus a "Forgot password?" link on login and a "check your inbox" toast on signup. Env: `RESEND_API_KEY` + `RESEND_FROM_EMAIL` are optional at boot (Polar-billing pattern) and added to `src/lib/env.ts` + `.env.example`. Tests: 5 new unit tests (`src/lib/email.test.ts`) — unconfigured throws, envelope + link correctness, `from` override, Resend error surfaced; full unit suite **785/785** green, tsc + biome clean. ⚠️ `next build` on this machine fails globally at "Collecting page data" (`PageNotFoundError` for every route — including untouched `/signup`, `/credentials`, `/icon.svg`), a pre-existing environment/Next-version issue unrelated to this task; `npm run build` could not be verified here. **Correction (2026-09-01):** the `RESEND_FROM_EMAIL` validator shipped here was `z.string().email()`, which rejects the RFC 5322 display-name form `Name <addr@host>` — the exact form in `.env.example` and in this task's own `resendFromEmail` fallback. A correctly configured install therefore threw out of `ensureEnv()` inside `createPrismaClient` and refused to boot, taking down the **entire integration project (95 tests / 10 files)**; the "785/785 green" figure above was the unit project alone. Replaced with an exported `emailSenderSchema` accepting a bare address or a (optionally quoted) display-name form and still rejecting a non-email in either position — 7 unit tests in the new `src/lib/env.test.ts`, full suite **893/893** across 94 files. | M8 |
| 2026-08-31 | **AF-M8-13 — Fixed the flaky template-instantiation test.** `src/features/templates/server/instantiate.test.ts:116` asserted `not.toContain("$node.a")`, which failed nondeterministically whenever a freshly-rotated cuid (from `@paralleldrive/cuid2` in `prepareTemplateGraph`) happened to start with the letter `a` — the rewritten reference `$node.ax…main.value` contains the substring `$node.a`, so the assertion tripped even though the id rewrite was correct (AF-M7-01 code, caught by a full-suite run during AF-M8-02 verification). Replaced the single brittle assertion with two deterministic ones: the serialized config must contain the *mapped* reference `$node.${idMap.get("a")}.main.value`, and must not contain the exact old token `$node.a.main.value` (a cuid is alphanumeric, so it can never collide with the literal `.main.value` suffix). Verified with 15/15 isolated runs and the full default unit suite **780/780** green, biome clean — `npm test` is now flake-free. | M8 |
| 2026-08-31 | **AF-M8-02 — Unified rate limiting across auth, webhooks, public API, and tRPC.** Extracted one shared, injectable module `src/lib/rate-limit/`: the `RateLimitStore` interface (`consume(key, config)` → `{ allowed, retryAfterSeconds, remaining, limit }`) with a `MemoryRateLimitStore` token-bucket default. No new dependency — this is an ADR-worthy decision (ADR `0013-rate-limit-store.md`): the interface is the seam for a future Redis/Postgres-backed distributed store, but that multi-instance store is a documented follow-up, not built here, because the in-memory default fully covers a single-instance deployment and adding a cache dependency now would violate engineering_rules §154. Plan buckets (`PLAN_BUCKETS`, FREE 60/1s … ENTERPRISE 60000/1000s, `resolvePlanBucket` FREE-collapse per ADR-0010) now back the webhook surface; the public-API limiter (`src/features/api-keys/lib/rate-limit.ts`) was refactored to a thin re-export of the shared store, preserving AF-M8-01's exact behavior and unit tests. **Webhook** `POST /api/webhooks/[workflowId]/[path]` dropped its ad-hoc fixed 60/min counter for the plan-aware token bucket (`webhook:{workflowId}:{path}`, bucket from `Organization.plan`, `429` + `Retry-After` unchanged). **Auth** — wrapped the Better Auth catch-all `src/app/api/auth/[...all]/route.ts` with `tryRateLimited` (`src/lib/rate-limit/auth-route.ts`, a `server-only` helper reading `x-forwarded-for`/`cf-connecting-ip`/`x-real-ip`/`x-vercel-forwarded-for` with an `unknown-ip` fallback and cloning the request body so the underlying handler still reads it): sign-in/sign-up `5/15min` keyed `{surface}:{ip}:{email}`, password reset `3/hr` keyed email only (`src/lib/rate-limit/auth.ts`). **tRPC** — a per-user mutation burst cap (60 burst, 1/s refill, keyed `trpc:mutation:{userId}`) merged directly into the existing `protectedProcedure` auth middleware in `src/trpc/init.ts` (deliberately NOT a second `.use()` — chained middlewares broke `workflows.versioning`'s org-membership resolution; a single middleware keyed by `session.user.id` passes all 5 + the 11 `org-isolation` cases). Queries pass through unthrottled. `X-RateLimit-*` headers remain on the public API; adding them to webhook responses is a follow-up. Tests: 9 new unit tests (`src/lib/rate-limit/auth.test.ts` — surface detection + key derivation; the shared store is covered through the AF-M8-01 limiter test via the re-export). Unit green, tsc + biome clean, `next build` clean (with `NODE_OPTIONS=--max-old-space-size=4096`), `workflows.versioning` 5/5 + `org-isolation` 11/11. ⚠️ ~~**Pre-existing, not AF-M8-02:** `public-api`/`api-keys` integration suites fail in their `api_key.organizationId` FK seeding (reproduced at the AF-M8-01 baseline — an environment/DB-state issue, tracked as a follow-up), so the integration total reflects the tRPC suites only.~~ **Misdiagnosis corrected 2026-09-01:** there was no FK-seeding fault. Those suites — and eight others — died in `createPrismaClient` because AF-M8-04's `RESEND_FROM_EMAIL` schema rejected the sender format shipped in `.env.example`. "Reproduced at the AF-M8-01 baseline" was accurate and was pointing at the env schema, not the seed data. Fixed under the AF-M8-04 correction above. Follow-ups (ADR-0013): distributed `RateLimitStore`, `X-RateLimit-*` on webhooks, `Retry-After`-aware client retries. ⚠️ Also filed separately: `src/features/templates/server/instantiate.test.ts:116` is a **pre-existing flaky test** — `not.toContain("$node.a")` fails whenever a freshly-rotated cuid happens to start with the letter `a` (nondeterministic, passes in isolation, AF-M7-01 code, unrelated to AF-M8-02). | M8 |
| 2026-08-31 | **AF-M8-11 — Cost columns converted from `Float` to `Decimal(12,6)`.** The M2 migration shipped `Execution.costUsd` / `NodeExecution.costUsd` / `AiResponseCache.costUsd` as `DOUBLE PRECISION`, and AF-M5-05/07/08 built capture, caching, and reporting on float — against `data_model.md`'s explicit "money is `Decimal(12,6)`, never a float" rule. This converts all three to `Decimal(12,6)` with a single expand-migrate-contract migration `20260831140000_cost_usd_float_to_decimal`: add a `costUsd_new` Decimal column, backfill from the Float, drop + rename, all in one transaction (the tables are small). Because Prisma returns `Decimal` instances (not plain numbers) from now on, every boundary that previously read a raw `costUsd` is fixed: the runner rollups in `src/inngest/functions.ts` (both the failure and success aggregate paths `Number(...)`-convert before the pre-rounded write), the `costs.summary` router (totals / byWorkflow / byModel / topRuns), the `executions.list` and `getOne` routers (shape `costUsd` and each per-node trace to a plain `number`), and the AI cache read in `src/lib/ai/cache.ts`. The two raw-SQL queries that aggregate cost already cast `::double precision` (`ai.cacheStats` `saved`, `costs.summary` daily series) and need no code change. The execution-detail client component now types its trace rows with a local `TraceRow` (plain `costUsd: number`) instead of the Prisma `NodeExecution` type, so no `Decimal` crosses the superjson wire. Unit 712/712, integration 47/47, tsc, biome, and `next build` all green. `data_model.md`'s "as built it is Float" note corrected. | M8 |
| 2026-08-31 | **AF-M8-01 — Public REST v1 + scoped API keys.** The first external surface: a `src/app/api/v1` set of routes authenticated by bearer API keys, and a `ApiKey` model storing only a SHA-256 hash + 8-char support prefix (never the plaintext; ADR `0012-public-api-and-api-keys.md`). Endpoints (list/get workflows, trigger run, get/cancel execution) speak `snake_case` (`serialize.ts` maps explicitly — internal `camelCase` never leaks, dates become ISO, `Decimal` costs become numbers) and return the `{ error: { code, message, details? } }` envelope. Cursor pagination (`?limit&cursor`, base64url `[sortValue,id]`), per-key in-memory token-bucket rate limiting resolved from `Organization.plan` (FREE 60/1s … ENTERPRISE 60000/1000s; plan collapses to FREE on unknown — ADR-0010 pattern), and `Idempotency-Key` dedup on `POST /run` via a `(workflowId, idempotencyKey)` unique index. The key IS the tenant boundary: cross-tenant reads return `404 NOT_FOUND` (never discloses existence, matching the webhook pattern), and auth failures are `401 UNAUTHENTICATED` / missing-scope `403 FORBIDDEN` / exhausted `429 TOO_MANY_REQUESTS` + `Retry-After` + `X-RateLimit-*` headers. Scopes: `workflows:read|execute`, `executions:read|write`. Management is an org-admin tRPC router (`apiKeys.create`/`list`/`revoke`; `create` returns the secret once, mutations audit-log `apiKey.create`/`apiKey.revoke`); management UI deferred (backend-only scope). Migrations `20260831160000_add_api_keys` + `20260831170000_add_execution_idempotency_key` (31 total). Tests: 21 unit (key/rate-limit/cursor) + 41 integration (30 REST-route cases including idempotency, cancel, cross-tenant from both orgs, and 429, + 11 management-router cases incl. VIEWER-forbidden and cross-tenant revoke). Full unit 686 + integration 76 green, tsc, biome, and `next build` clean. `api_contract.md` §1/§5 and `security.md` marked BUILT. | M8 |
| 2026-08-31 | **AF-M7-03 follow-up — Monitoring dashboard UI/UX pass.** Fixes reported from a wide-viewport screenshot where the stat row and the range control ran past the right edge and the chart rendered ~420px tall. **Root cause:** `/monitoring` was the only dashboard page not wrapped in `EntityContainer`, so it had no page padding and no `max-w-screen-xl`; the hand-rolled SVG chart's only size constraint was its viewBox aspect ratio, so its height grew with the viewport width. Now wrapped in `MonitoringContainer` (`EntityContainer` + a `MonitoringHeader` whose range control is the same `Select` the `/costs` header uses, replacing three bare buttons), with the header outside the Suspense boundary so switching windows no longer collapses the page frame. `svg-charts.tsx` is deleted and replaced by `monitoring-charts.tsx` on recharts + the shared `ChartContainer` — the same primitives `/costs` already uses, so no new dependency (engineering_rules: "prefer the existing stack") — giving a fixed `h-[240px]`, real axes with integer-only ticks, gridlines, a legend, and per-day tooltips. **Honesty fixes:** `RUNNING` is now charted rather than dropped, so a day of in-flight runs no longer renders as an empty column; `Avg Duration`/`p50`/`p95` are relabelled `Avg run`/`p50 step`/`p95 step` because the first is the mean of `Execution.durationMs` while the other two are percentiles over `NodeExecution.durationMs` — three identically-labelled cards invited reading them as one distribution; success rate shows the exact counts behind it (`0 succeeded · 2 failed`, summed from the same series the chart draws rather than reconstructed from the rounded percentage) and renders `—` instead of a misleading `0%` for a workspace with no runs; every `—` duration now says why. Error breakdown and top-failing-workflows move from horizontal SVG bars to the share-bar list pattern `cost-breakdown-tables.tsx` established, with workflow names linking to the workflow and a relative last-failure time. The quota meter gains a remaining count, an amber 80% band, and an explicit at-limit warning naming `QUOTA_EXCEEDED`; the stale user-facing "AF-M5-02 cost capture pending" notice is removed (capture shipped in AF-M5-05) in favour of a link to `/costs`. New `src/features/analytics/lib/format.ts` (+11 unit tests) and 9 DOM tests covering the reported state (2 runs, 0%, null durations), in-flight runs, unlimited plans, and the empty workspace. `vitest.setup.ts` gains a `ResizeObserver` stub — jsdom has none and recharts' `ResponsiveContainer` constructs one on mount, so any chart component test threw before its assertions ran. Suite 695/695 across 75 files; `tsc` and `biome` clean on the touched files. **Not visually verified:** `requireAuth()` needs a real session, so the rendered page still wants a human eyeball. | M7 |
| 2026-08-31 | **AF-M7-08 — In-app notification centre.** Migration `20260831180000_notification_model` (additive, guarded, replayable): `NotificationType` enum, the `Notification` table, and `Workflow.notifyOnFailure` (default **true**) / `notifyOnSuccess` (default **false**) — failure is what people need told about, while success on a five-minute cron would write 288 rows a day and make the centre worthless. `Notification` is tenant-scoped rather than per-user, matching executions and approvals; the honest consequence, recorded in `data_model.md` §2.9, is that **read state is shared** across the workspace. `dedupeKey` is `@unique` and every write goes through `writeNotifications` — the single write path — with `skipDuplicates`: Inngest can re-run a step whose memo it lost, `onFailure` can fire alongside a partial tail, and a cron runs again on every redeploy, so keys are composed from the **logical event** (`execution:<id>:<type>`, `credential:<id>:expiring:<expiry ISO>`) and never from a timestamp. That write path also logs-and-swallows its own failures: a notification is a courtesy, and failing a user's run because we could not announce it would be absurd — the swallow is deliberate, narrow, at one place, logs at error level, and is pinned by an integration test. `workflowId` is a real FK with cascade; `executionId`/`credentialId` are deliberately plain columns so a pruned execution leaves the history intact rather than erasing what was announced. **Producers:** execution failure (`onFailure`) and success (its own memoized `step.run`) are live and preference-gated, with TEST-mode runs never notifying; credential expiry is a daily `notifyExpiringCredentials` cron over `oauthExpiresAt` with a +7d window and a 14d grace floor, grouped per org so each workspace's warnings are one write. `/notifications` lists with all/unread filters and pagination; the bell with its unread badge (capped at "99+") sits in the sidebar header — **recorded deviation:** this layout has no top header bar, so the sidebar header is the equivalent region. Both mutations are `updateMany`, so the org scope is in the same statement as the write and a cross-tenant id changes nothing without a read-then-check. Per-workflow toggles sit beside the editor run controls, behind `workflows.updateNotificationPrefs` (editor rung — it changes what the whole workspace is told). **Honest gap:** `APPROVAL_REQUESTED` and `SYSTEM` have builders, copy, icons, and dedupe keys but **no producer**. For approvals the blocker is upstream and bigger than notifications — **nothing in the app creates `ApprovalRequest` rows**; no approval node ships, so the AF-M6-09 approvals dashboard reads a table with no writer. Filed as **AF-M8-13** and recorded in `api_contract.md` as producer-less rather than implied to fire. **Verification:** 29 unit tests (dedupe-key stability and separation, error summarisation including never-empty, day arithmetic floored so a warning never overstates time left, expiry copy across future/singular/zero/lapsed) plus **10 integration tests** against real Postgres — cross-org list/count/markRead/markAllRead isolation, the replayed-write no-op, per-org key independence, and that a rejected write returns 0 instead of throwing at the runner. Full gates: **803 unit across 84 files**, **65 integration across 8 files**, `tsc --noEmit`, `biome check`, `next build` clean. A type error caught at build time was worth keeping: `step.run` results cross Inngest's JSON boundary, so Prisma's `Date` arrives as an ISO string and is rehydrated explicitly — the dedupe key is built from `toISOString()` and `daysBetween` does arithmetic on it. | M7 |
| 2026-08-31 | **AF-M7-07 — Global Cmd+K command palette.** New `src/features/search/` slice. `search.query` (org-viewer) searches workflows by name, executions by id prefix / status / parent workflow name, and credentials by name — each scoped on `ctx.org.id` in the `where` clause (executions reach the org through their workflow) and each `take`-limited. Executions match the `ExecutionStatus` enum, so typing "fail" finds FAILED runs. Credential results carry **type only** — no `preview`, no envelope column: there is no read path for credential secrets and a global search box is exactly where an accidental one would surface. Navigation and action entries carry no tenant data, so they live client-side in `lib/static-commands.ts` and never make the round trip. `lib/fuzzy.ts` is a pure, dependency-free ranker — exact > prefix > word-boundary > substring > subsequence, with consecutive-character and word-boundary bonuses, shorter names winning ties, subtitle matches discounted below title matches, and a stable sort so an empty query preserves the server's recency ordering. `cmdk` supplies arrows/Enter/Escape with `shouldFilter={false}`, since its substring filter would otherwise re-filter away rows the server deliberately returned; `CommandDialog` gained an optional `commandProps` passthrough for that, leaving every existing caller unaffected. The palette queries only while open — a global shortcut must not put a search request behind every page load — and debounces at 150ms. Mounted once in `(dashboard)/layout.tsx`; Cmd+B was already the sidebar toggle, Cmd+K was free. **Verification:** 21 unit tests (12 ranking, 9 static-command — including one asserting every navigation `href` resolves to a real `page.tsx`, so a renamed route fails the build rather than 404ing a user) plus **8 integration tests** in `tests/integration/search-org-isolation.integration.test.ts` where **both orgs own rows with the identical name**, so a passing test cannot be a query that returns nothing; covers each of the three kinds, the empty-query browse case (where org scope is the only filter standing between the caller and the whole table), status and id-prefix lookup, the per-kind limit, and an assertion that no credential envelope field or preview ever serializes. Full gates: **774 unit across 82 files**, **55 integration across 7 files**, `tsc --noEmit`, `biome check`, `next build` all clean. **Recorded deviations:** "settings pages" are absent from the results because no settings route exists yet; "execute" is not offered because the palette is global and has no workflow in context; and the locked "UNION" decision shipped as three org-scoped `findMany`s in one `Promise.all` rather than raw SQL, because the union is over three differently-shaped tables and the SQL would have to erase Prisma's types to line the columns up — the tenancy guarantee is identical either way. | M7 |
| 2026-08-31 | **AF-M7-05 — First-run checklist, sample workflow, and real empty states.** New `src/features/onboarding/` slice, **no schema change**: `onboarding.status` (org-viewer) returns three counts — workflows, credentials, executions — each scoped through `ctx.org.id` in the `where` clause, with executions reaching the org through their workflow. Step completion is **derived** from those counts rather than stored, so a tick cannot claim something that was since deleted, and a user who clears their browser sees an accurate checklist instead of a stale one. `localStorage` (`autoflow.onboarding.v1`) holds only the user's preference and is **keyed per organization** — hiding the card in one workspace must not hide it in another that genuinely is on its first run. Every storage access is wrapped in try/catch: Safari private mode and blocked site data throw on access, and a dismissible hint card must never be the reason a page fails to render; on failure it falls back to showing the card, which is the harmless direction. Visibility is `!dismissed && !allComplete && (noWorkflows || started)` — the `started` flag exists because creating a workflow *satisfies* the "org has no workflows" trigger, so without it the card would vanish the moment the user did the first thing it asked, taking steps 2 and 3 with it; it disappears on its own once all three are done, no dismissal needed. "Create a sample workflow" installs `content-brief-generator` through AF-M7-01's `templates.instantiate` — the same path the gallery's Install button uses, so the sample cannot drift into a second differently-behaved code path — and `state.test.ts` asserts that slug exists in the catalogue, needs zero credentials, and has a manual trigger, so retiring it fails the build rather than the user's first click. Empty states on `/workflows`, `/executions`, `/credentials` rewritten from the design artifacts with real titles and a primary + secondary action; `EmptyView` gained optional `title`/`icon`/`actionLabel`/`secondaryAction` whose defaults reproduce the previous generic card exactly, leaving the knowledge and templates callers untouched. **Recorded deviation:** `screens/onboarding.html` specifies a three-step wizard (name org → create workspace → pick a template), but org and workspace creation already happen automatically at first sign-in (`resolveActiveOrg`, AF-M6-02) — building the wizard would add steps to undo work the product already does. The checklist the acceptance specifies is what shipped. **Verification:** 19 unit tests (`lib/state.test.ts` visibility + derivation, `lib/storage.test.ts` per-org isolation, malformed JSON, wrong shape, non-boolean flags, and storage that throws on both read and write); full suite **745 passed across 79 files**; `tsc --noEmit`, `biome check`, and `next build` clean. | M7 |
| 2026-08-31 | **AF-M7-02 — 20 authored gallery templates + the authoring harness that gates them.** New `src/features/templates/catalog/`: `types.ts` (the `TemplateSpec` format — authored kebab-case node ids, not cuids, since `prepareTemplateGraph` rotates them per install anyway), four domain files (`marketing.ts` 6, `support.ts` 5, `ops.ts` 5, `data.ts` 4), and `index.ts` which projects a spec into its row. `nodeCount` and `credentialCount` are **derived from the graph**, never authored, so the gallery's "fewest credentials" sort cannot drift from what installing actually costs. `harness.ts` is the gate: kebab-case slug + description floor + category-is-a-real-filter-chip; unique ids; registered **and non-deprecated** node types; edges that leave through a port the source node actually declares; leak scanning over every string in `node.data` (cuid2-shaped tokens — length plus a required digit, so prose does not trip it — UUIDs, 16 known secret prefixes, and any authored credential-bound field); model ids resolved against `src/lib/ai/registry.ts` so a typo fails here rather than on the user's first run; then the graph through the **existing testRun planner** (`buildTestGraph` → `buildTestRunPlan`), with `validate()` warnings escalated to failures. **Recorded deviation from the acceptance wording:** the smoke run stops at the plan and does not dispatch to Inngest under `E2E_SERVER=1` — that would need live Slack/Stripe/LLM credentials for someone's account, making it a suite nobody could run; everything knowable without those calls is checked instead, and checked hard. Coverage asserted, not claimed: every node type in `nodePalette` is demonstrated by at least one template; every gallery category has at least one; no template needs more than **one** credential and 9 of 20 need none. Adds the `Ops` category chip (`TEMPLATE_CATEGORIES`), which the acceptance names as a domain but the gallery had no filter for. `scripts/seed-templates.ts` + `npm run seed:templates` upserts the catalogue by slug — dry run by default, `-- --yes` to write, matching the existing migration scripts; it runs the same harness and refuses to write on any issue, never touches `installs` (usage history, not content), and **deactivates rather than deletes** a slug that leaves the catalogue. It needs `tsx --conditions=react-server` so `server-only` resolves to its empty stub — the script legitimately is server code, and this is the same problem the vitest config solves with an alias. **Verification:** 22 unit tests in `src/features/templates/catalog/harness.test.ts`, ten of which prove the harness *rejects* what it claims to; full suite **724 passed across 77 files**; `tsc --noEmit`, `biome check`, and `next build` clean. **Also fixed (unrelated, pre-existing):** `tests/integration/analytics-org-isolation.integration.test.ts` from AF-M7-03 had an unused `beforeAll` import and unsorted imports that failed `npm run lint` on `main`. | M7 |
| 2026-08-31 | **AF-M7-04 — Per-plan execution-count quota enforced in the runner.** Adds `QUOTA_EXCEEDED` to the `ExecutionStatus` enum (additive, replay-safe migration `20260831120000_add_quota_exceeded_status` with a `pg_enum`/`pg_type` guard). Plan is resolved once per org from `Organization.plan` (ADR-0010: unknown/null collapses to FREE, never widens) and mapped to an execution-count limit via `src/lib/quotas.ts` alongside `COUNTABLE_EXECUTION_STATUSES` (`SUCCESS`/`FAILED`/`CANCELLED`/`TIMED_OUT` — running and quota-denied runs never inflate the count), `isMeteredRun` (`mode !== "TEST" && !e2eServer`; missing mode treated metered, conservative) and `quotaBreachMessage`. Two `step.run`s at the top of `executeWorkflow`, before `create-execution`, cover every trigger path (manual/webhook/cron/API all through `sendWorkflowExecution`): `quota-gate` reads the mode from the pre-created Execution row and the current-month countable-`Execution` count scoped by `workflow.organizationId` (UTC first-of-month cutoff); on breach, `fail-quota-exceeded` updates the pre-created row to `QUOTA_EXCEEDED` (or creates one) with `durationMs: 0` and the breach message, then returns without throwing — never entering the retry path or `onFailure`, and a run is never silently dropped. Test runs and `E2E_SERVER === "1"` bypass. UI renders `QUOTA_EXCEEDED` as a red `BanIcon` on both the executions list and detail, with the breach message in the existing error alert (no stack). Tests: 12 unit (`src/lib/quotas.test.ts` — limit resolution, boundary, TEST/E2E bypass, message formatting) + 3 gate-shaped integration cases appended to `tests/integration/org-isolation.integration.test.ts` (per-org independence, denied/TEST rows never counted, previous-month exclusion). Full gates green (696 unit / 41 integration / build). **Deferred sub-items** (documented, not in this PR): Polar customer meter for billing visibility and AI-spend limits once AF-M5-02 capture is authoritative. | M7 | Ships the tenant-agnostic `Template` model (migration `20260831000000_template_model`): slug-unique, open-set `category`/`tags`, `graph` in workflow-graph shape, `featured`/`isActive`, denormalized `nodeCount`/`credentialCount`/`installs`. New `templates` router (4th, in `_app.ts`): `list` and `getOne` are org-viewer reads — category/search/sort (`mostInstalled`/`recent`/`fewestCredentials`) pagination via the shared `PAGINATION` constant, no premium gate; `getOne` additionally returns per-node `nodeSummary` and `pendingCredentials` (manifest `CredentialRequirement[]`). `instantiate` (org-editor): loads the template, rewrites every node id to a fresh cuid (no cross-install collisions), nulls `credentialIdRef` on `data` so installs start from unbound placeholders, validates the copy through the engine `validate()` path (invalid graph raises `BAD_REQUEST`, nothing created), creates a tenant-scoped draft workflow via `saveWorkflow.workflow.save()` + `node.connect()`, increments `installs`, and returns `{ workflowId, nodeCount, pendingCredentials }`. A pre-placeholder run fails with the existing `MissingRequiredCredentialError` (visible, not silent). UI: `/templates` gallery (card grid, category chips, sort, search, prefers-reduced-motion fade-in) + `/templates/[slug]` detail (stats, `Before you install` credential checklist, per-node summary, Install toasts `Installed as a draft` and pushes `/workflows/[workflowId]`), sidebar entry. **Deviations recorded:** deep-planned Fork button and post-install credential dialog not built (pre-install checklist shipped instead); installs land as an unsaved draft, not deployed. Type-fix notes for the next M7 task: on Prisma 7.9.1 `Parameters<typeof prisma.template.findMany>[0]` does not resolve — use `Prisma.TemplateWhereInput` / `Prisma.TemplateOrderByWithRelationInput` from `@/generated/prisma/client`, `as unknown as TemplateGraph` for the `JsonValue` cast, and `n.data ?? {}` for both `validate()` and the `Prisma.InputJsonValue` write. **Verification:** 11 unit tests in `src/features/templates/server/instantiate.test.ts`; full suite **684 passed across 75 files**; `tsc --noEmit` and `biome check` clean; `next build` green. | M7 |
| 2026-08-31 | **AF-M5-09 - Tutorial per-provider AI nodes retired.** `OPENAI` / `ANTHROPIC` / `GEMINI` are marked `deprecated: { since, replacedBy: "AI_LLM", reason }` through the new `NodeDefinition.deprecated` field. They stay registered and executable — deleting a registration makes `nodeRegistry.resolve()` throw and fails graph validation, so a workflow that ran yesterday would break at execution time — but `nodeManifest` now also exports `nodePalette` (non-deprecated only), which `node-selector.tsx` reads, so no new instance can be created. The config panel renders a deprecation notice naming the replacement. `src/nodes/ai/legacy-migration.ts` maps a legacy config onto `AI_LLM` (credential moved to the provider's `*CredentialId` field, prompts and variable name preserved, `_`-prefixed engine keys carried through, undeclared keys reported rather than silently dropped, result validated against `AI_LLM`'s own `configSchema`); `npm run migrate:legacy-ai-nodes` applies it to live `Node` rows, dry-run by default, printing every change. Graph snapshots are deliberately not rewritten — a published version and a trace record what actually was. The hard-coded models (`gpt-4`, `claude-sonnet-4-5`, `gemini-2.0-flash`) are absent from the provider registry and cannot be priced, so each maps to the nearest registered model of its provider (`openai:gpt-4o`, `anthropic:claude-3-5-sonnet`, `google:gemini-1.5-flash`) — a behaviour change, printed per node. Folder deletion is AF-M8-12, blocked on the migration having run everywhere. ADR-0011. | M5 |
| 2026-08-31 | **AF-M5-08 - Cost views: per run, per workflow, per model, over time.** Added `NodeExecution.model` (migration `20260830150000_node_execution_model`): the fallback chain's winning model was already in the run's usage envelope but was dropped at persist time, so per-model attribution was impossible. New `src/features/costs/` slice — `costs.summary` (org:VIEWER, 1-90 day window) returns totals, a zero-filled UTC daily series (`date_trunc` in parameterized `$queryRaw`, which Prisma cannot express), and top-10 `byWorkflow` / `byModel` / `topRuns`, all scoped through `Workflow.organizationId` rather than the nullable `Execution.organizationId`. Pure helpers (`fillDailySeries`, `roundUsd`, `shareOfTotal`, `formatUsd`/`formatTokens`/`formatPercent`/`formatDayLabel`) carry the shaping rules and are unit-tested without a DB. `/costs` page with sidebar entry, nuqs `days` param, server prefetch + Suspense + ErrorBoundary: summary cards (including the AF-M5-07 hit rate), a recharts area chart, and share-of-spend breakdowns linking through to the workflow and the run. Test runs are included and labelled — a test run bills the provider like any other. 18 unit + 3 DOM tests + 5 integration cases. | M5 |
| 2026-08-31 | **AF-M5-07 - Workspace-scoped AI response cache with TTL and hit-rate reporting.** New `AiResponseCache` model (migration `20260830140000_ai_response_cache`, unique on `[organizationId, cacheKey]`) plus a nullable `NodeExecution.cacheHit` that is deliberately three-valued: `null` = the node had no cache configured, so it counts in neither half of a hit rate; `false` = a real miss. `src/lib/ai/cache.ts` — `canonicalize` (depth-sorted keys), `buildAiCacheKey` (sha256 over node type + ordered model chain + compiled prompts + call params, so a prompt/temperature/schema edit misses instead of replaying the old answer), `normalizeCacheTtlSeconds` (0 = off, clamped to 7 days), `readAiCache`/`writeAiCache` (the workspace is part of the lookup, not just the hash; read and write failures degrade to a provider call and are logged, never failing a run), `purgeExpiredAiCache`. Wired through `executeWithFallback`, where a live entry short-circuits the entire chain and reports 0 tokens / $0 because nothing was purchased; opted into per node via `cacheTtlSeconds` on `ai.llm` and `ai.extract`, with `NodeDefinition.supportsResponseCache` marking cacheable types so reporting reads the registry instead of a hard-coded list. `NodeRunParams.organizationId` threaded from the engine. Daily `sweepAiResponseCache` cron; reads already filter on `expiresAt`, so a missed sweep leaves dead rows, never stale answers. New `ai` router: `cacheStats` (hits, misses, rate, `uncachedRuns`, live entries, `savedUsd` = `costUsd x hitCount`) and `clearCache` (org:ADMIN). Execution detail gained a per-node "Cached" badge and a run-level "N of M AI nodes served from cache" line. Also fixed the empty `catch {}` around `estimateRunCostUsd` in `fallback.ts` (engineering_rules section 2). 39 unit tests + 10 integration cases. | M5 |
| 2026-08-30 | **AF-M5-05 - Token and cost capture across node and workflow executions.** Added `WORKFLOW_USAGE_KEY = "__usage"` and `extractStepUsage` in `src/inngest/trace.ts`. Updated `executeWithFallback` in `src/lib/ai/fallback.ts` to compute actual input/output token counts and micro-dollar pricing via `estimateRunCostUsd`. Updated AI executors (`src/nodes/ai/llm/`, `src/nodes/ai/extract/`, `src/nodes/ai/compatible/`) to report token metrics under `__usage`. Updated Inngest runner in `src/inngest/functions.ts` to record `tokensIn`, `tokensOut`, and `costUsd` per node in `NodeExecution` on `trace-end` and roll up total usage to `Execution` via `prisma.nodeExecution.aggregate` on both success and failure paths. 6 new unit tests across trace, fallback, and AI executors. Full suite: **563 total — 546 passed + 17 skipped (60 files)**; tsc clean, biome clean, `next build` clean. | M5 |
| 2026-08-30 | **AF-M5-04 - Fallback chains with served-model trace tracking.** Implemented `src/lib/ai/fallback.ts` providing pure candidate list parsing (`parseModelChain` supporting delimiter-separated fallback strings and deduplication), candidate context resolution (`resolveCandidate`), and error-recovering execution (`executeWithFallback`). Integrated fallback chains into `src/nodes/ai/llm/` (`definition.ts` + `execute.ts`) and `src/nodes/ai/extract/` (`definition.ts` + `execute.ts`) via new `fallbackModels` config field. Execution iterates through candidate models in prioritized order, automatically recovering from provider failures/rate limits, and outputs the exact `provider:model` that successfully served the run. 12 fallback unit tests + 4 new fallback execution tests across LLM and Extract. Full suite: **557 total — 540 passed + 17 skipped (60 files)**; tsc clean, biome clean, `next build` clean. | M5 |
| 2026-08-30 | **AF-M5-03 - `ai.extract` node.** New `src/nodes/ai/extract/` — `definition.ts` (isomorphic `configSchema` + `ExtractData`: `model` string with provider:model resolution, provider credential selects, `content` prompt template with Handlebars expressions, `fields` array of `{ name, type, description }` schema-builder rows, optional raw `jsonSchema` escape hatch string, `variableName`), `execute.ts` (server-only with `import "server-only"`): model resolution via `resolveAiModel`, `createLanguageModel` via `@/lib/ai/provider` with credential extraction from `credentialResolver`, dynamic JSON Schema generation (`buildExtractionSchema` enforcing strict types and required keys, or JSON-parsed `jsonSchema`), prompt template compilation (`compileTemplate`), `generateObject` with AI SDK `jsonSchema()`, `step.ai.wrap` tracing, result saved to `data.variableName` as `{ data, model }`. Editor config schema extended in `src/features/editor/lib/config-schema.ts` and `node-config-panel.tsx` with `fieldList` support for editing structured object arrays with column type mapping. Registered `ai.extract` in `src/nodes/registry.ts`, `manifest.ts`, and `registry.test.ts`. 29 execute tests + 25 config schema tests + 9 DOM config panel tests. Full suite: **541 total — 524 passed + 17 skipped (59 files)**; tsc clean, biome clean, `next build` clean (23 node types registered). | M5 |
| 2026-08-30 | **AF-M5-02 - `ai.llm` node.** New `src/nodes/ai/llm/` — `definition.ts` (isomorphic `configSchema` + `LlmData`: model string, system/user prompts, `temperature` 0–2, `maxTokens`, `jsonMode`, optional `jsonSchema`, per-keyed-provider credential select; declares `providers` capability, reads `NodeCategory.AI`), `execute.ts` (server-only — `import "server-only"` at top, never imported from a client component): splits `"provider:model"` (`splitModelId`), unknown provider → `NonRetriableError` naming the `AI_PROVIDERS` list, model via `resolveAiModel` (exact match → provider default; unknown model on a known provider silently falls back per M5-01 design, only unknown providers throw), prompts compiled through `compileTemplate` against config + current `context.data`, `createLanguageModel` from `@/lib/ai/provider` wires the registry adapters (Groq/DeepSeek/Ollama share the OpenAI adapter over their own base URLs — Ollama unkeyed with `apiKey: "local"`), `generateText` for prose or `generateObject` + `jsonSchema` for JSON mode (invalid configured schema → `NonRetriableError`, not a retry), both wrapped in `step.ai.wrap` so runs are observable, empty-response guard, output stored under `data.variableName` as `{ text, model }`, credentials via `credentialResolver.getCredentialSecrets`. Registered `ai.llm` in `src/nodes/registry.ts` + manifest + `registry.test.ts` folder list. 15 execute tests. Full suite: **493 total — 476 passed + 17 skipped (57 files)**; tsc clean, biome clean, `next build` clean (22 node types registered). | M5 |
| 2026-08-30 | **AF-M5-01 - AI provider registry (data layer).** New isomorphic `src/lib/ai/registry.ts`: 6 provider defs (OpenAI, Anthropic, Google Gemini, Groq, DeepSeek, Ollama) + 12 model defs keyed `provider:model`, each with `adapter` (openai/anthropic/google; Groq/DeepSeek/Ollama reuse the `openai` adapter over their own base URLs declared in `aiProviderDefs`), `contextWindow`, `capabilities`, and static per-1M input/output USD (`estimateRunCostUsd` reads the same tables, rounded to µUSD). `resolveAiModel(provider, model?)`: exact `provider:model` → provider `defaultModel` → `UnknownAiModelError` — ADR-0009 records the deliberate deviation from overview.md §5.4's "first provider's default" leg (unreachable for registered providers; throwing beats silently running another provider's model). `validateAiRegistry()` runs at module load and fails the build on dup ids, provider/credential drift, unknown adapters/capabilities, non-positive context windows, negative prices, and unregistered default models. Credential lockstep (ADR-0008 pattern): `groq.apiKey` + `deepseek.apiKey` defs + write/update schema variants + `/models` connection testers (`isTestable: true`); registry-parity and field-key tests auto-cover them. ADR-0009 added. Suite: **478 total — 461 passed + 17 skipped (56 files)**; tsc clean (0 errors, full `--noEmit`), biome clean on touched files, full `biome check` clean. | M5 |
| 2026-08-29 | **AF-M1-07 - Canvas validation and linting.** The canvas now lints the live draft against the same pure function the server and engine use. `validate(graph, registry)` in `src/engine/validate.ts` is reused at **four** call sites: `workflows.saveGraph` (server boundary), test-run compile (`test-run.ts`), `executeWorkflow` (engine compile), and a new client registry adapter (`src/features/editor/lib/validation.ts`) that resolves node types + config schemas from the isomorphic manifest (never the server-only registry), aliases `INITIAL` → `MANUAL_TRIGGER`, and normalizes defaulted edge handles (`|| "main"`) to match the server — so client, save, and engine report identical issues. Live lint flows through a derived `validationResultAtom` over `nodesAtom`/`edgesAtom`: offending nodes render an error badge with a hoverable reason list (`node-validation-badge.tsx`); a collapsible top-left summary panel lists every problem (no trigger, cycles, unreachable nodes, unconnected required inputs, invalid configs) and focuses the node on click (`validation-panel.tsx`). Checks: triggers, cycles, unknown types, config `safeParse`, unconnected required inputs, disconnected nodes. This closes AF-M1-06's last open AC (field-level validation from the same schema used server-side). Tests: 11 new/extended unit tests across `validate.test.ts` + `validation.test.ts`; also fixed a pre-existing build blocker in the uncommitted M1-06 tree (`entity-components.tsx` `EntityItemProps.title: React.ReactNode`). Suite 405 collected — 388 passed + 17 skipped; lint + `next build --turbopack` green. Branch `af-m1-07-canvas-validation` stacked on `af-m1-06-schema-driven-config-panel`; both remain local — GitHub pushes blocked on network (see AF-M1-06 row). | M1 |
| 2026-08-28 | **AF-M1-06 - Schema-driven node config panel (partial, do not close).** Selecting a node opens a config panel rendering a form generated from that node's Zod `configSchema` (`src/features/editor/lib/config-schema.ts`): supported kinds string / number / boolean / z.enum(select) / multiline / credential-reference / kv-list (`z.record` arrays). Anything unsupported throws `UnsupportedConfigFieldError`, which renders a loud error box instead of a broken control. Rename node, notes, and enable/disable patch the node into `nodesAtom` and flow through the existing dirty/autosave cycle from AF-M1-04. Internals that mattered: panel form is remount-keyed by `node.id` so switching selection can never show stale list rows; kv-list rows carry stable ids (counter-ref) for React keys and exact `aria-label`s (`Headers key 1`, `Remove Headers row 1`); enum fields show a "Select…" placeholder only when the saved value is not an enum member. Deliberately not built (written down, not silently scoped): field-level validation from the same schema (queues into AF-M1-07 canvas linting), expression editor, JSON textarea, and any runtime honouring of `node.disabled` (engine skip lands in M2). 35 new tests → **394 collected, 377 passed, 17 skipped**; lint + `next build --turbopack` green. Branch `af-m1-06-schema-driven-config-panel` stacked on `chore/prisma7-dev-fixes-and-docs` — push/un-pushed and PR body staged; M1 status updated (no longer closed). **Closed 2026-08-29 by AF-M1-07** (canvas linting delivered the queued field-level validation from the same schema). | M1 |
| 2026-08-28 | **AF-M3-06 - Eight connectors (stage 1 completed).** All eight connector nodes now ship definition + execute + unit tests + palette metadata + `docs/nodes/<name>.md`: Webhook-out `97f95ac`, Email/SMTP `3776de1`, Postgres `9cf42fc` (parameterized `$1`/`$2` with bound values — string-concatenated SQL is a rejection, enforced and tested; node doc forbids it), Google Sheets `eca50ce`, Airtable `3d64c8c`, HubSpot `eedbd73`, OpenAI-compatible chat `b311262`, and Slack send-message tests + doc `9a475d9`. Palette logos + save-status polish in `4e8b364`. The stage-1 "held commit" caveat is moot — the versioning `graphSnapshot` type error that blocked `next build` was fixed in `28764f4` and the build is green (infra row, `04b9a35`). Two follow-ups carried forward: Slack sends via incoming webhook (`slack.oauth2` credential type registered but unused — API/token path out of scope) and `webhookUrl` does not compile `{{...}}` templates (only `content` does). Suite 359 collected (342 passed, 17 skipped). | M3 |
| 2026-08-28 | **AF-M4-02 - Version history UI (completes the AF-M4-01 backend).** `56cbb86` adds `VersionHistorySheet` to the editor header (`editor-header.tsx`): revision list with a node-level diff summary, Deactivate, and one-click "Rollback to vN" via the publish/activate/deactivate procedures from AF-M4-01; `Execution` rows may pin a `workflowVersionId`. The versioning integration tests pass and the build is green — the last 5 `noExplicitAny` warnings in the diff/rollback UI were fixed in `28764f4`, resolving the blocker the AF-M3-06 stage-1 row below warned about. | M4 |
| 2026-08-28 | **Vercel production build green on `main` (04b9a35).** Root-caused the `main` deploy failure — 17 Turbopack `Module not found: Can't resolve '@/generated/prisma/{browser,client}'` — to the generated Prisma client being absent on the build machine: `/src/generated/prisma` is gitignored, and the build ran `next build` with no generation step. Fixed by adding `"postinstall": "prisma generate"` to `package.json` (Vercel runs `npm install` → postinstall → client emitted to `./src/generated/prisma` before `next build`). Also pinned `turbopack.root: __dirname` in `next.config.ts` so a stray `package-lock.json` in the user home dir can't confuse Turbopack's root detection and cause an all-routes `PageNotFoundError: Cannot find module for page` at page-data collection (a local-only artifact, not the Vercel cause). Verified end-to-end: local build compiles and generates all 15 static pages; Vercel `main` (04b9a35) build now succeeds. Remaining (non-blocking, pre-existing) Vercel warnings: npm ERESOLVE peer overrides for `@polar-sh/*` (React 18 peers vs app React 19), npm allow-scripts list for 8 packages incl. `@prisma/engines`/`@sentry/cli`/`sharp`, and no `SENTRY_AUTH_TOKEN` (source-map upload skipped). | infra |
| 2026-08-28 | **AF-M4-01 - Workflow versioning (backend).** Added WorkflowVersion model to schema, linking draft states to published revisions via graphSnapshot. Workflow model gains activeVersionId. Router updated with publish (creates version + activates by default), activate (rolls forward/back to specific version), deactivate (removes active version), and getVersions (lists history). Executions can now optionally reference a specific workflowVersionId. 5 integration tests added verifying publish defaults, manual overrides, and version incrementing against a real database. Schema synced, tests pass. | M4 |
| 2026-08-28 | **AF-M3-06 - Eight connectors (stage 1: OpenAI-compatible node + Slack tests/doc).** New `OPENAI_COMPATIBLE_CHAT` ACTION node (`src/nodes/ai/compatible/`): zod config schema (SSRF-guarded `baseUrl` template, `model`, system/user prompts, `variableName`); server-only `execute` reads the credential once via the single-decrypt path (`resolveNodeCredentials`, requires `openaiCompatible.apiKey`), templates prompts + base URL, `assertSafeEndpoint`-guards the rendered endpoint, POSTs `/chat/completions` via `ky` (`throwHttpErrors: false`; non-2xx plus any parsed `error.message` → `NonRetriableError`, capped body read), and stores `{ id, model, text, usage: { promptTokens, completionTokens } }` under `variableName`; realtime status via the `openAiCompatibleChatChannel`; wired into registry, manifest, registry.test, the palette (`node-components` + `node-selector`) and the editor node-status icon map; definition + execute unit tests; `docs/nodes/openai-compatible-chat.md`. Slack send-message completed to the stage-1 contract with `definition.test.ts` + `execute.test.ts` and `docs/nodes/slack-send-message.md`. Suite now 359 collected (342 passed, 17 skipped). Connector commit **held** pending tree green: the AF-M4-01 versioning WIP (`version-history.tsx` reads `v.graphSnapshot` that the router's version rows do not return) still fails `next build`. Slack caveats surfaced: node posts via an **incoming webhook** (`slack.oauth2` credential type exists but is unused — API/token path out of stage scope), and `webhookUrl` itself does not compile `{{...}}` templates today (only `content` does). | M3 |
| 2026-08-28 | **AF-M3-06 - Eight connectors (stage 0: credential types + testers).** Extended the credential vault registry with five new typed defs — `postgres` (host/port/database/username/password/ssl), `smtp` (host/port/username/password/tls), `airtable.apiKey`, `hubspot.apiKey`, `openaiCompatible.apiKey` — each with matching write + mirror update variants in the `.strict()` public schema (variants auto-covered by the registry parity + field-key equality tests). Server-side connection testers added to `credential-registry.ts` for Postgres (`pg` client, `connectionTimeoutMillis` + optional `ssl: { rejectUnauthorized: false }` on "require"), SMTP (`nodemailer` `verify()`, `ssl`/`starttls` TLS modes), Airtable (`GET /v0/meta/whoami`) and HubSpot (`GET /crm/v3/objects/contacts?limit=1`); all bounded by a 10s `runWithTimeout` (rejection-guarded race that returns `timeout` instead of leaking a rejection) and classifying auth (28P01/28000, EAUTH/EENVELOPE), timeout, and connection failures. `openaiCompatible.apiKey` is intentionally `isTestable: false` (any OpenAI-compatible server may not expose a stable capability endpoint). Testers are `total`-semantics — never throw, five-way outcome (`ok | AUTH | CONNECTION | TIMEOUT | NOT_TESTABLE`); no plaintext at any boundary; `pg` + `nodemailer` (+ `@types/*`) added to package.json. 5 test/registry assertions extended additively → **252 total collected**; `npm run test` 237 passed + 12 skipped; Biome clean on touched files; full `next build --turbopack` passes (page-data collection succeeded after clearing a stale `.next` from an aborted run; the earlier all-routes `PageNotFoundError` was corrupt build cache, not a code regression). The eight connector nodes (Slack/SMTP/Sheets/Postgres/Airtable/HubSpot/OpenAI-compatible/Webhook-out) are next. | M3 |
| 2026-08-27 | **AF-M3-03 - Credentials UI.** Upgraded the credentials CRUD UI to support type-driven forms with masked inputs, partial updates, and usage warnings. Server-side: added `credentialUpdateBody` for partial updates (omitted secret fields preserve the encrypted envelope) and `getUsage` to list workflows referencing a credential ID. Client-side: created `SecretInput` with Eye/EyeOff visibility toggle, added `useTestCredential` (with status badge UI) and `useCredentialUsage` hooks. When deleting an in-use credential, `DeleteCredentialDialog` now warns the user and lists affected workflows (with links). Form inputs default to placeholder •••••••• in edit mode for secrets. `isTestable` metadata added to `openai.apiKey`, `anthropic.apiKey`, `gemini.apiKey`. | M3 |
| 2026-08-27 | **AF-M3-04 - Credential injection into execution.** `NodeDefinition.credentials: CredentialRequirement[]` added to node types (`{ key, type, required }`); AI nodes (OpenAI/Anthropic/Gemini) declare `{ key: "credentialId", type: "<provider>.apiKey", required: true }`. New `src/features/executions/server/credential-resolver.ts` — **the single decrypt site** for node runs: `resolveNodeCredentials()` is injected a `loadCredentialRow` loader (tenant-scoped `prisma.credential.findUnique`), returns `Record<string, CredentialSecret>`, throws `MissingRequiredCredentialError` on missing required, skips optional. Engine (`src/inngest/functions.ts`) calls `resolveNodeCredentials` in a dedicated Inngest step `resolve-credentials:<nodeId>` and threads the result into `execute()` via `NodeRunParams.credentials`. The credential map is **never** merged into `context`/`output`/trace — plaintext has no path to `NodeExecution.input/output`. Executors (`openai`/`anthropic`/`gemini`) dropped their own `prisma.credential.findUnique` + `openSecret`; they now read `credentials?.credentialId` and throw `NonRetriableError` if absent. 13 new tests: resolver round-trip (loader called once), missing-required throws, optional skipped, multi-requirement order, leak guard (decrypted secret absent from node data/output), registry-parity (each declared type exists in `credentialRegistry`), AI-def requirement assertions → **250 total**. tsc + lint clean. | M3 |
| 2026-08-27 | **AF-M3-02 - Credential model + registry + API.** `Credential` model rewritten (migration `20260827170000_credential_vault_af_m3_02`): the Cryptr `value` string and the `CredentialType` enum are gone; storage is the sealed AEAD envelope (ADR-0004) as `ciphertext/iv/authTag/wrappedDek` `Bytes` + `keyVersion Int @default(1)`, plus `preview`, `oauthExpiresAt`, `lastUsedAt`, `@@index([type])` and `@@index([oauthExpiresAt])`; tenancy stays `userId` until the M-org migration. Legacy rows convert via `npm run migrate:credentials` (dry-run by default; raw-SQL column writes; valid on fresh DBs too) BEFORE `migrate deploy`. Typed registry mirroring the node SDK: isomorphic `src/features/credentials/credential-types.ts` (kind union `apiKey\|bearer\|basic\|header\|oauth2`, 8 registered defs incl. migrated `openai.apiKey`/`anthropic.apiKey`/`gemini.apiKey`, `secretFromInput`/`maskSecretValue`/`computePreview`; provider testers server-only in `credential-registry.ts`: OpenAPI/Anthropic `/v1/models` + Gemini `/v1beta/models` with 10s timeout). API rewritten to `create` (premium) · `update` · `remove` · `getOne` · `list` (paginated + search + type filter) · `test` (server-side probe → `AUTH\|CONNECTION\|TIMEOUT\|NOT_TESTABLE`); every credential-returning procedure is `.output()`-validated against a strict `CredentialPublic` schema (metadata + `preview` + `usageCount` from `_count.Node`) so no secret field can ever serialize. Decrypt sites: node executors (`openSecret`, `src/features/credentials/server/vault.ts` — row-shaped, runtime byte-guards for Inngest serialization) + server-side `test`. Client updated: hooks (`useCredentialsByType(type)` uses `list` with `pageSize=MAX_PAGE_SIZE`), registry-driven create/edit form, list page logos/preview/usage counts, OpenAI/Anthropic/Gemini dialogs + executors use the vault. `ENCRYPTION_KEY` demoted to optional (converter only); `src/lib/encryption.ts` frozen as the legacy path. 42 new unit tests (registry validation, write-schema↔registry parity, vault round-trip/tamper, credentials-security no-plaintext) → **225 total**. ADR-0008; tasks/progress/api_contract/data_model/security/env docs updated. tsc + lint clean. | M3 |
| 2026-08-30 | **M-KB — Knowledge Base (pgvector) complete.** Implemented `AF-KB-01` through `AF-KB-05`: PostgreSQL pgvector integration (migration `20260829120000_knowledge_base_pgvector` with HNSW vector index); recursive text chunker (`chunkText`); multi-format text extractor (`PDFParse` for PDF, `mammoth` for DOCX, HTML parser with entity decoding, TXT, MD); SSRF-safe URL crawler; OpenAI `text-embedding-3-small` embedding generator with credential vault integration; durable Inngest background ingestion pipeline (`processKnowledgeSource` + `scheduledKnowledgeSync`); `AI_RETRIEVE` node with top-k vector cosine similarity search and formatted context/citations injection; full Knowledge Base dashboard (`/knowledge`) with document upload dialog, chunk inspector, and test retrieval console. 448 unit tests passing, tsc and lint clean. | M-KB |
| 2026-08-27 | **AF-M3-01 - Credential crypto module.** New `src/lib/crypto.ts`: envelope encryption (AES-256-GCM). Each value is encrypted under a fresh random 32-byte DEK; that DEK is wrapped by the KEK derived from `CREDENTIAL_MASTER_KEY` (32 bytes, base64 — validated by `assertCredentialMasterKey`, called at boot from `src/instrumentation.ts` so a missing/invalid key refuses to start; `SKIP_ENV_VALIDATION=1` bypasses). Stored shape per security.md §3 stays five fields: `{ v: keyVersion, iv, ct: ciphertext, tag: authTag, dek }` — the DEK wrap is encoded as an `iv(12)|tag(16)|ct(32)` frame inside `dek` so no extra columns are needed. `CURRENT_KEY_VERSION = 1`; `decryptCredential` accepts an optional `CredentialKeyRing` (`Map<version, KEK>`) so N-1 generations remain decryptable during rotation, and `rewrapCredential` re-wraps a DEK without touching the payload — the rolling rotation path. Tampered ciphertext/authTag, wrong key, malformed wrap frame, and unknown keyVersion all throw with generic non-oracle messages. Env: `CREDENTIAL_MASTER_KEY` added as a **required** var to `src/lib/env.ts`, `.env` (generated dev key) and `.env.example`; operator docs updated (§1, §4.1, §5.2, §8.2, §11 of `environment_setup.md`, plus `local_setup_guide.md`). The legacy Cryptr path (`ENCRYPTION_KEY`, `src/lib/encryption.ts`) still powers the three AI node executors and is replaced in AF-M3-02 when the `Credential` model moves to the envelope. 16 unit tests: round-trip (incl. unicode/multiline), envelope never contains plaintext, tamper×2, malformed frame, wrong key, generic-message, unknown version, rotation re-wrap (ct untouched + N-1 decrypt), missing-key-source, 4 master-key validation cases. 183 total tests, tsc + lint clean. | M3 |
| 2026-08-27 | **AF-M2-08 - In-editor test runs.** `workflows.testRun` runs the current canvas draft (unsaved) via an inline `graphSnapshot` in the inngest event - the engine never touches the persisted workflow for test runs. Whole-draft run ('Test workflow' button) executes every reachable node; single-node run ('Test node' button on a selected node) skips every topological predecessor (`skipNodes` + shared `computeSkipNodes`, fixing retry-from-node which previously sent skipNodes the engine ignored) and stops exactly after the target (`endAfterNodeId`), writing SKIPPED rows for the rest. Draft validated at the boundary (zod + `validate()`); plan buildable + unit-tested in `src/features/workflows/server/test-run.ts` (8 tests: linear, trigger-target, diamond isolation, missing target, invalid draft). Test runs recorded with `mode: TEST`; `executions.list` now filters them out by default and supports an explicit mode filter. Editor bottom panel: Test workflow (always) + Test node (enabled on single selection), buttons read atoms lazily to avoid drag re-renders; on success the client navigates to the execution detail page, which already paints per-node status onto the canvas via realtime and shows inline output/errors. TEST badge on the execution detail header. 167 total tests, tsc + lint clean. | M2 |
| 2026-09-02 | **Polar checkout success redirect fixed at the root.** `POLAR_SUCCESS_URL` (and `NEXT_PUBLIC_POLAR_SUCCESS_URL`) removed entirely — the checkout `successUrl` in `src/lib/auth.ts` is now a **relative path** (`/workflows/billing/success`) that the Polar better-auth plugin resolves against the request's own host (`new URL(successUrl, ctx.request.url)`). Fixes the production bug where a stale absolute `http://localhost:3000/workflows` value was baked into Polar's hosted checkout and redirected paying customers to localhost. Also dashboard-independent env handling: committed non-secret `.env.production` (public vars Vercel loads at build — `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_POLAR_PRODUCT_SLUG`) and a `vercel.json`; secrets stay in the dashboard. Docs corrected across `tasks.md`, `progress.md`, `polar_setup.md`, `environment_setup.md`, `local_setup_guide.md`, `engineering_rules.md`, `implementation_plan.md`, `.env.example`, `.gitignore`. | M0 |
| 2026-08-26 | **AF-M2-07 - Executions UI.** List page upgraded: trigger icon + label (MANUAL/WEBHOOK/SCHEDULE/API), workflow name, duration (ms/s formatted), cost ($), status filter dropdown (All/Running/Success/Failed/Cancelled/Timed out via nuqs params), auto-refresh every 3s via `refetchInterval` while any execution is RUNNING. Detail page: metadata grid with trigger, duration, cost, tokens; Cancel button (RUNNING only, invalidates query on success); Retry dropdown (FAILED/TIMED_OUT/CANCELLED, navigates to new execution); node traces: per-node status icons including SKIPPED (BanIcon), nodeName + nodeType display, skip reason italicized, per-node duration + cost, collapsible `JsonViewer` with search highlight + copy-to-clipboard + truncation marker (128 KB); error panel per node with attempt count + timestamp; workflow-level input/output viewers; error + collapsible stack trace. 159 total tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M2-06 - Executions API.** `executions.list` (paginated, filters: workflowId, status, startedAfter, startedBefore; excludes large IO columns to keep payload small), `executions.getOne` (full run + ordered node traces by order), `executions.cancel` (validates RUNNING status, returns NOT_FOUND for cross-tenant), `executions.retry` (FAILED/TIMED_OUT/CANCELLED only; creates new execution + re-emits inngest event), `executions.retryFromNode` (BFS from triggers via `computeSkipNodes` helper to determine upstream skip list; creates new execution + emits with `skipNodes`). `workflows.run` creates Execution record first then emits inngest event (new flow; old `execute` kept for backward compat). `sendWorkflowExecution` now returns `{ eventId }` for record linking. Inngest function updated to check for pre-created execution via `event.data.executionId`. `saveWorkflowInputSchema` updated with 5 new node types (SET, CONDITION, MERGE, WEBHOOK_TRIGGER, SCHEDULE_TRIGGER). 8 new unit tests for helpers (`computeSkipNodes`: trigger, linear, diamond, null snapshot, missing target; `retryableStatuses` membership). 159 total tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M2-05 - First 7 node executors.** SET node: dot-path field mapping with Handlebars expression resolution (supports nested paths like `user.name`). CONDITION node: operator-based evaluation (`equals`, `contains`, `gt`, `lt`, etc.) with `_outputPort` set to `true` or `false` for branch-taken routing (consumed by AF-M2-04 `markTakenEdges`). MERGE node: three modes - `append` (flattens arrays into `items`), `mergeByKey` (recursive deep-merge), `combine` (wraps under a configurable key). HTTP_REQUEST node enhanced: `headers` (template-resolved key-value), `queryParams` (template-resolved key-value appended to URL), `failOnNon2xx` flag (throws `NonRetriableError` for non-2xx responses); Content-Type only set if not already provided via headers. WEBHOOK_TRIGGER + SCHEDULE_TRIGGER: no-op passthrough stubs pending M4. Manual trigger already existed (AF-M1-01). All 5 new nodes registered in `registry.ts` and `manifest.ts` (14 total). 19 new definition tests across 5 new node test files. 156 total tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M2-04 - Runner upgrades.** Engine loop rewritten with branch-taken semantics: `buildExecutionPlan()` extracts per-node config (`timeoutMs`, `retry`, `continueOnFail`) from node data + node definition defaults. `buildGraphMaps()` constructs adjacency + incoming maps from connections. `computeSkippableNodes()` BFS from triggers via taken edges determines reachability; nodes not reachable are SKIPPED with reason before execution. `markTakenEdges()` marks outgoing edges based on `_outputPort` (branching nodes) or all edges (non-branching). Per-node timeout via `Promise.race` (default 60s). Per-node retry with exponential backoff (manual retry loop inside `step.run` for Inngest memoization compatibility). `continueOnFail` marks outgoing edges taken on failure + continues run. Concurrency keys: per-workflow (limit 1, serializes same-workflow runs) and per-tenant (limit 10). Branch-taken skip traces written in a single batch step after the main loop. 8 new tests: `buildGraphMaps`, `computeSkippableNodes` (4 cases: all taken, partial taken, branching, triggers-only), `markTakenEdges` (3 cases). 137 total tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M2-03 — Expression context helpers (Handlebars).** `buildTemplateContext(accumulatedContext, nodeOutputs, meta)` enriches the template context with `$json` (alias for accumulated context), `$node["Name"]` (per-node output map), `$execution.id`, `$workflow.id`, `$now` (ISO-8601). Engine loop in `functions.ts` now tracks `nodeOutputs[nodeName] = executorReturn` alongside the flat `context` accumulator, then builds the enriched context before each executor runs. `ExpressionError` class exported from `template.ts` for future strict-resolution use (Phase 1 keeps Handlebars default: missing paths → `""`). Access via `lookup $node "Name"` or `{{$node.[Name]}}` syntax. Prototype-access sandbox unchanged — `$`-prefixed keys inherit the same `allowProtoPropertiesByDefault: false` guard. `$env` and `$items` intentionally deferred. ADR-0007 amended with full context table; `execution_engine.md` §5 updated with status column. 17 new tests: `buildTemplateContext` shape, `$json`, `$node` (via `lookup` + dot-bracket), `$execution`, `$workflow`, `$now`, missing node → `""`, flat + `$`-prefixed coexistence, `json` helper with `$node`, `ExpressionError` class. 129 total tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M2-02 — Shared graph validator (compile-lite).** New `src/engine/validate.ts`: isomorphic pure function `validate(graph, registry?)` → `{ errors, order }`. Checks: cycles (toposort catch), missing/multiple triggers (naming convention), unknown node types (registry lookup), invalid configs (safeParse), unconnected required inputs (PortDef.required), disconnected nodes (BFS from triggers, warning). Deterministic topological order with alphabetical tie-breaking; empty for cyclic graphs. Wired into `executeWorkflow` (replaces inline `topologicalSort` — now validates before running; throws `NonRetriableError` on critical errors) and `workflows.saveGraph` router (rejects with `BAD_REQUEST` before the write transaction). 14 unit tests covering linear, branching, diamond, disconnected, cyclic, single-node, empty, trigger checks, unknown types (mock registry), and ordering determinism. 112 total tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M2-01 — Execution data model upgrade.** Additive migration `20260826160000_execution_model_upgrade`: `Execution` gains `trigger` (MANUAL/WEBHOOK/SCHEDULE/API/SUBWORKFLOW), `mode` (PRODUCTION/TEST), `graphSnapshot Json` (immutable graph snapshot for P3), `input Json?`, `durationMs`, `nodeCount`, `tokensIn`, `tokensOut`, `costUsd`; indices on `(workflowId, startedAt DESC)` and `(status)`. `ExecutionStatus` enum extended with `CANCELLED` and `TIMED_OUT`. `NodeExecution` gains `nodeName`, `typeVersion`, `input Json?`, `output Json?`, `skipReason`, `tokensIn`, `tokensOut`, `costUsd`; indices on `(executionId, startedAt DESC)` and `(status)`. Engine (`functions.ts`) updated: execution creation snapshots the workflow graph; node trace rows include `nodeName`; finalize step computes `durationMs` and `nodeCount`; `onFailure` handler computes `durationMs`. `TraceNode` type gains `name`; `NodeExecutionRow` gains `nodeName` and `skipReason`. Execution UI switch statements handle CANCELLED/TIMED_OUT with distinct icons. 98 tests, tsc + lint clean. | M2 |
| 2026-08-26 | **AF-M1-04 — Editor persistence + autosave.** Nodes/edges lifted to Jotai atoms (`nodesAtom`, `edgesAtom`) observable across header and editor. `ServerSnapshotRef` tracks last-loaded/saved graph state; dirty detection via deep comparison. Debounced autosave (1.5s idle via `useDebounce`). Save states: saved / saving / unsaved / save-failed exposed via `saveStatusAtom`. `beforeunload` warning when dirty. CONFLICT auto-reloads latest version by invalidating the workflow query (Editor's `useEffect` rebuilds the snapshot from refetched data). `NodeSelector` rewritten to use atoms directly (`useSetAtom(nodesAtom)`) instead of `useReactFlow().setNodes()`, removing the hidden state split. `useSaveWorkflow` hook now sets `saveStatusAtom` on success/error and invalidates the specific workflow query (not just `getMany`). 97 tests, tsc + lint clean. | M1 |
| 2026-08-26 | **AF-M1-03 — saveGraph mutation with optimistic concurrency.** `workflows.update` replaced by `workflows.saveGraph`: input schema gains `revision: z.number().int().min(0)`; router checks `workflow.revision !== revision` → `CONFLICT`; transaction deletes all nodes/edges then recreates valid set (orphaned edges silently dropped via node-ID intersection); `Workflow.revision` incremented on success; returns canonical saved graph (`{ id, name, revision, nodes[], edges[] }`). `getOne` now returns `revision`. Client hook renamed `useUpdateWorkflow` → `useSaveWorkflow`; `EditorSaveButton` reads `workflow.revision` from suspense query and passes it on save; CONFLICT errors surface a "please reload" toast. Migration `20260826145957_add_workflow_revision`. 3 new schema tests (revision field validation). 97 total tests, tsc + lint clean. | M1 |
| 2026-08-26 | **AF-M1-02 — Drop NodeType enum.** Prisma migration `20260826143713_drop_node_type_enum`: converts `Node.type` and `NodeExecution.nodeType` from Postgres enum to `TEXT`, migrates `INITIAL` → `MANUAL_TRIGGER` in both tables, drops the `NodeType` enum type. Adds `typeVersion Int @default(1)`, `disabled Boolean @default(false)`, `notes String?` to Node. All 5 source files updated. | M1 | `20260826143713_drop_node_type_enum`: converts `Node.type` and `NodeExecution.nodeType` from Postgres enum to `TEXT`, migrates `INITIAL` → `MANUAL_TRIGGER` in both tables, drops the `NodeType` enum type. Adds `typeVersion Int @default(1)`, `disabled Boolean @default(false)`, `notes String?` to Node. Schema updated; Prisma client regenerated. All 5 source files referencing `NodeType` enum updated: `config/node-components.ts` (string-literal keys, no Prisma import), `components/node-selector.tsx` (string literals), `features/editor/components/editor.tsx` (string comparison), `features/workflows/server/routers.ts` (string literals, registry import removed — Zod discriminated union handles unknown-type rejection), `inngest/functions.ts` (removed `type NodeType` import and `as NodeType` casts). S5 defect note updated — NodeType Postgres enum defect (D7) now resolved. | M1 |
| 2026-08-26 | **AF-M1-01 — Node SDK registry.** New `src/nodes/` tree: `types.ts` (isomorphic `NodeDefinition`/`NodeRegistration`/`NodeRun`/`NodeRunParams`/`WorkflowContext`/`NodeExecutionError`), `registry.ts` (server-only; validates + stores registrations with duplicate/malformed-definition errors), `manifest.ts` (client-safe definitions-only array). Flat `NodeRegistration` interface extends `NodeDefinition` with `execute` method (bivariant for TS param variance). Nine executor modules migrated: `core/manual-trigger`, `forms/google-form`, `payments/stripe-trigger`, `http/request`, `ai/openai|anthropic|gemini`, `discord/send-message`, `slack/send-message`. Old executor layer deleted: `features/executions/lib/executor-registry.ts`, `features/executions/types.ts`, all 9 `features/**/executor.ts` files. `features/workflows/schemas.ts` rewritten to derive union from registry. `inngest/functions.ts` rewired to use `getNodeRegistration()`. `lib/db.ts` converted to lazy Proxy pattern — `ensureEnv()` no longer called at module-evaluation time, fixing vitest `server-only` import failures (test stub alias in `vitest.config.ts`). 22 new tests in `registry.test.ts`: factory validation, alias resolution, manifest↔registry consistency, client-reachability assertions. 94 total unit/dom tests, tsc clean, lint clean. | M1 |
| 2026-08-26 | **Phase A UX wins.** Landing page at `/` (AF-M7-06): session-aware CTAs, capability grid mirroring verified features only, real root metadata replacing create-next-app leftovers. Polar success page `/workflows/billing/success` (AF-M0-10): renders inside app shell, invalidates the `["subscription"]` cache so the sidebar flips without reload; `.env.example` duplicate `POLAR_SUCCESS_URL` removed and default retargeted; `polar_setup.md` §4 updated. AGENTS.md §6: client components must import Prisma enums/types from `@/generated/prisma/browser`. | M0 / M7 |
| 2026-08-26 | **Deep-plan refresh** (`tasks.md`/`implementation_plan.md`): internal-demo horizon; decisions register (engine extended incrementally, Handlebars kept as the one template system, tenancy deferred to pre-public-beta, KB planned in as new **M-KB** milestone); M2 re-scoped 4w→3w against verified pre-built engine work. **Integration test harness shipped:** `integration` vitest project vs Docker postgres:16 (:5433) with per-suite `migrate deploy` + truncation, setup hard-pins `DATABASE_URL` to the test DB before imports (dev/prod cannot be truncated by tests). First suite: 12 webhook-authz route tests (AF-A-01 acceptance) — found & fixed unsigned Stripe request returning 500 instead of 400. Docs: `testing_strategy.md` §6 contract + local recipe (incl. Windows `127.0.0.1`-not-`localhost` wslrelay trap), stale §8 rewritten to match live ci.yml. | M0 / docs |
| 2026-08-25 | **Setup manual.** `docs/operations/environment_setup.md` rewritten as a full operator's guide (env-var reference reconciled against `src/lib/env.ts` — fixed wrong required/optional claims, removed nonexistent vars GROQ_API_KEY/INNGEST_BASE_URL/TEST_DATABASE_URL rows, NEXT_PUBLIC_POLAR_PRODUCT_ID/SUCCESS_URL; step-by-step acquisition guides for Postgres/secret generation/GitHub+Google OAuth/Polar sandbox/Inngest Cloud keys/Stripe webhook secret/Sentry token/ngrok domain; local walkthrough + smoke test; ngrok internet-exposure flow; Vercel+Neon+Inngest Cloud deployment with the Polar `server:"sandbox"` hardcode called out as a go-live blocker). Stale references removed: `scripts/dev-db.ps1`, `test/db.ts`, `prisma:studio` script, ESLint->Biome. | docs |
| 2026-08-24 | **M0-06 Testing Library.** `@testing-library/react` + jsdom `dom` vitest project (`*.dom.test.{ts,tsx}`, jest-dom matchers); first component test `upgrade-modal.dom.test.tsx` (3 tests). Root-caused the long-standing "`@/` imports fail under Vitest" quirk: inline `test.projects` don't inherit root-level `resolve.alias` or `setupFiles` — both now declared per project in `vitest.config.ts`, unblocking `@/` runtime imports in all future tests (AGENTS.md §6 updated). Gates: 81/81 tests, tsc clean, biome clean. | M0 |
| 2026-08-25 | **Prisma 7 upgrade (6.16.3 → 7.9.1).** Schema datasource `url` removed (P1012 in v7) — connection URL moved to new `prisma.config.ts` (loads `.env` via `dotenv/config`; the v7 CLI no longer auto-loads `.env`). Generator swapped `prisma-client-js` → `prisma-client` (plain ESM TypeScript emitted to `src/generated/prisma`, no barrel index) and all `@/generated/prisma` imports repointed to `@/generated/prisma/client` across 18 files. Runtime now requires a driver adapter: `@prisma/adapter-pg` wired into `src/lib/db.ts` (`new PrismaClient({ adapter })`, URL via `ensureEnv()`). Verified: 15/15 migrations apply to dev DB, live query + enums OK, Turbopack boot OK, tsc/biome clean, 78/78 tests. | infra |
| 2026-08-24 | **M0 cleanup wave.** AF-M0-04: fabricated `userId` removed from tRPC context (`createTRPCContext` returns `{}`; identity only via `protectedProcedure`'s Better Auth session → `ctx.auth`; `baseProcedure` documented as unauthenticated/tenant-unsafe). AF-M0-09: real `README.md` (honest status, stack, quick start, gates), root `AGENTS.md` created (truth files, spec routing, hard rules, conventions, workflow), `docs/README.md` ADR index completed (0007). AF-M0-02: sentry example page/route deleted; migration `20260824100000_drop_post_table` drops the dead tutorial `Post` table from deployed DBs. AF-M0-03: Polar product ID/slug externalized (`POLAR_PRODUCT_ID`, `POLAR_PRODUCT_SLUG`, `NEXT_PUBLIC_POLAR_PRODUCT_SLUG` via `src/lib/env.ts`; empty products list when unset so the app boots without billing; client checkout buttons read `polarProductSlug`; `.env.example` updated). Gates after each commit: 78/78 tests, tsc clean, biome clean. Remaining M0: M0-06 Testing Library + docs sync (needs live DB for integration tests). | M0 |
| 2026-08-24 | **Hardening wave 2.** AF-A-02: SSRF egress guard (`egress-guard.ts` — scheme/host allowlist, private-IP + metadata-endpoint blocks, DNS-resolve check) + 30s timeout + response byte cap on http-request. AF-A-03: ADR-0007 — Handlebars runtime compilation kept, sandbox defaults pinned (`noEval`, prototype guards), `compileTemplate` wrapper; raw `Handlebars.compile` banned. AF-A-04: Zod v4 save-boundary validation (`saveWorkflowInputSchema`, discriminated union over the 10 node types; cuid2 node ids → length-bounded `nodeId()`); editor save filters untyped nodes; 9 schema tests. **AF-A-05: per-node execution traces** — additive `NodeExecution` model (migration `20260824090000`) with status/attempt/order/durationMs; engine writes `trace-start/end/fail:<nodeId>` steps around every executor plus bulk SKIPPED rows for unreached nodes; execution detail UI renders the ordered trace list; 5 helper unit tests (`src/inngest/trace.ts` kept free of generated-client runtime imports for Vitest). Gates: 78/78 tests, tsc clean, biome clean. M-A complete. Remaining M0: AF-M0-04 real auth ctx, AF-M0-09 README/AGENTS, M0-06 Testing Library remainder, M0-02/03. | M-A |
| 2026-08-24 | **Hardening wave 1.** AF-A-01: per-workflow `webhookSecret` (migration `20260822030000`) + Stripe signature verification; both webhook routes rewritten (400/404 semantics, `secureCompare`), trigger dialogs embed secret URLs. AF-M0-07: redacting structured logger + Sentry `beforeSend` redaction. AF-M0-08: Zod env validation at boot (`src/lib/env.ts`, `SKIP_ENV_VALIDATION=1` for CI). AF-A-06: lint debt cleared (303→0 errors), CI gate live. AF-A-07: single `ENGINE_RETRIES` retry policy + truncated error stacks. AF-M0-06: Vitest+Playwright+GitHub Actions harness, 16 unit tests. **D15 found & fixed:** toposort self-edge false-cycle bug. Remaining M-A: A-02 SSRF/timeout, A-03 template-injection decision, A-04 config validation, A-05 traces. | M-A / M0 |
| 2026-08-22 | **Docs↔code reconciliation.** Full audit of the repository against this doc set found the prior snapshot wrong in both directions: (a) claimed-missing capabilities that exist — Inngest execution engine w/ 10 executors, canvas save, executions/credentials UIs, google-form/stripe triggers; (b) claimed-shipped work that does not exist — test harness/CI, node SDK registry, enum drop, logger/env modules, dead-code cleanup. All M0/M1 "shipped" statuses reverted to todo; new milestone M-A opened for audit findings S1–S14. Versions corrected throughout (Next 15.5.4 / Prisma 6.16 / Inngest 3.44 / Biome — not 16/7/4.2/ESLint). Prior changelog rows below describe aspirational state and are retained only as history of intent. | — |
| *2026-08-21* | *Aspirational entry (enum drop, node registry) — not present in code.* | — |
| *2026-08-14* | *Aspirational entries (Node SDK scaffold, backend hygiene) — not present in code.* | — |
| *2026-08-07..08-02* | *Aspirational entries (dead-code removal, Polar externalization, env validation, pagination fix, test harness, docs set creation) — except docs-set creation itself, none are present in code. Pagination count is nonetheless correct in current code (pre-existing implementation).* | — |
| 2026-10→11 (actual git) | Tutorial lessons merged to `main` as PRs #17–#30: entity infra, dashboard/auth/billing, canvas, workflow CRUD, node templating, executions UI, credentials, HTTP node, OpenAI/Anthropic/Gemini/Discord/Slack nodes, google-form/stripe triggers, deployment wiring. This is the true provenance of everything in §3. | pre-M0 |

---

## 9. How to update this document

When you complete a task:

1. Move the item from §4 to §3 with a file-path evidence reference.
2. Close any defect in §5 that the change fixes; add any it introduces.
3. Update the §2 percentage for the affected PRD area — **only if the capability actually works end to end**, not if the code merely compiles.
4. Update the §6 milestone status.
5. Add a §8 changelog line.
6. If it is the end of a milestone, re-measure §7.

**Do not mark anything green that you have not personally exercised.** An honest progress document is the only thing standing between this project and a rebuild.

| 2026-09-01 | **AF-UI-01**: Created KpiCard component and /test-kpi page to match UI designs. | M7 |

| 2026-09-01 | **AF-UI-02**: Styled ExecutionsOverTimeChart to match the Sales Trend dark theme design. | M7 |

| 2026-09-01 | **AF-UI-03**: Made the trend sparkbars thicker and more visible across all dashboards. | M7 |

| 2026-09-01 | **AF-M8-21**: Configured alert delivery and external uptime monitors in slos.md. | M8 |

| 2026-09-01 | **AF-M8-23**: Payment doesn't change the plan - Implemented Polar webhooks in auth.ts to automatically upgrade/downgrade organization plans based on subscription events. | M8 |

| 2026-09-01 | **AF-M8-12**: Deleted deprecated legacy AI nodes (OPENAI, ANTHROPIC, GEMINI) and their migration script since no references exist in the database. | M8 |

| 2026-09-01 | **AF-M8-23 (hardening)**: Moved the three hard-coded Polar product UUIDs out of `auth-webhooks.ts` into `POLAR_PRODUCT_ID_*`; added `POLAR_WEBHOOK_SECRET` to the env schema and `.env.example`; made every refusal log instead of returning silently. 14 unit tests. Recorded the task in tasks.md, where it had never been written down. | M8 |

| 2026-09-01 | **AF-M8-13 (SYSTEM half)**: Built `npm run notify:system` — the broadcast surface support.md §6 already documented and nothing implemented. Dry-run by default, dedupe key per workspace. Approvals half stays blocked on the approval node (AF-P2-E). | M8 |

| 2026-09-01 | **AF-M8-10 (wiring)**: Legal pages linked from the landing footer and signup (4.7); `/support` built and linked (5.3); the footer's hardcoded "All Systems Operational" badge now links to /status instead of asserting health. | M8 |

| 2026-09-01 | **AF-M8-05 (target + harness)**: Documented a derived concurrency target (100 req/s = one PRO tenant's own bucket) in `docs/operations/load_test.md` and built `npm run load-test`. No run performed — needs a deployed environment and an API key. | M8 |

| 2026-09-02 | **AF-M8-13 (rework)**: Replaced the script-only SYSTEM producer with `system-notifier.ts`, ported from the parallel `af-m8-23-polar-plan` branch where the better implementation had been written independently. The script's direct `prisma.notification.createMany` bypassed `writeNotifications` — the documented single write path — so the replay guard was being re-implemented instead of reused. Now batched (200/statement), targetable by organization, with a tested `previewSystemBroadcast` behind the dry run. 13 integration tests. | M8 |

| 2026-09-02 | **Verification**: full suite green on `af-m8-12-delete-legacy-ai-nodes` — tsc, biome, 955 unit+dom, **127 integration**, production build. Integration had never been run before; the test database now comes up under Podman with `--pull=never` and the host-network recipe. | M8 |

| 2026-09-02 | **Removed `/test-kpi`** — a scratch page of hardcoded e-commerce figures that was shipping in the production build. 52 static pages → 51. `KpiCard` kept but now has no caller. | M8 |

| 2026-09-02 | **AF-M8-23 closed**: operator configured `POLAR_WEBHOOK_SECRET` and the three `POLAR_PRODUCT_ID_*` values. Verified by resolving the plan map — `ensureEnv()` passes, 3 distinct UUIDs, all three plans mapped. No test purchase made yet, so the dashboard endpoint itself is still unproven. Checklist 1.4/1.5/1.7 closed. | M8 |

| 2026-09-02 | **AF-M8-26**: renumbered from the second, colliding **AF-M8-21** entry (same collision class as AF-M8-14). AF-M8-21 is the problem and the plan; AF-M8-26 is the delivery. Operator created the uptime monitor 2026-09-02, closing checklist 3.5 — not verifiable from the repository, and no alert has been seen to fire yet. | M8 |

| 2026-09-02 | **AF-M8-19 done**: upgraded to `ai@6` + `@ai-sdk/{openai,anthropic,google}@3` — one major, not the two `npm audit fix --force` proposed. All six LOW advisories cleared: **0 vulnerabilities**. The only forced code change was v6's widened `usage` shape; added `pickRunUsage` so the persisted trace shape stays identical to v5. Removed the now-redundant `provider-utils → undici` override. | M8 |

| 2026-09-02 | **AF-M0-06: CI was never running the integration suite.** The workflow started Postgres and migrated it but never set `TEST_DATABASE_URL`, so all 127 integration tests skipped themselves and CI went green having proven nothing about cross-tenant isolation, the public REST surface, or API-key auth. Now wired: `npm test` runs 1088 tests instead of 961. | M0 |

| 2026-09-02 | **Integration suites could hit the dev database.** Skipping was opt-in per suite and `polar-webhooks.integration.test.ts` had no guard, so with `TEST_DATABASE_URL` unset it created and deleted users/orgs in whatever `.env` pointed at. Guard restored, and the setup now repoints `DATABASE_URL` at an unresolvable host when the test DB is absent — an unguarded suite fails loudly instead of mutating real data. | M0 |

| 2026-09-02 | **AF-M0-08 done**: `environment_setup.md` had drifted 13 variables behind `.env.example` (Resend, Polar webhook + per-plan product ids, legal/support block). All documented; also removed a quick-start step invoking `migrate:legacy-ai-nodes`, deleted by AF-M8-12. | M0 |

| 2026-09-02 | **Record corrections**: AF-A-01 and AF-M1-06 were both complete but still marked 🟡 — every acceptance item checked, AF-M1-06's own status reading "done" since 2026-08-29. Closed. | M-A / M1 |
